#!/usr/bin/env python3
"""bin/rl-reflect — corrections → CLAUDE.md/rules 反映 CLI。"""
import sys
from pathlib import Path

_lib = Path(__file__).resolve().parent.parent / "scripts" / "lib"
_scripts = Path(__file__).resolve().parent.parent / "skills" / "reflect" / "scripts"
sys.path.insert(0, str(_lib))
sys.path.insert(0, str(_scripts))

from reflect import main

if __name__ == "__main__":
    main()
