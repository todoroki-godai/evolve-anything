#!/usr/bin/env python3
"""bin/rl-discover — rl-anything discover CLI。"""
import sys
from pathlib import Path

_lib = Path(__file__).resolve().parent.parent / "scripts" / "lib"
sys.path.insert(0, str(_lib))

from discover import main

if __name__ == "__main__":
    main()
