# コンポーネント詳細

CLAUDE.md のコンポーネント表の詳細版（SoT）。CLAUDE.md には 1 行サマリのみを置き、
設計経緯・根拠・issue/ADR 参照を含む詳細はこのファイルに記録する。

**運用ルール**: 新コンポーネント追加・既存変更時は、このファイルに詳細を書き、
CLAUDE.md のサマリ表には 1 行（名前 + 一言 + 参照）だけ追記する。

| コンポーネント | 説明 |
|----------------|------|
| Observe hooks (20個) | LLM コストゼロで使用・エラー・修正フィードバック・ワークフロー・ファイル変更を自動記録 |
| Auto Trigger | セッション終了・corrections 蓄積・ファイル変更時に evolve/audit 実行を自動提案（`trigger_engine.py`） |
| `userConfig` | CC v2.1.83 manifest.userConfig で trigger 閾値（auto_trigger/interval/cooldown 等）と cleanup スキル prefix（`cleanup_tmp_prefixes`）・slow command 閾値（`slow_threshold_ms`）・subagent 警告閾値（`subagent_warning_threshold`=5）と計測時間窓（`subagent_window_minutes`=5、累積でなく直近 N 分のバーストを検出）・スキル数上限（`max_skill_count`=30）・correction pre-flight 閾値（`correction_preflight_threshold`=3）・error pre-flight 閾値（`error_preflight_threshold`=3）に加え `skill_lr_budget`・`spec_trigger_enabled`・`idiom_autopromote_daily_cap` 等を含む 20 項目をプラグイン有効化時に設定可能（実体は `.claude-plugin/plugin.json` の `userConfig`） |
| `genetic-prompt-optimizer` | corrections/context ベースの LLM 1パス直接パッチで最適化 + **GEPA 数値ガードレール（#120）**。GEPA 本番知見（最適化サンプルは 20-100 件が最適・500 件超で過学習＋プロンプト肥大化 / 上限を設けると性能ほぼ不変で 4x 圧縮）を当 PJ に取り込む: ⑴ 入力 corrections は `MAX_CORRECTIONS_PER_PATCH=10`（過学習側上限を厳守）、context の pitfalls.md 全文投入は `MAX_CONTEXT_PITFALLS_CHARS=8000` で切り詰め（入力肥大化ガード）。⑵ 生成パッチの文字数上限 `line_limit.max_chars_for(max_lines)=max_lines×MAX_CHARS_PER_LINE(200)` を `regression_gate.check_gates(max_chars=)` の新ゲート `char_limit_exceeded` として追加（行数ゲートを通っても 1 行が異常に長い bloat を捕捉。両 caller = `run_regression_gate` / `generate_candidate` に配線）。⑶ 閾値 200/8000 は他ドメイン数値の流用でなく、当 PJ の skill/rule/pitfalls 実ファイルへの **dry-run 較正**で決定（誤ブロック 0 件・headroom 1.5〜3.4x。[[learning_gate_design_needs_real_corpus_dryrun]]）。max_chars 未指定は char ゲート非適用の後方互換 |
| `evolve-loop-orchestrator` | ベースライン取得→バリエーション生成→評価→人間確認のループ統合 |
| `evolve-scorer` エージェント | オーケストレーター(haiku) + 3サブエージェント並列(tech/struct=haiku, domain=sonnet)で3軸採点 |
| `skill-triage` | テレメトリ+trigger evalで CREATE/UPDATE/SPLIT/MERGE/OK の5択判定（`scripts/lib/skill_triage.py`） |
| `trigger-eval-generator` | sessions.jsonl+usage.jsonl → skill-creator互換 evals.json 自動生成（`scripts/lib/trigger_eval_generator.py`） |
| `evolve-skill` | 特定スキルに自己進化パターン（Pre-flight / pitfalls.md）をピンポイント組み込み（`assess_single_skill` + `apply_evolve_proposal`） |
| `agent-brushup` | エージェント定義の品質診断・改善提案・upstream監視 + model exact-ID pin 検出（`scripts/lib/agent_quality.py`）。`check_model_pin()` が frontmatter `model:` の exact ID（`^claude-[a-z]+-\d+` パターン、将来モデル名に頑健）を silent stale リスクとして警告（severity medium・推奨エイリアス提示）。エイリアス（opus/sonnet/haiku/fable/inherit）・未指定は警告しない。背景: 新モデルリリース後も古い ID に固定される事故が global agents 4 件で実発生（#449）。**tools 宣言と実付与の乖離検出（#130）**: `check_tools_grant_divergence()` が「frontmatter に `memory:` あり + `tools:` 宣言に Write/Edit なし」の agent を列挙し issue `tools_grant_divergence`（severity low・advisory）に surface。真因は harness 仕様＝`memory:` があると Persistent Agent Memory 書込用に Write/Edit が **tools 宣言と独立に自動付与**されるため、助言専用設計の agent でも実行時は書込可能になる。実行時付与は静的 audit から見えないため宣言ベースのヒューリスティックで決定論検出する。`tools:` 宣言自体が無い agent（全ツール継承）は乖離が存在しないため対象外（FP 回避）、tools は文字列/YAML リスト両形式対応（`_normalize_tools`）。列挙のみ・auto-fix なし・本文の安全弁有無の意味判定はしない（Phase 1）。発見経緯: 2026-07-03 PC 環境手動監査で助言専用 3 agent（ambiguous-intent-resolver / refactor-engineer / tacchi）の実付与乖離を確認、#449 の check_model_pin と同型で配線（check 関数 → `check_quality` issue → agent-brushup SKILL.md レポート行） |
| `critical-instruction-compliance` | スキル指示の遵守保証サイクル — critical行抽出+calm/directリフレーズ+違反検出+pitfall自動学習（`scripts/lib/critical_instruction_extractor.py`） |
| `second-opinion` エージェント | cold-read セカンドオピニオン（startup/builder/general 3モード）。codex 不要で Agent ツールのみで動作 |
| `growth-level` | env_score (0.0-1.0) → Lv.1-10 + 日英称号マッピング。audit がキャッシュに保存、greeting で表示（`scripts/lib/growth_level.py`） |
| `optimize_history_store` | accept/reject 履歴（fitness calibration 母集団）の正準ストア。保存先を `DATA_DIR/optimize_history/<slug>.jsonl` の **PJ スコープ**に集約し、従来の 3-way split-brain（plugin generations 更新リセット / run_loop の `.evolve-loop` 孤立 / readers 不一致）を解消。slug は worktree 安全に `git --git-common-dir` 親 basename で解決（`--show-toplevel` basename は worktree 名を返すため不可）、git 外は `_unattributed`。optimize/run_loop/evolve-diff の writer と fitness_evolution/discover/audit の reader 計 6 箇所が単一経路を共有（`scripts/lib/optimize_history_store.py`、[ADR-031]） |
| `evolve_decisions` | evolve 提案の accept/reject を**日次ループで決定論キャプチャ**し optimize_history を育てる（#360-A, [ADR-041]）。空 store の根因は「accept/reject 記録が SKILL.md の MUST（手で `record_evolve_diff_decision` を叩く）止まりで決定論コードから呼ばれず実行されない」＝`install ≠ enforcement` の SKILL.md 版。emit→（インライン適用）→drain の2相で **accept=適用実績（ディスク差分）/ reject=明示却下 / skip=記録しない**（C: ハイブリッド）。`emit_decisions`（run_evolve 末尾）が discover の `matched_skills` 候補の before_sha をキュー `DATA_DIR/evolve_decisions/<slug>.jsonl` に上書きスナップショット、`ingest_decisions`（SKILL.md Step 7.8 drain）が `after_sha != before_sha` を accept・明示却下 id を reject として `record_evolve_diff_decision` 経由で optimize_history へ冪等記録（fitness_func=`skill_quality` で母集団は混合でなく増量）。accept がディスク差分由来なので #360 の失敗モード（記録ステップ未実行）を構造的に塞ぐ。対象は matched_skills（skill diff）+ skill_evolve の high/medium 適性提案（両者とも SKILL.md content を変えるので skill_quality で均質）。remediation の fix は target 異種（rules/hooks/構造）で均質性を壊すため対象外。**dry-run 運用の根治（#400 バグ#1）**: 旧版は emit が `--dry-run` 時にキュー（before_sha）を書かず Step 7.8 も「dry-run のため未記録」でスキップしたため、標準フロー（`evolve --dry-run` で分析→assistant が対話適用）では accept が**永久に記録されず optimize_history が空＝fitness が `0/30` から動かない**根因になっていた（ADR-041 の効果が実運用で出ない＝「テスト緑だが本番経路 untested」「dry-run 検証は apply 境界を越えないので構造的に観測不可能」）。修正後は `ingest_decisions(pending=result.evolve_decisions.pending)` で**キュー不在でも result 同梱の pending を直接消費**し、apply 後のディスク差分から accept を取る。Step 7.8 は分析が dry-run でも apply 完了後に必ず ingest を `dry_run=False` で実行（純プレビューは全件 skip に落ち self-correcting）。回帰は **apply 境界をまたぐ E2E**（emit dry-run→apply→ingest→fitness load_history が +1 を観測）で封じる。writer(ingest)/reader(fitness load_history) は同一正準ストア `optimize_history_store.history_path(resolve_slug())` を共有（パス分裂なし確認済）。**drain の enforcement gap 是正（#402）**: #400 修正後も ingest を呼ぶのは SKILL.md prose だけ（決定論コード非経由）で、assistant が飛ばすと再び空になる `SKILL.md MUST ≠ enforcement` の穴が残っていた。`drain_pending`（`evolve --drain` の実体）で SKILL.md を inline python から**単一コマンド**へ集約し、emit が `--dry-run` でも env 非依存固定パス `~/.claude/evolve-anything/evolve_pending/<slug>.json` に「未 drain 提案」マーカー（before_sha 付き、評価 store/queue とは別の運用状態、drain でクリア）を記録、**SessionStart hook**（`restore_state._deliver_evolve_drain_reminder`）が `undrained_applied`（marker の before_sha と現ディスク sha を突合・`optimize_history` 非読込）で「適用済みなのに未 drain」を検出し `evolve --drain` を促す。drain は **tool 文脈（CLI）**で走り reader と同一 DATA_DIR に書くため hook/tool の DATA_DIR split（#358）を踏まない。Stop hook auto-drain は apply タイミング非依存にできず env scrub も脆弱で不採用（second-opinion 反映）、timing は「次 SessionStart で見る」ことで構造回避。マーカーの実 home 汚染は conftest autouse + harness で隔離。**SessionStart 自動 drain へ昇格（#421）**: #402 のリマインド表示のみ（実 drain は SKILL.md prose 依存＝install ≠ enforcement の穴が残存）を、`restore_state._deliver_evolve_drain` で apply 済み提案を**実際に drain して optimize_history へ自動記録**する人手ゼロ回収へ昇格。**Stop ではなく SessionStart** で走るため #402 で不採用とした Stop hook auto-drain の apply タイミング問題は踏まない（apply は前セッションで完了済み）。レイテンシ予算（pitfall_hot_hook_eager_import）: pending marker 不在は MARKER_ROOT ディレクトリ存在 → slug 解決 → marker ファイル存在の軽量 early-return で重い経路に入らない（実測 no-marker 0.001 ms/call、旧リマインドは無条件 `resolve_slug` で ≈6.5 ms/call だったので高速化）。`undrained_applied` をゲートに未 apply のときは marker を温存して将来の apply を取り逃さない。書き込み先は `restore_state._resolve_canonical_history_file`（marker ゲート付き `rl_common.resolve_data_dir`）で tool reader と同一の正準 DATA_DIR に固定し #358/#364 の DATA_DIR split を踏まない。drain 中の例外で hook を落とさない（degrade）。決定論・LLM 非依存（`scripts/lib/evolve_decisions.py`） |
| `evolve_reconcile` | evolve result の相互排他 reconcile / observability 昇格（#400）。`evolve_introspect.reconcile_split_archive`（split↔archive, #301 #302）と対をなす2つを持つ: ① **`reconcile_skill_evolve_archive`（#400 バグ#2）** — 同一スキルが prune の archive 候補かつ skill_evolve high/medium（自己進化を組み込め）と評価される矛盾を archive 優先で解消。assessments の該当を `suppressed_by_archive` へ降格（emit_decisions が high/medium のみ拾うので母集団からも外れる）+ high/medium カウント再計算 + remediation の `skill_evolve_candidate` issue を batch 各バケツから除外し count 整合。除外は `skill_evolve.evolve_suppressed_by_archive` に記録（silent に消さない）。evolve/ Phase 4.2 が emit より前に発火。② **`build_remediation_batch_skip_observability`（#400 バグ#6）** — 低 confidence の proposable まとめスキップ件数を `result["observability"]["remediation_batch_skip"]` に決定論で昇格し Step 3.8 が必ず surface（SKILL.md の surface MUST 依存をやめ silence≠evaluated を構造担保）。0件でも「✓ 0件」を残す。`_collect_archive_skills` は evolve_introspect から import（一方向・循環なし）、file-size budget のため分離。決定論・LLM 非依存（`scripts/lib/evolve_reconcile.py`） |
| `token_usage_store` | PJ 別 LLM トークン消費の DuckDB SoR — PK uuid で冪等 ingest、`token_usage.db` にスキーマ自動作成（`scripts/lib/token_usage_store.py`） |
| `token_usage_ingest` | `~/.claude/projects/<pj>/*.jsonl` の `message.usage` を walker で取り込み、days mtime filter + batch insert。表示 slug は transcript の `cwd`（曖昧さなし）を `pj_slug_fast` で導出 → 無ければ `pj_slug.pj_id_to_slug`（fs 貪欲復元）に委譲（旧 `-` 末尾 split 化け `figma-to-code`→`code` / `sys-bots`→`bots` を根治・#68）。表示側 `top_n_consumers` と fleet `_inject_token_metrics` も pj_id 由来 basename で統一し join desync を回避（`scripts/lib/token_usage_ingest.py`） |
| `token_usage_query` | TOP-N / WoW スパイク / cache hit 異常 / PJ ドリルダウン (session/model/week)。fleet status・tokens サブコマンド・audit セクションが利用（`scripts/lib/token_usage_query.py`） |
| `auto_memory_runner` | Stop hook は corrections 直近 5 件を生成前ゲート（`memory_gating`）して PJ スコープキュー `DATA_DIR/auto_memory_queue/<slug>.jsonl` に enqueue するだけのゼロ LLM（claude -p 全廃, ADR-037 Phase 2）。slug は memory dir と一致させるため `project_name_from_dir(CLAUDE_PROJECT_DIR)`。内容ハッシュ in-queue dedup（`(session_id,timestamp)` 列の sha256）で毎 Stop の重複を防ぐ（`hooks/auto_memory_runner.py`） |
| `auto_memory_broker` | auto-memory の生成+belief ゲート+memory 書込を担う2相ブローカ（ADR-037 Phase 2）。`emit_memory_requests`（Phase A: キュー record→prompts, 決定論・IO なし）→ assistant インライン生成（subscription 課金）→ `ingest_memory_results`（Phase C: 応答回収→生成後ゲート `belief_entropy`→new-file-per-entry .md 書込+**temporal provenance 書込**+append-only index+importance+archive→stored/blocked をキューから消化, 空応答は再試行のため残置）。**APEX-MEM provenance 書込（#2）**: .md 生成直後・importance 採点の **前** に `_apply_temporal_metadata` が `memory_temporal.write_temporal_metadata` 経由で `valid_from`（生成時刻）+ `source_correction_ids`（その memory を生んだ corrections の `make_source_correction_id` 列＝memory→correction 単方向因果リンク）を frontmatter へ決定論で埋める。reader 側（`parse_memory_temporal`/`build_temporal_memory_warnings`/instructions_loaded stale フィルタ）は実装済みで write 側が休眠していた配線を活性化。`decay_days`/`superseded_at` は書かないため `is_stale`/`is_superseded` 非発火＝純加算（opt-in gate 不要）。importance 採点前に書くことで `compute_importance_score` の correction_bonus が乗る。evolve SKILL.md Step 6.5 が drain を駆動（手動 CLI 止まりにしない）。決定論・LLM 非依存（`scripts/lib/auto_memory_broker.py`） |
| `meta_quality` | スキル追加前の meta-skill 品質フィルタ — 再利用頻度と Jaccard 類似度で CREATE/REVIEW/SKIP を判定。`skill-triage` の CREATE 判定パスに組み込み（`scripts/lib/meta_quality.py`） |
| `triage_ledger` | SKIP 判断に状態（TTL・再発カウンタ）を持たせる正準ストア（#308）。`meta_quality_check` のステートレス SKIP を `DATA_DIR/triage_decisions/<slug>.jsonl` の **PJ スコープ**に永続化（slug は optimize_history_store と同じ worktree 安全解決）。3層トリガー: ①抑制（SKIP 済み&クールダウン内&再発閾値未満→個別表示せず `skip_suppressed_summary` の「SKIP 抑制 N件 ✓」1行に畳む。0件でも残す＝silence≠evaluated）②再発エスカレーション（窓内 `times_skipped>=ESCALATE_N`=3→SKIP→REVIEW 昇格）③TTL 切れ（`now>decided_at+ttl_days`=45日→🔄1回だけ強制再評価）。`skill_triage.triage_skill` の CREATE→meta SKIP パスに配線し evolve のたびに自動発火（手動 CLI 止まりにしない）。evolve SKILL.md Step 3.8 が summary を必ず surface。last-write-wins append + `compact()` で肥大化抑制。**`evolve --dry-run` 時は書き込まない**: `apply_ledger(persist=False)` が3層判定は計算しつつ `upsert_record` を全経路スキップし、`triage_skill`/`triage_all_skills`/`evolve/` が `dry_run` を書き込み層まで貫通させる（「変更なし」契約を ledger 層で担保＝#308 follow-up の dry-run 副作用 fix）。決定論・LLM 非依存（`scripts/lib/triage_ledger.py`） |
| `constraint_decay` | セッション後半 30% ターンに集中する correction を検出して decay_rate を算出。O(N+M) pre-index・30日 mtime フィルタ。`run_discover()` に統合（`scripts/lib/discover/patterns.py`） |
| `negative_transfer` | スキル追加イベント前後の success rate delta を計測し `delta < -0.05` で負の転移フラグを付与（`scripts/lib/audit/usage.py`）。`compute_component_transfer` は更新コンポーネント（追加スキル）別に isolation window で delta を分離し「どの更新が回帰させたか」を帰属（#288, arXiv 2605.30621 ablation）。observability contract の `build_negative_transfer_section` で evolve のたびに surface（決定論・LLM 非依存） |
| `eval_saturation` | trigger eval 飽和度診断（#292, TASTE arXiv 2605.28556）。`trigger_eval_generator` の順生成 eval が「緑でも頑健でない」飽和兆候を eval 実行なし・LLM 非依存・決定論で測る（`low_negative_coverage` / `easy_negatives`（trigger 語が取れる skill のみ）/ `thin`）。`build_eval_saturation_section`（`audit/sections_eval.py`）を observability contract の calibration_drift 直後に登録し evolve のたびに同帯で surface。eval-sets は環境グローバル（DATA_DIR 配下）系の builder（`scripts/lib/eval_saturation.py`） |
| `subgoal_scorer` | BES 後ろ向き分解（#253）。候補を 5 サブゴール（frontmatter/trigger/correction/line_budget/slop_free）に分解し密な中間フィードバックを返す。`optimize_core.run_subgoal_scoring` がラップ、決定論・LLM 非依存（`scripts/lib/subgoal_scorer.py`） |
| `evolution_operators` | BES 前向き進化探索（#256）。crossover/mutate/select_parents(fitness-proportional)/evolve_generation の決定論演算子。evolve-loop の `--evolve-search` が subgoal fitness を consume（`scripts/lib/evolution_operators.py`） |
| `memory_trace` | MemTrace 帰属診断（#254）。episodic 検索エラーを misretrieval/context_drift/corruption の3類型に分類し `event_id` 帰属。LLM/oracle 不使用、`audit/memory.py` が利用（`scripts/lib/memory_trace.py`） |
| `slop_detector` | AI slop 辞書検出（#255）。日英 10 パターンを決定論 regex で検出、`detect_slop -> SlopResult(slop_score, hits)`。constitutional に 10% ブレンド + subgoal slop_free に接続（`scripts/lib/slop_detector.py`） |
| `skill_extractor` | SIRI ①の成功軌跡採掘（#238 Phase 1 実装 → #291 配線）。`sample_trajectories` がセッション履歴の `<command-name>` ターンを採掘し `extract_skill_candidates` が generalizability_score 付き候補を生成。`run_discover` が発火し `generalizability_score >= TRAJECTORY_SKILL_SCORE_THRESHOLD`（既定 0.25）でフィルタ、`trajectory_skill_candidates` に surface しつつ triage の `missed_skill_opportunities` 形式へ変換して合流。各候補は Workflow-to-Skill (arXiv 2606.06893) の4軸分解 `decomposition`（routing=発火文脈 / workflow=実行プロファイル / semantics=何をするか / attachments=anchor の広がり・distinct session 数で定着度を測り単一セッション由来なら session_bound）を決定論で持ち、採用判断に効く routing/attachments を merged にも持ち上げて候補テーブルに surface（#381、`decomposition.py`）。`routing.trigger_keywords` のノイズは3層で除去（#387）: ① **機構ターンフィルタ**（`trajectory_sampler._is_machinery_prompt`）が compaction サマリ/SKILL.md 注入/`<task-notification>`/`<system-reminder>`/Stop hook feedback という「type=user だが非発話の harness 注入ターン」を直前プロンプト探索で飛ばす（パス・tool-use-id がキーワードを汚す最大の出所を source で断つ）② static stopword 拡充（英語機能語 + 拡張子 token、環境非依存）③ corpus document-frequency 減衰（`corpus_frequent_tokens`、環境固有の遍在語を allowlist せず DF で落とす＝`learning_detector_fp_context_not_allowlist` 準拠）。合成 fixture で露見せず実 PJ E2E で特定（`learning_synthetic_fixture_false_confidence`）。LLM 非依存、discover=evolve recurring ループに配線済（手動 CLI 止まりにしない）。**failure-rollout マイニング + 失敗の罠軸（#27）**: 旧 `_determine_outcome` は success/unknown しか返さず failure を**誰も producer していなかった**ため、下流 `effectiveness.compute_contrast` の success/failure コントラスト分岐（`CONTRAST_BOTH` / `CONTRAST_ALL_FAILURE`）と `skill_extractor._compute_success_rate` の `outcome != "failure"` 判定が**到達不能な休眠配線**だった。この穴を `trajectory_sampler._has_error_tool_result`（user ターンの message.content list 内 `type=="tool_result"` かつ `is_error` truthy を型ガード付き検出。observe/pitfall_lint と同じエラー判定構造）+ `_determine_outcome` の**保守的 failure 判定**（window 末尾が未回復エラー＝エラー tool_result の後に回復 assistant が無いときのみ failure。エラー後に assistant 回復していれば success のまま＝一過性エラーを failure 扱いしない FP ガード。issue が FP 多発で再設計とゲートしたため保守側に倒す）で配線して活性化。さらに `decomposition` に5軸目 `failure_analysis`（failure_count / failure_rate / sample_failure_triggers 最大3件 / is_failure_derived）を追加し、失敗がどの文脈に集中するかを surface（成功基準そのものは workflow.outcomes が担う）。merged にも `failure_analysis` を持ち上げ discover 候補テーブルへ surface。完全に決定論・LLM 非依存（`scripts/lib/skill_extractor/`） |
| `skill_rm` | Skill-RM スキル軸の異種基準統一報酬（#304, arXiv:2606.03980）。スキルごとに異なる成功条件を全スキル共通な3軸（`structure`=CSO 構造品質 / `success`=invoke 直後 60s に correction 無しの暗黙成功率 / `validity`=invoke あたりエラー率の補数）へ射影し、単一報酬で横断評価する。軸合成は `environment._normalize_weights` を数式単一ソースとして再利用（`SKILL_RM_BASE_WEIGHTS` を base 引数で渡し、算出できた軸のみで再正規化）。fitness の「軸別」統合とは直交（スキル別）なので overall には混ぜず、`compute_environment_fitness` の `result["skill_rm"]` に per-skill 報酬分布（mean/spread/worst_skill）を surface して calibration drift の帰属（どのスキルが乖離源か）へ接続。対象スキル0件でも report に「評価したが対象なし ✓」を1行残す（silence≠evaluated）。決定論・LLM 非依存、evolve/audit のたびに発火（`scripts/rl/fitness/skill_rm.py`） |
| pitfall 自動強制 (`pitfall_lint` / `pitfall_commit_gate`) | install + `pitfall-curate enable` で登録した pitfalls.md に対し、編集時 hook（PostToolUse・警告のみ）と commit 時ゲート（PreToolUse Bash・staged を検査し danger を exit 2 でブロック）の二段で正準フォーマットを自動 lint。判定は `normalize --check`（ok/drift/danger、書き換えなし）、台帳は `scripts/lib/pitfall_registry.py`（オプトイン・決定論、`unmanaged_candidates` で未登録を集合差検出。探索は `_DISCOVERY_IGNORE` で `.git`/`node_modules` 等に加え `worktrees` を除外し、`.claude/worktrees/<name>/...` の一時作業コピーを未登録誤検知しない＝#393）。自動書き換えはしない（silent wipe 防止、ADR-027）。audit は「育っている（エントリ3+件）のに未登録」の pitfalls.md を `Unmanaged Pitfalls` セクションで可視化し enable を誘導（evolve のたびに発火、liveness 判定は `parse.count_entries`）。pitfalls.md が1件でもある PJ では該当なしでも「評価したが対象なし ✓」を1行残し（沈黙=配線漏れ誤認を防止）、1件も無い PJ のみ非表示 |
| `agent_team` | エージェント編成ギャップの決定論検出（#326）。`agent_quality`（単体品質）と直交し、エージェント *間* の関係を 2 軸で見る: ① **役割重複**（description の役割語を Examples 除去＋ストップワード除去で集合化し `similarity.jaccard_coefficient` SoT で全ペア Jaccard、閾値 0.5）② **孤立**（他エージェント本文への名前出現を参照とみなし **入次数 0 かつ出次数 0** のみ孤立＝ルーター（出>0）と被参照の専門家（入>0）を除外）。observability builder `build_agent_team_section`（`scripts/lib/audit/sections_agent.py`、sections.py の hard 800 行直前のため分離）を `_OBSERVABILITY_BUILDERS` に登録し、audit を消費する evolve のたびに surface（手動 CLI 止まりにしない＝version≠enforcement）。2 個未満は対象外（None）、2 個以上はギャップ無しでも「✓ 該当なし」を残す（silence≠evaluated）。整理は人間判断（`/evolve-anything:agent-brushup`）。決定論・LLM 非依存（`scripts/lib/agent_team.py`） |
| observability contract (`audit/observability.py`) | 「必ず surface すべき observability 行」（glossary_drift / unmanaged_pitfalls）を単一ソース `_OBSERVABILITY_BUILDERS` に集約。markdown 経路（`report.generate_report`）と構造化経路（`collect_observability` → evolve が `result["observability"]` に格納し evolve SKILL.md Step 3.8 で必ず surface）の両方が同じリストを消費する。audit の 217KB markdown を選択読みすると中盤の observability 行が埋もれて surface されない問題（silence≠evaluated が観測性 fix 自身の配線で再発）を、生成側でなく出力経路の契約で塞ぐ。将来 observability 項目は builder を1行登録するだけで両経路に自動伝播（モグラ叩き回避）。[ADR-028] |
| `scaffold_advisory` (`scripts/lib/scaffold_advisory.py` + `bin/evolve-scaffold-advisory`) | advisory 3点セット（module + store + observability section）追加の scaffold（#118 (b)）。発端: ほぼ全 advisory 機能（#14/#15/#38/#64/#73 等）が「新モジュール + store_registry 登録 + observability builder + keyset snapshot + CONTEXT.md + CLAUDE.md」の多点同時更新を要求し、増加がこのパターンの直積で keyset snapshot 追従漏れ等のミス面も比例拡大していた。(a) 限界コスト低減は #115 の advisory 共通枠（`build_advisory_section`）で対応済み。本コンポーネントは (b) を担う: `scaffold_advisory(name, *, title, has_store) -> ScaffoldResult`（純関数・決定論）が ⑴ #115 共通枠を使った `sections_<name>.py` の builder stub（compute→applicable→render の TODO 付き雛形）を **テンプレ生成** し、⑵ 残りの手動配線（observability.py の import + `_OBSERVABILITY_BUILDERS` 登録行 / per-section テスト / CLAUDE.md 1行 / CONTEXT.md 用語 / store 有りは store_registry active 宣言 + `store_write` barrier + `test_write_barrier` の active store keyset snapshot 追従）を **チェックリスト** で明示する。observability の既知 key は `_OBSERVABILITY_BUILDERS` から動的導出されるため（`dogfood/invariants.py`）observability 契約側の別 snapshot 追従は不要で、keyset snapshot 追従は store を持つ場合だけチェックリストに出す（誤誘導しない）。CLI `bin/evolve-scaffold-advisory <name> [--title T] [--with-store] [--write]` は既定 dry-run（stub + checklist を print）、`--write` で stub を書き出す（既存ファイルは非上書き＝clobber 拒否・exit 1、不正名 exit 2）。生成 stub は実 audit パッケージに配置して import→呼出で沈黙 None を返すことを smoke test 済み。(c) 低トラフィック advisory の畳み込みは既存 `fold_clean_observability`（#49・全✓ clean と ℹ/データ不足 watch を `## ✓ 評価済みクリーン: …` の1行に collapse し、⚠/🔴 critical のみ full 展開＝個別行と floor 較正は維持）で実質対応済みのため本 PR では新規グループ section を追加しない。TDD 12件・決定論・LLM 非依存 |
| `evolve_introspect` | evolve メタ層の自己解析（#299）。evolve 完了後に `result` dict 全体を決定論で読み、3カテゴリの GitHub issue 候補を生成する: ① self_detection（split↔archive 矛盾・line budget 悪化提案）② runtime_errors（握り潰された phase 例外・observability 取得失敗）③ improvement_opportunities（系統的却下 type・calibration regression・整合性 drift＝`evolve_consistency` 合流 #377-5）。`run_evolve` 末尾が `result["self_analysis"]` に格納（手動 CLI 止まりにしない＝evolve のたびに自動発火）。0件でも各カテゴリ `summary_line` に「✓ 評価したが該当なし」を残す（silence≠evaluated）。起票は半自動（evolve SKILL.md Step 11 が人間承認 → `gh issue create --repo todoroki-godai/evolve-anything` 固定）。重複起票防止は body 埋め込みマーカー `evolve-introspect:<dedup_key>`（root cause 単位）→ タイトル類似度の二段 dedup（`filter_duplicates`）。observability builder は project_dir しか受け取れず result の error/矛盾を見られないため builder ではなく独立モジュール（`scripts/lib/evolve_introspect/`＝#122 で detectors/render/dedup/helpers に分割・`__init__` で re-export、[ADR-033]）。split↔archive 矛盾は検出だけでなく `reconcile_split_archive` が prune 直後（evolve/ Phase 4.1）に本流で解消する（archive 優先で split 候補から除外、除外は `split_suppressed_by_archive` に記録、検出器は regression guard として残置。#301 #302、[ADR-034]） |
| `evolve_result_schema` | evolve result JSON の**正準スキーマ契約**（1ソース・#375）。SKILL.md/references が result のキー名を手書きし impl からドリフトする事故（proposable を件数 int でなく配列参照、split を `.skill/.content_lines` で参照等）を封じる。`CANONICAL`（Key リスト）を単一ソースに `check_conformance_structured(result) -> List[ConformanceViolation(path,reason,detail)]`（機械可読・#379-5）＋後方互換 str ラッパ `check_conformance`（impl 側 drift 検出）と `extract_documented_paths` + `documented_path_drift`（longest-prefix で dict sub-field の FP build 破壊を回避・#379-3、bracket 記法も対応）が同一定義を consume。逆方向 drift（新 phase 追加で契約が静かに陳腐化）は `COVERED_PHASES ∪ UNCOVERED_PHASES` が全 phase を覆う契約テストで enforce（意図的部分カバーを明示・#379-1）。doc-drift 走査は SKILL.md + `references/**/*.md`（#379-2）。test-time ゲート（`scripts/tests/test_evolve_result_schema.py`、実 dry-run dogfood）+ runtime consumer は `evolve_consistency`（`scripts/lib/evolve_result_schema.py`） |
| `evolve_consistency` | P1 invariant の **runtime self-detect**（#377-5）。evolve_introspect から遅延 import され `improvement_opportunities` に candidate を合流（手動 CLI 止まりにしない＝evolve のたび発火）。2 検出: ① CANONICAL との**型レベル** drift（`check_conformance_structured` の wrong_kind/item_key_missing/null_not_allowed のみ。`missing` は部分実行 phase gating で FP ノイズ源のため runtime 除外、完全性は test-time が enforce）② usage_count==0 なのに suitability∈{high,medium}（P1 #376 修正後 0 件＝regression guard、split↔archive:88 と同パターン）。candidate 形は evolve_introspect 他検出器と同一、健全時 0 件でも improvement zero_line に「整合性 drift なし」を残す（silence≠evaluated）。決定論・LLM 非依存（`scripts/lib/evolve_consistency.py`） |
| `hook_drift` | 他ツール追従 hook（gstack flow を参照する `~/.claude/hooks/suggest-gstack-next-action.py` 等）の陳腐化を決定論検出する（`scripts/lib/hook_drift.py`）。第一フェーズ **stale_pin** — `~/.gstack/flow-chain.json` の `gstack_version` と実環境 `~/.gstack/.last-setup-version` を突合し乖離を検出（version 同士なので表記ゆれ false positive が無い）。builder `build_hook_drift_section`（`audit/sections_hook.py`）を observability contract の `_OBSERVABILITY_BUILDERS` に登録し markdown/構造化の両経路へ自動伝播、evolve のたびに追従漏れを surface。gstack はグローバルのため builder は project_dir 非依存（環境グローバル系）。gstack 未導入は None で沈黙、一致時「drift なし ✓」、実 version 不明時「判定保留 ℹ」（silence≠evaluated）。stale 警告には検出元パス（`pinned_source`=flow-chain.json / `actual_source`=.last-setup-version）を evidence として併記し、独自検証時に `gstack --version` の PATH フォールバックが flow-chain.json を読み戻す誤判定を防ぐ（#394）。hook 本体は SoT 整合の FALLBACK 修正＋発火 fire-log（`hook-fires.jsonl`）で follow-through 計測（第3フェーズ）の種をまく。**第二フェーズ dead_ref（#316）**: flow-chain.json が参照する skill 名（chain のソースキー + 各 `next` 遷移先）が live registry（`~/.claude/skills/` ∪ evolve-anything 本体 repo の `skills/` ∪ `skill_origin.get_plugin_skill_names()`）に実在しないものを `detect_dead_refs` が返す。後回しの真因だった表記ゆれは `normalize_skill_ref`（前後空白除去 / 先頭 `/` 除去 / `plugin:skill` 名前空間剥がし / 引数除去）に閉じ込め、変換を契約テスト（`test_normalize_skill_ref` パラメトライズ）で先に固定。**FP 厳禁（precision 優先）**: 正規化不能（空・記号のみ）の参照は flag せず、live registry が空（skill 列挙そのものに失敗）なら全参照を dead に見せないため沈黙。`build_hook_drift_section` が stale_pin の後ろに `⚠ 実在しないスキル N 件` を追記（dead が無ければ行を出さない）。実 `~/.gstack/flow-chain.json`（128 live skills）でドッグフードし FP 0 を回帰テスト化（`@pytest.mark.real_home` で autouse HOME 隔離をオプトアウト）。internal_drift / 有用性評価は別 issue（`second-opinion` レビューで初期の汎用一括案を YAGNI 判定、[ADR-036]） |
| `data_dir_migration` | DATA_DIR hook/tool 分裂の一元化 migration（#364 Phase 2）。正準 = `~/.claude/evolve-anything` 固定（#402 の env 非依存固定パス前例に整合）。redirect は marker（`.data-dir-unified`）ゲート: `rl_common.resolve_data_dir`（純関数）が CC install レイアウト（`~/.claude/plugins/data/*`）を指す CLAUDE_PLUGIN_DATA を marker 存在時のみ正準へ向け直す（テスト isolation の tmp env は無条件尊重＝conftest 隔離を壊さない）。`hook_store_path` も marker 存在時は正準を返す（ADR-042 の probe が migration 後に「空の旧 dir」を読む逆転を防止）。マージ規則: `.jsonl`=行 dedup append / `.db`=DuckDB テーブル単位 union dedup（書込可 ATTACH で WAL replay、per-fire connection 開閉で肥大した sessions.db の compaction を兼ねる: 実測 9.6GB・84k 行・実データ約 14MB）/ `.wal`=コピーせず削除 / その他=mtime newer-wins。dry-run は書き込みゼロ、marker は全 entry 成功時のみ（部分失敗は再実行で回収＝冪等）。**実行順序が重要**: 旧版 hook 稼働中に migrate すると分裂が即再発するため、本 fix を含む版のインストール後に `evolve-fleet migrate-data` を1回実行する。SessionStart（`restore_state._deliver_data_dir_migration_reminder`）が CLAUDE_PLUGIN_DATA env（install レイアウト配下のみ＝実環境 probe をテストに漏らさない）から未解消を検出して案内（install ≠ enforcement 対策、#402 drain リマインドと同型）。**再分裂の恒久沈黙を根治（#137）**: 旧実装は `if marker.exists(): return` で marker 済みなら二度と `needs_migration` を評価せず、旧版 hook が plugin-data に書き続ける等で分裂が再発しても永久沈黙していた（marker は「一度 migrate した」事実しか意味しないのに「以後永久に分裂しない」前提）。判定を「marker の有無に依らず毎回 `needs_migration(source)` を評価し、未マージストアが残っていれば案内。marker 済みでの再蓄積は再分裂として区別メッセージ」に修正（`needs_migration` は source を1回 iterdir するだけで SessionStart hot path 安全）。併せて **session store 側の split-brain 根治（#137）**: `session_store` が生 `CLAUDE_PLUGIN_DATA` を直読みして `resolve_data_dir` の marker ゲートを迂回していたため hook（plugins-data 書込）と tool（canonical 読み）で乖離していたのを、DATA_DIR を call-time に `resolve_data_dir` 経由で解決（`_data_dir()`/`_sessions_db()`/`_sessions_jsonl()` + module `__getattr__` 外部読取 shim）し hook/tool を同一 canonical へ収束させた（read/write 同一関数 #492・import 時固定コピー pitfall #96 を構造回避）。**ロバスト化（#417）**: ①スキーマ乖離 — src/old で同名テーブルの列が食い違っても永久失敗させない（`_merge_table_both`）。列追加/削除は列名で揃えた superset union（欠損は NULL 補完で行・列とも無損失）、和解不能な型差は old を残し src を `{table}__src_unmerged` へ退避＝**データ損失ゼロ**で `format_summary` に surface し手動統合を促す（旧 `UNION` 直叩きは Binder Error→failure→marker 不書込→リマインド永久発火の根因）。②並行書き込み窓 — `.jsonl`/単発ファイルは merge 前後で `(mtime,size)` を突合し、マージ中に別セッションの hook が追記したら削除を見送り次回 dedup 回収（`.db` は writable ATTACH の WAL replay で source 自身が変わるため対象外、CLI help で idle 実行を案内）。③`UNION` 行折り畳みは jsonl 行 dedup と同じ意図的設計で PK 持ちストア（token_usage の uuid 等）は無害＝docstring 明記。決定論・LLM 非依存（`scripts/lib/data_dir_migration.py`） |
| `spec_trigger` | main に着地した「挙動コードを変えたのに仕様アーティファクトを未更新」のマージを SessionStart で検出し spec-keeper update / ADR 化を1回提案する（[ADR-044]）。現状トリガー（`spec-keeper-trigger.md` ルール + gstack `ship→spec-keeper`）は /ship 経由でしか発火せず、`gh pr merge` 直叩き・GitHub web squash マージでは無音だった穴を塞ぐ（`install ≠ enforcement`）。web squash は「セッション外で main が進んだ」状態でローカルイベント(Stop/PostToolUse)では原理的に拾えないため検知点は **SessionStart 一択**、`restore_state.py` の `_deliver_spec_drift()` が `detect()` を fail-safe 呼び出し（新規 hook 不要）。較正版ゲートは実コーパス 40 commit への dry 適用で確定（素朴 feat:+plugin.json=8件全FP / structural-only=0件死蔵 / 広域=12件中10件 fix の FP → **feat/refactor/feat! × scripts・hooks の .py 変更 × 仕様アーティファクト未更新で 2件の真 TP**）。仕様アーティファクト＝`SPEC.md | spec/** | docs/decisions/** | CONTEXT.md | **CLAUDE.md**`（この PJ の生きた仕様は CLAUDE.md の component table、SPEC.md 単点は FP/FN 源）。`fix:` は信号源から除外（挙動は触るが仕様は変えない）。ADR 化は breaking(`!`) のみ併記。重複抑制は cooldown(3日)+リマインド1回で打ち止め（at-most-once は silence≠evaluated 再発）＋**解消プロキシ**（スキャン範囲に仕様アーティファクトを触った commit があれば pending 全クリア＝dev が仕様維持＝沈黙）。`detect(persist=False)` でマーカー書き込みゼロ（pitfall_dryrun_stateful_store_write）。slug は `optimize_history_store.resolve_slug`（worktree 安全, ADR-031）。`userConfig.spec_trigger_enabled`(default true) で無効化可。決定論・LLM 非依存（`scripts/lib/spec_trigger.py`）。**DATA_DIR は意図的に自前リゾルバ**（`CLAUDE_PLUGIN_DATA` 直読み・`.data-dir-unified` marker 非対応）で write/read を同一 SessionStart hook 文脈の同一 dir に揃える設計。marker 対応へ寄せて物理単一化する案（②）は **不採用**（hot path に marker I/O が乗る・[ADR-049] 不採用案 2026-06-20） |
| `outcome_metrics` | env_score が全 PJ で 0.6 前後・Lv.6-7 頭打ちだった構造要因2つへの対処（#423）。**(1) utilization 恒久0 の修理**: `telemetry._find_all_skills` は `project_dir/.claude/skills/` のみ走査していたため plugin レイアウト（リポジトリ直下 `skills/`）の本 PJ では skills=0→utilization=0.0→telemetry 重み25%が死に枠だった。audit 収集系 `audit.artifacts.find_project_skill_dirs`（`.claude/skills/` と `skills/` の両走査 + #419 収集除外 `is_excluded_skill_path` 共有）に統一し委譲。本リポジトリ実測 skills 0→21 / util 0.0→≈0.54。他軸の計算式は不変（snapshot 回帰なし）。**(2) 行動アウトカム3軸の advisory 導入**（スコア重みには未反映）: ①correction 再発率＝窓内 distinct `correction_type` のうち 2 セッション以上で再発した割合（corrections.jsonl）②一発成功率＝`error_count==0` のセッション割合（sessions.jsonl）③rework 率(近似)＝編集ありセッションのうち検証ツールを介さない連続 Edit/Write が閾値(3)以上のセッション割合（sessions.jsonl `tool_sequence`）。observability builder `build_outcome_metrics_section`（`audit/sections_outcome.py`）を `_OBSERVABILITY_BUILDERS` に登録し markdown/構造化両経路へ自動伝播。各軸に evidence（件数・session_id 例・再発 type 例）併記、データ不足の軸は「データ不足」明示、3軸とも no_data（該当ストア皆無＝評価対象なし）なら None で沈黙（orphan_store/hook_drift と同じ境界）。**最小分母 floor（#529-2）**: correction 再発率は分母（distinct `correction_type` 数）が `MIN_DISTINCT_TYPES_FLOOR`=5 未満なら率を `None` にし evidence に `reason="insufficient_sample"` + `distinct_types`/`floor`/`records` を付ける（n=2 で 0.50 のような低サンプル誤シグナルを抑止。promotion_readiness の `CORRECTION_FLOOR`=10 件で平均2件/type のとき distinct≈5 と整合する暫定値）。`sections_outcome` は「データ不足」と区別して「サンプル不足（distinct N type < floor 5）— 率は非表示」と表示。環境グローバル DATA_DIR 配下を読むため project_dir 非依存（テストは `monkeypatch.setattr(outcome_metrics, "DATA_DIR", tmp)` で隔離）。**rework の近似限界**: 既存ストアに編集対象ファイル ID が無い（usage.jsonl は Skill/Agent のみ、sessions の tool_sequence もファイルパス無し）ため厳密な「同一ファイル N ターン内再編集」は算出不能 → tool_sequence の編集バーストを proxy。重み昇格は 2〜4 週 advisory 並走→分布実測（分散十分・全 PJ 非同値・件数下限）→判断（coherence/constitutional のゲート降格は将来案、[ADR-046]）。決定論・LLM 非依存（`scripts/lib/audit/outcome_metrics.py`） |
| `outcome_attribution` | outcome_metrics（#423/ADR-046）が環境**全体**の行動アウトカム3軸を advisory 表示するだけで evolve のターゲット選定に流れていなかった（読者は audit 内部のみ＝画面表示で終端）問題への先行配線（#433）。corrections 非依存の **2 軸（一発成功率 / rework 率）だけ** をスキル単位に分解し、skill_triage 候補の順位に自動入力する（advisory→閉ループ）。**per-skill 帰属**: `usage`(skill/skill_name→session_id) と `sessions`(session_id→error_count/tool_sequence) を **in-memory で join**（DATA_DIR 再読込なし＝dry-run 安全。evolve が既に query_usage/query_sessions 済みの list をそのまま渡す）。一発成功率＝そのスキルが呼ばれた distinct session のうち error_count==0 の割合（error_count 欠損は分母除外＝None ソート落ち pitfall 回避）、rework 率＝outcome_metrics と同一定義（`_has_edit_burst`/`_EDIT_TOOLS` 共有）。**ランキング配線**: `apply_outcome_ranking` が CREATE/UPDATE/SPLIT/MERGE 各候補に `outcome` evidence を付与し、(outcome_priority desc, confidence desc) で安定ソート（純粋関数＝入力 triage_result を非破壊）。priority は「悪いアウトカムほど高い＝進化対象として上位」で揃った軸の平均、degraded は neutral 0.0（順位を動かさない）。**#10 で 3 軸目（correction 再発率）+ negative_transfer gate を配線**: correction 再発率＝スキルに帰属する correction（`last_skill`）の同型が 2+ distinct session に跨る割合（corrections.jsonl 由来・現状ほぼ空なので大半 None。per-skill では表示側 floor を課さず None graceful、既存2軸の degraded 契約・multiview_eval 互換を壊さない）。negative_transfer gate＝`compute_negative_transfer` でフラグの立ったスキル（追加が他スキルの成功率を下げた）に `outcome.suppressed=True` を立て (suppressed asc, priority desc, confidence desc) で**末尾へ rollback**（outcome が悪く本来トップでも退行を生んだスキルを優先進化対象にしない安全弁）。**#28 RODS**: 各候補に `outcome.reward_variance`（session 別 reward proxy=一発成功 0/1 の分散を既存 `check_variance` per-skill 転用で判定。高分散=能力境界=学習余地大）を advisory 添付（自動昇格はしない）。`phases_diagnose` から corrections（PJ スコープ read-only）+ negative_transfer を渡して enforcement 配線。**dry-run observability**: action ごとに before/after のスキル順 + changed + scores + suppress されたスキルを `outcome_ranking` に記録（#393-#396: 数字に意味を添える）。残スコープ外: fitness 重みの変更（ADR-046 の 2-4 週後判断レール）。**#35（実PJ dogfood 訂正・close）**: correction 再発率が実データで大半 None なのは `last_skill` の writer/reader 不一致ではなく（同一パス `workflow.py:89` で一致）、correction の大半が **skill 非経由の作業（Bash/Edit/git）への修正**で発火し直前 skill が無いため＝構造的スパース（preceding_tool_calls も Skill エントリ無しで救えない）。配線バグでなく**設計上の制約**＝スキル前置 correction 蓄積時のみ寄与（仕様化して受容。他2軸が graceful に主信号）。決定論・LLM 非依存（`scripts/lib/audit/outcome_attribution.py`、#10/#28） |
| `reward_ema` | バッチ跨ぎ符号付き advantage の EMA 累積（MAA, arXiv:2606.20475・#64）。`outcome_attribution._reward_variance`（RODS #28）は**単一スナップショット**の reward 分散で「学習余地大」を判定し、1 時点の高分散が偶然の好成績/不成績の混在でも拾ってしまう＝通時の効きを区別できない問題への補完。**advantage の定義**: `compute_batch_advantage` が `attribute_outcomes` の per-skill `first_try_success` から baseline（degraded/None を除く有効スキルの平均）を引いた符号付き差（MAA の alignability＝共通 baseline 基準 / comparability＝同一指標で比較。有効スキル<2 は baseline 比較が無意味なので `{}` graceful）。**EMA 畳み込み**: `fold_ema(α=0.3)` が初回=advantage / 以降 `α*adv+(1-α)*prev`、`n_batches` 増分（小さい α＝過去の累積を厚く＝通時安定性を重視）。**ストア**: 新規 `reward_ema.jsonl` を store_registry に **active**（`writer_locus="batch"`・`retention=permanent`）登録。書込は必ず **store_write barrier（ADR-049）経由**で canonical DATA_DIR に解決し、`pj_slug` を record に持たせ read 側で照合（全PJ共通 DATA_DIR 単一ファイル pitfall 回避）。`read_reward_ema` は `_om._read_jsonl`（read-only・非作成）で読み skill 単位 last-append-wins で fold（ファイル不在→`{}`・**1バイトも書かない**＝dry-run 純度）。**読み配線**: `phases_diagnose` が `resolve_pj_slug` で slug を取り `read_reward_ema` で prior EMA を読み `apply_outcome_ranking(reward_ema=)` に渡す（apply_outcome_ranking は in-memory 純粋契約を保つため自身は DATA_DIR を読まない＝呼び出し側が渡す read のみ・dry-run 安全）。各候補に `outcome.reward_ema`（EMA レコード or None）+ `outcome_ranking[action]["reward_ema"]` に skill 別の通時安定ラベル（`ema_stability_label`）を添える。**順位は変えない**（advisory のみ・RODS と同位置）。reward_ema は attribute_outcomes を import するため逆方向 import は循環＝`_ema_stability_label` 遅延ラッパで回避。**書き配線**: `evolve --drain` の apply 境界（非 dry-run・正準 DATA_DIR・weak_signals #484 と同型）でのみ `persist_reward_ema_batch` を発火し、その時点の usage/sessions を読んで attribution を再計算→batch advantage→prior に fold→各 skill 1 レコードを store_write。**閾値の捏造回避**: `ema_stability_label` は magnitude での pass/fail 判定をせず、`MIN_CYCLES_FOR_SIGNAL`(=3) 未満は「サイクル不足」、以降は符号（+有効寄り / -低調寄り / 中立）と cycle 数のみ surface（実コーパス calibration 前に断定しない＝#42 と同じ保守姿勢）。**plant-the-seed 型**＝store を今作れば累積が始まり 3-4 サイクルから有意になる（再評価条件を満たすのを待たず substrate を活性化）。決定論・LLM 非依存（`scripts/lib/audit/reward_ema.py`、#64・RODS #28 と相補） |
| `subagent_traces` | subagent 内部軌跡ストア（#38・senpai 推奨 telemetry）。既存 outcome 帰属（outcome_attribution / sections_paired / negative_transfer）は全て usage+sessions ベースで**親セッションの error_count しか見ない**ため、impl-worker が内部で error 連発して最終成功すると親 error_count=0 で「一発成功」と誤記録される盲点があった。これを **subagent transcript の内部軌跡**で埋める。**データ契約**: `subagents.jsonl` の各行（`agent_id`/`agent_type`/`agent_transcript_path`/`project`…）の transcript（`{type, message:{role,content:[blocks]}}`）を `extractor.extract_trace` がパースし `tool_use`/`tool_result`(`is_error`)/`text` ブロックを数える（`is_error==True`=ツール失敗・`first_try_success`=tool_error_count==0）。**ストア**: 新規 `subagent_traces.jsonl` を store_registry に **active**（`writer_locus="batch"`・`retention=permanent`）登録。書込は **store_write barrier（ADR-049）経由**、`pj_slug` を record に持たせ read 側照合（全PJ共通 DATA_DIR pitfall 回避）、`agent_id` 単位 last-append-wins。`read_traces` は read-only 純度（ファイル不在→`{}`・非作成）。**増分 ingest**: `ingest_all_projects(max_new=200)` が `read_all_agent_ids` で既 ingest を dedup し、未 ingest かつ transcript 現存の行だけ最大 max_new 件を取り込む。**named transcript のみ読み projects 全 walk しない**（#28 の O(N) runaway 回避）+ max_new 打ち切りは `capped`/`remaining` で surface（沈黙切り捨てしない）。**読み配線**: `audit/sections_subagent_traces.build_subagent_traces_section` が当 PJ slug で `per_agent_type_summary`（floor=3 件ゲート・`is_noise_agent_type` で空/ID 形除外）を読み agent_type 別の内部一発成功率+平均 tool error を advisory 表示（fitness 重み非干渉・outcome_metrics/fanout_cost と同レーン・silence≠evaluated）。**低品質 ⚠ 発火（#76 Finding A）**: floor 以上の agent 種別で `first_try_success_rate < LOW_FIRST_TRY_SUCCESS`(=0.5・strict <) または `avg_tool_error >= HIGH_AVG_TOOL_ERROR`(=5.0) のとき `⚠` 行 + 見直し誘導行を付与し、`report.py` の畳み込み（⚠/🔴 だけ full-text 展開・他は1行に畳む）に乗せて『✓ 評価済みクリーン』への埋没を防ぐ（dogfood で figma 0.17/sys-bots 0.33・tool error 8.33 が分類段で消えていた根治）。閾値は実 PJ dogfood で較正（要発火=0.17/0.33・tool error 8.33、非発火=senpai 1.0/Plan 1.0、境界 Explore 0.50 は strict < で非発火）。avg_tool_error は rate 良好でも独立発火（成功多数に内部大量リトライ agent が混じるケースを拾う）。**書き配線**: evolve batch（`phases_capture`）に dry-run 安全な増分 ingest を配線（dry-run では書かない＝dogfood Layer1 SHA256 不変契約）。writer slug=`pj_slug_fast(project)`↔reader slug=`pj_slug_fast(project_dir path)` は going-forward の当PJ実値 `project="evolve-anything"` で一致を実測（旧名 `rl-anything` 行は rename-legacy orphan・許容）。**実PJ E2E bench**（transcript-store-bench 規律）: 300件 1.12s（3.7ms/件・runaway 無し）、当PJで impl-worker 内部一発成功率 0.36/tool error 1.64 を実測 surface＝盲点が実証。決定論・LLM 非依存（`scripts/lib/subagent_traces/`、#38） |
| `verbosity` | 回答冗長性の学習ループ（#75・ユーザー standalone の正式統合）。ユーザーが `~/.claude/verbosity/`（record-verbosity hook + judge.py + concise output style）で運用していた「AI 応答が無駄に冗長か」の学習ループを evolve-anything へ統合。各部品は既存コンポーネントへ **1:1 対応する移植+統合**（発明ではない）。**スコープ判断**: PJ スコープ（record に `pj_slug`・read 側照合）+ fleet 集計（subagent_traces #38 と同型）。**改善先判断**: suggestion-only（protected・人間承認）—`~/.claude/output-styles/concise.md` は CC グローバル機能ゆえ自動編集せず `rules/concise.md` 追記案を提示のみ。**dedup 判断**: weak_signals に `channel="verbosity"` を新設し correction 系チャネルと分離。**コストゲート判断**: dry-run 既定・llm-batch-guard 準拠（件数+推定トークン先出し）。⑴ **Stop hook `hooks/record_verbosity.py`**: ゼロ LLM・非ブロッキング（**必ず exit 0**・記録失敗でセッションを止めない）で、transcript 末尾の最後の assistant text（tool_use のみのイベントは飛ばす）を取り、`char_len < GATE_CHARS`(=800・env `VERBOSITY_GATE_CHARS`)を足切りして `verbosity_candidates.jsonl` に記録。出力は **store_write barrier（ADR-049）経由**で canonical DATA_DIR/<name> を内部解決、`pj_slug`=`pj_slug_fast(cwd)`（hot path ゆえ subprocess なし）を付与。⑵ **`scripts/lib/verbosity/`**（store/judge/query）: `store`=read-only 純度の read（slug filter + hash dedup・ファイル不在→空・**1バイトも書かない**）+ `write_verdict` の store_write barrier write（subagent_traces #38 同型）、`judge`=Haiku バッチ判定（**dry-run 既定**で `estimate_cost` の決定論見積もりを先出し・実 LLM 非呼出・非書込／`--run` で実判定し subprocess は `call_haiku` **1 箇所に集約**＝no-llm-in-tests 整合）。「無駄に冗長か」+7パターン（preamble/repetition/filler/over_summary/restate_question/hedging/meta）を判定→`verbosity_verdicts.jsonl` 永続化、verbose=True を `WeakSignal(channel="verbosity")` で `append_signals`（reflect 昇格フローに相乗り）、多発パターン→`rules/concise.md` 追記案を `build_suggestion` で提示（**auto-apply しない**）。`query`=`verbosity_summary`（floor=3 ゲート・judged<floor は `verbose_rate=None` で不足明示）。**import**: judge は audit が案内する `python3 scripts/lib/verbosity/judge.py --run` の `__main__` 直接起動に備え **絶対 import**（`from verbosity import ...`・相対 import は `__main__` で `ImportError`）。⑶ **`audit/sections_verbosity.build_verbosity_section`**: 当 PJ slug で冗長率/パターン Top-N を advisory surface（**スコア重み未反映**・候補ゼロは None で沈黙・判定不足は `judge --run` 誘導で silence≠evaluated）、`_OBSERVABILITY_BUILDERS` に登録。⑷ **ストア**: `verbosity_candidates.jsonl`（writer_locus=hook・retention=ttl 45日）+ `verbosity_verdicts.jsonl`（writer_locus=batch・retention=permanent）を store_registry に **active** 宣言し active store keyset snapshot 追従。**頭レビュー修正**（worker targeted run の盲点）: judge 相対→絶対 import + subprocess 回帰テスト / `write_verdict` mock を importlib モジュール取得方式（再export 関数の曖昧性回避）/ record_verbosity docstring の standalone パス literal が `find_store_writers` FP を踏む問題を除去。実環境 E2E で hook 記録（足切り未満は非記録）+ judge dry-run 書込ゼロを実証。決定論部は LLM 非依存（`scripts/lib/verbosity/` + `hooks/record_verbosity.py` + `audit/sections_verbosity.py`、#75） |
| `capture_rate` | RL ループの報酬入力（corrections）の捕捉率を決定論算出する（#421）。実測で corrections がほぼ空（9 件/76 日・全件 reflect skipped、evolve_decisions ≒0 件）だったが、それが検出器の仕様通りの少なさか capture 漏れかを誰も監視していなかった（ADR-041 の配線は正しいが上流に水が流れていない）。capture 率 = 「直近 N 日で `min_turns`(=20)+ ターンを持つセッション」のうち「同一 session に correction を 1 件以上検出したセッション」の割合。ターン proxy は usage.jsonl の同一 session_id レコード数（UserPromptSubmit 専用の per-turn ストアが無いため、最も信頼できる per-session 活動量を採る）。usage.jsonl / corrections.jsonl はいずれも hook-writer 系ストアで `hook_store_path`（base=audit.DATA_DIR）で #358 を踏まず正準解決。builder `build_capture_rate_section`（`audit/sections_capture.py`）を observability contract の `_OBSERVABILITY_BUILDERS` に登録し markdown/構造化 両経路へ自動伝播、active session があるのに capture 率が閾値(10%)未満なら ⚠ で分母/分子を併記して surface（active 0 は None で沈黙＝テレメトリ未蓄積、閾値以上は「枯渇兆候なし ✓」＝silence≠evaluated）。**スコア重みには入れない**（advisory のみ — 壊れた入力の上に重みを作らない）。実機で active 6 件中 capture 0% を即 surface。決定論・LLM 非依存（`scripts/lib/capture_rate.py`） |
| `orphan_store` | writer はあるが reader がいない jsonl ストアを決定論検出する（#422）。発端は `tool_durations.jsonl`（実環境 5.1MB）が `tool_duration.py` により全 Bash 実行ごとに書かれるのに reader が 0 で、純粋なレイテンシ + ディスクコストだった主要ストアの producer→consumer 突合。この手動突合を決定論化: **writer = hooks.json に登録された hook 本体が書く jsonl ファイル名**（未登録 hook は発火しないので対象外＝false positive 防止）/ **reader = scripts/・skills/（tests 除外）に現れる jsonl ファイル名** をストアファイル名文字列の出現で静的突合し、writer にあって reader 0 のストアを orphan として報告する。observability builder `build_orphan_store_section`（`scripts/lib/audit/sections_orphan.py`）を `_OBSERVABILITY_BUILDERS` に登録し、audit を消費する evolve のたびに surface（手動突合に依存しない＝install≠enforcement の可視化）。検査対象は evolve-anything 自身の hooks/scripts/skills で project_dir 非依存（環境グローバル系、`_default_plugin_root` でテスト隔離）。登録 writer が無い環境は None で沈黙、orphan 無しでも「✓ 該当なし」を残す（silence≠evaluated）。tool_duration hook（登録・本体・テスト）は #422 で削除。決定論・LLM 非依存（`scripts/lib/orphan_store.py`） |
| `utterance_archive` | 全PJ human 発話の恒久アーカイブ utterances.db（#430・Phase B）。transcript は `cleanupPeriodDays`（14→60 暫定延長済）で消えるため human 発話が毎日失われ再構築不能だった。correction 個人辞書（#431）・暗黙シグナル（#432）・遡及分析はすべて human 発話の履歴が基盤なので、ゼロ LLM の batch ingest で恒久 DuckDB ストアに蓄積する基盤を新設。package `scripts/lib/utterance_archive/`（extractor / store / ingest / query の4分割、各500行以内）。**extractor**: `~/.claude/projects/*/*.jsonl` から `type=user`+`role=user` の human 発話のみ抽出。`isMeta`/`toolUseResult`/`tool_result` content・harness 注入6種（`<system-reminder`/`<command-name`/`<local-command`/`Caveat:`/`[Request interrupted`/`This session is being continued`＝`learning_trajectory_mining_machinery_turns` 準拠）を除外。>2000字は `source_kind='long_paste'`、非対話 PJ（`EXCLUDED_PJ_SLUGS` 初期値 `bots`）は `excluded_pj` でタグ分類（除外でなく分類＝後から判断を変えられる、`learning_detector_fp_context_not_allowlist`）。`prev_action` は当該 human より前・直前 human より後の assistant メッセージ群の tool_use 名を出現順 join（上限10+超過時 `…`、無ければ None）。**pj_slug は encoded dir 名のデコードを諦め transcript の `cwd` から導出**（`/` と `.` が同じ `-` に潰れる非可逆エンコードのため `evolve-anything` 等ハイフン入り名を復元不能）: cwd に `/.claude/worktrees/` があればそこで切って本体 repo basename へ正規化（worktree セッションを main repo に帰属）、cwd 欠損ファイルは encoded dir 名をそのまま slug に使い起源は source_path で追う（評価対象から外さない）。**store**: 物理 PK `(source_path, line_no)`（増分 ingest と自然整合）+ 論理 UNIQUE `(session_id, timestamp, text_hash)`（resume の履歴 replay で同一発話が別ファイルに複製されるのを弾く＝turn_index PK は resume/compaction で不安定なので不採用、second-opinion 指摘）。最上位1 connection（DuckDB checkpoint pitfall #28 準拠、per-row connect 禁止）。`INSERT ... ON CONFLICT DO NOTHING` で物理 PK + 論理 UNIQUE 両方に冪等。`ingest_state(source_path, mtime, line_offset)` で増分（mtime 同一かつ offset 既達ならスキップ、全量再走査は初回 backfill のみ、UNIQUE があるので state 破損時の再走査も冪等）。完走時に staleness marker `utterances_last_ingest_at` を DATA_DIR に書く（marker 不在=未 ingest=∞ 扱い、null-safe 誤実装を仕様で封じる）。**query**: `query_utterances(pj_slug 必須, since=None, source_kinds=('dialogue',) デフォルト, keyword, session_id, limit)` — pj_slug 必須で全PJ共通 DATA_DIR 単一ファイル pitfall の read 側照合を API 強制、横断は明示関数 `query_utterances_all_projects()`（fleet recall 用）。source_kind デフォルト dialogue のみで下流（#431 個人辞書）の分母汚染を防ぐ opt-in 方式。**配線**: ① evolve オーケストレーター（`evolve/` run_evolve 末尾、**dry-run 時は ingest しない**＝実 DATA_DIR を書かない）② `evolve-fleet ingest`（`--root`/`--days`/`--max-files`/`--quiet`）③ SessionStart staleness advisory（`restore_state._deliver_utterance_staleness`、marker 読みのみの observe-first・marker 不在=未 ingest=advisory・閾値14日・DuckDB 接続も transcript 走査もしない＝`pitfall_hot_hook_eager_import`）。advisory は強制でなく安全弁（本線は evolve/audit 同居、`install ≠ enforcement`）。**store_registry 拡張**: `.db` ストア対応に `StoreKind`（jsonl/db）フィールドを追加、`utterances.db` を retention=permanent / kind=db で宣言、contract-drift の stale 突合から db ストアを除外（writer が batch ingest で hook-writer 突合に出ないため）。DATA_DIR は ADR-042 resolver（`rl_common.resolve_data_dir`）経由で hook/tool 分裂 pitfall を踏まない。実機 1 PJ E2E（evolve-anything 実 transcript 131 files・transcript-store-bench ルール準拠）: wall 1.76s（<60s 要件）/ 495 件 ingest（dialogue 436 + long_paste 59）/ DB 3MB / 機構ターン混入 0%（20サンプル目視・5%未満要件）。TDD 新規・決定論・ゼロ LLM。設計 SoT: docs/evolve/utterance-archive-430-415-design.md の Phase B（`scripts/lib/utterance_archive/`） |
| `SessionStore`（jsonl-first） | sessions の永続化 Repository。**#415 Phase A で書き込みパターンを jsonl-first に一本化**して sessions.db 再肥大（9.6GB / 実データ約14MB ≒680倍）を根治。病巣は `append()` が hot path（hooks の発火ごと）で **per-fire connect→INSERT→close** していたこと。①**append は jsonl 追記のみ**（DuckDB 経路削除＝hot path から接続を消す）。②**`ingest()`** が `sessions.jsonl → sessions.db` を**最上位 1 connection**で取り込む（DuckDB checkpoint pitfall 準拠、per-row connect 禁止を `test_ingest_uses_single_connection` で回帰封じ）。dedup キーは `(session_id, timestamp)`（既存 `migrate_from_jsonl` と同一）。取り込み成功確認**後**に live jsonl を `.ingested-<ts>` へ rotate（glob `sessions.jsonl.ingested-*` で恒久除外＝mtime 非依存、1世代保持）。取り込み失敗時は rotate せず jsonl を温存（次回再試行＝冪等）。③**読み取りは union read**: `count_unique_since` / `query` は現実装の排他分岐（db があれば jsonl を見ない）から **db 結果 + 未 ingest（live）jsonl 結果を `(session_id, timestamp)` で dedup 合算**へ書き換え（query は timestamp 昇順・limit は union 後適用）。理由: trigger_engine 等は ingest と**非同期**にカウントを読むため「ingest 直後にしか読まない」仮定が成立しない。④**保険 compaction**: ingest 完走時に db ファイルサイズ vs `rows×平均 raw_json 行長` の乖離 >10倍（かつ絶対 `_COMPACTION_MIN_BYTES`=4MB 超: DuckDB の最小ファイルサイズ床近辺での false compaction 防止）で rebuild。in-place DROP/CREATE では DuckDB がブロックを返さないため、新規 db ファイルへ `ATTACH`+`CREATE TABLE AS` でコピー→`os.replace` で swap する fresh-file 方式（connection を閉じた後に実施）。⑤ingest 呼び出しは evolve オーケストレーター（`skills/evolve/scripts/evolve/` パッケージ・#531/ADR-048 で分割済み、ingest は block D = `phases_capture.py`）の batch 文脈に同居、**dry-run 時は ingest しない**（DATA_DIR 非書込の規約）。`migrate_from_jsonl` は後方互換 CLI（`migrate_sessions_to_duckdb.py`）用に rotate せず残置。既存呼び出し元（trigger_engine / session_summary / instructions_loaded 等）は SessionStore API 経由なので無変更。テストは `monkeypatch.setattr` で DATA_DIR/SESSIONS_DB/SESSIONS_JSONL を tmp 隔離。決定論・LLM 非依存（`scripts/lib/session_store.py`、設計 doc `docs/evolve/utterance-archive-430-415-design.md`）。**直読 reader も union read へ集約**: `telemetry_query.query_sessions`（`sessions_file` 未指定時）は旧 `_query_sessions_table`（SESSIONS_DB 直読）を廃し、`HAS_DUCKDB=True` 経路を `_query_sessions_via_store`（`session_store.query` 経由）に置換。これにより未 ingest（live）jsonl のセッションも `query_sessions` / その下流 `discover.detect_missed_skills` に反映される（直読では append 直後・ingest 前のセッションを取りこぼす実害があった）。`HAS_DUCKDB=False` の DuckDB 非依存フォールバックは `DATA_DIR / sessions.jsonl` 直読を維持（union read は session_store の DuckDB 経路に依存するため）。`telemetry_query_internal_surface` snapshot も追従更新。**DATA_DIR は意図的に自前リゾルバ**（`CLAUDE_PLUGIN_DATA` 直読み・`.data-dir-unified` marker 非対応・write は例外口 `store_write_raw` 経由）で sessions.db と sessions.jsonl を co-located に保つ設計。hook 文脈では plugins-data に書くが reader が union read で救済（#469）するため欠落しない。marker 対応へ寄せて物理単一化する案（②）は **不採用**＝sessions.db だけ別 dir に取り残される co-location 破壊リスクのため（[ADR-049] 不採用案 2026-06-20） |
| reader union 網羅監査（#45 (a)） | DATA_DIR 断片化（rename rl-anything→evolve-anything / plugins-data hook split）の移行期に、14 self-resolver store の **reader** が legacy/plugins-data のデータを欠落なく読むかを監査（write 側 self-resolver は [ADR-049] で意図的に維持＝触らない）。実 PJ census で legacy(`~/.claude/rl-anything`) に実データが残り canonical 不在＝現に全 orphan を確認。自前 store の jsonl reader で reader-union で救済できる実 orphan は **`evolution_memory.load_patterns`（成功パターン）と `optimize_history_store.load_history`（fitness calibration 母集団）の2件**→ `rl_common.iter_read_data_dirs` で cross-dir union 化（canonical 先頭 dedup・hermetic・**write は canonical 固定**で暗黙の物理 merge を起こさない）。DuckDB 系（token_usage.db / episodic.db / sessions.db）は reader-union 非適用で `evolve-fleet migrate-data` 経路（[ADR-042]）。**migrate-data で canonical へ統合した副作用で latent な dry-run byte バグが露出（#65）**: 3ストアの read 関数（`episodic_store.query_relevant` / `token_usage_store.query` / `session_store.query`・`count_unique_since`）が DuckDB を read-write で開き（`_connect()` が `CREATE TABLE IF NOT EXISTS` も実行）、実 DB の初回 open が write transaction commit でファイルを canonical 形に fold して byte が変わり dry-run byte 契約（#461）違反 → dogfood Layer 1a 赤になっていた（論理データ＝行数は不変）。各ストアに `_connect_ro()`（read_only=True・schema/mkdir/chmod なし）を追加し read 関数を `read_session_records`（read_only 手本）に揃えて根治。回帰テストは「DB が legacy のみで gate が見えていなかった」「クリーン小 DB は既に canonical 形で SHA 不変では捕捉不能」の二重盲点を踏まえ chmod 444 で write 権限要求の有無を assert。spec_trigger=hook↔hook co-located / hypothesis_tracker=session-scoped / trigger_engine=ephemeral 制御状態 / triage_ledger・world_context=legacy 無し は OK 判定。corrections/errors の古典 raw reader（telemetry_query/discover/audit/fitness/workflow_checkpoint）の union 化は #45 (b) として別途。監査表は issue #45（`scripts/lib/evolution_memory.py` + `scripts/lib/optimize_history_store.py`） |
| `store_registry` / ストア契約ゲート | ストア新設の**事前**契約ゲート（#434）。orphan_store（#422/#426/#427）は「writer あり reader 0」の**事後**検出でモグラ叩き（message_display.jsonl #427 が代表例）だったため、新 jsonl ストア追加時に **writer / reader / retention の3点宣言を必須化**する。宣言 SoT は機械可読な Python dict（`scripts/lib/store_registry.py` の `StoreDeclaration` リスト。`_OBSERVABILITY_BUILDERS` 等の既存宣言慣習に統一し、消費側 orphan_store から import 一発で参照、JSON parse 経路を増やさない）。retention は3種別（`permanent`=恒久 / `ttl`=N日失効・`ttl_days` 必須 / `compaction`=サイズ/件数条件・条件散文必須）で `validate_declarations` が宣言自身の整合性を検証。orphan の処遇は `disposition`（`keep_future` 将来基盤で意図的に reader 不在 / `drain` reader が別経路 / `remove` 削除予定）で明示。検出は orphan_store に `detect_store_contract_drift` を追加し、**宣言なしの新規 writer = undeclared**（reader 有無に関わらず検出）/ **宣言あり実 writer 不在 = stale** / 宣言不整合 を突合。observability builder `build_store_contract_section`（`audit/sections_orphan.py`）を `_OBSERVABILITY_BUILDERS` に登録し audit/evolve に surface。既存 hook writer 8 ストア（corrections/usage/usage-registry/sessions/errors/workflows/skill_activations/subagents）を宣言バックフィル済み。message_display.jsonl（#427 の orphan）は #495 で MessageDisplay hook 不発登録を削除し宣言も撤去、dead code 本体・テストも #427 で削除済み。`test_all_live_hook_writers_are_declared` が宣言漏れを回帰検出。決定論・LLM 非依存（`scripts/lib/store_registry.py`） |
| `store_write` write barrier | 全ストア書込の単一ゲート（[ADR-049], #55）。`store_write(store_name, record)` が canonical `DATA_DIR/<name>` を内部解決し、store_registry の active 登録を **runtime guard** で照合する。**既定 reject**: Phase 2b で全 production caller（hooks 10 + scripts/lib 6）を移行完了し warn-only から昇格。登録外の書込は `StoreWriteError` を送出、緊急避難は env `EVOLVE_WRITE_GUARD=warn`（不正な guard 値は warn へ de-escalate）、store_registry 不在時は fail-open。例外口は別名関数 `store_write_raw`（登録外書込を明示的に許すバックドア）。read（union 寛容 = legacy dir も畳んで読む）と write（厳格 = active 登録のみ）を非対称に分離し、共有は store_registry の宣言のみ。決定論・LLM 非依存（`scripts/lib/rl_common/store_write.py`） |
| `weak_signals` | 暗黙修正シグナルの決定論検出 → weak_signals レーン（#432）。明示的な修正発話は語彙依存で稀（#431 実測）だが、修正の**行動シグナル**は語彙非依存でゼロ LLM 検出できる。4 チャネルをバッチ側（hot path 非介入）で検出し新ストア `weak_signals.jsonl` に provenance 付き記録。package `scripts/lib/weak_signals/`（store / detectors / batch）。**corrections 本流には直接入れない**設計（deny は「今はやるな」・手編集は「続きの作業」の可能性があり本質的にノイジー）— 昇格は reflect 確認後（`promoted` フラグ）、#431 のバッチ LLM 判定もこのレーンを共有。**4 チャネル検出器**（決定論）: ①直後手編集 = transcript の `<tool_use_error>File has been modified since read...`（user/linter どちらか不明なので attribution=`user_or_linter` と明示・捏造しない）/ ②permission deny = `errors.jsonl` の `permission_denied` レコード（`permission_denied` hook を writer→reader 化、新 hook 追加なし）/ ③言い直し = #430 utterances.db の同一セッション**隣接**発話の jaccard token 重複（`similarity.tokenize`/`jaccard_coefficient`・cross-session 比較なし）/ ④Esc 中断 = transcript の `[Request interrupted` user text block（utterance extractor は harness として除外するが検出器側は transcript 直読で拾う）。①④は observe hook が永続化しないため transcript 直読、②は既設ストア、③は utterances.db。**言い直し閾値 0.8 は実コーパス dry-run で決定**（ADR-044 準拠・固定値を設計前に決めない）: evolve-anything 全 PJ utterances.db 3204 発話 / 1289 連続ペアの jaccard 分布を実測。0.6=162 ペア（大半が並列 agent 派遣テンプレの誤検知）、0.8 + dispatch 除外=16 ペア（目視 100% が真の言い直し/再送＝typo 修正・完全再送・疑問符追加）。**FP 除去は個別 allowlist でなく「機構生成テンプレ」という除外理由の直交分離**（`learning_detector_fp_context_not_allowlist`）— `<task-notification>`/「あなたは…エージェントです」/「比較実験パターン」等の派遣テンプレマーカーを片方でも含むペアを 1 つの理由で落とす。**store**: dedup キー（channel+provenance の sort_keys 正規化ハッシュ16桁）でバッチ再実行の二重記録を防止。レコードスキーマは将来共有用に汎用化（channel / provenance / detected_at / session_id / pj_slug / promoted / signal_key）。**dry-run は最下層 write まで一切書かない**（`pitfall_dryrun_stateful_store_write`・親ディレクトリ作成すらしない・E2E で `store.exists()==False` を assert）。**store_registry 宣言**（#434 事前ゲート）: `weak_signals.jsonl` を writer/reader/retention 宣言。batch script 書き込み jsonl が hook-writer 突合（find_store_writers）に出ず stale 誤検知される問題を、`StoreDeclaration.writer_locus="batch"` フィールド + `stale_exempt_names()`（db kind と batch locus を集約・orphan_store の db-only 除外を一般化）で解消。**配線**: evolve オーケストレーター（`evolve/` run_evolve の utterance ingest **後段** — 言い直し検出が更新済み utterances.db を入力に使うため順序依存）に `run_batch` を同居、dry-run でも検出は走り書き込みのみ弾く。observability builder `build_weak_signals_section`（`audit/sections_weak_signals.py`）を `_OBSERVABILITY_BUILDERS` に登録しチャネル別件数・未昇格数を **advisory surface（スコア重み非関与**＝corrections に直入れしない設計と整合）。実コーパス検出: 直後手編集 6 / permission deny 5 / 言い直し（全 PJ）16 / Esc 中断 33。TDD 新規 30 テスト・決定論・ゼロ LLM（`scripts/lib/weak_signals/`）。**45日 TTL（#442）**: corrections の decay と整合する TTL を導入し、期限切れレコードは削除せず `expired` マーク + `read_unpromoted` 等の昇格候補 reader から除外（古い修正候補を昇格させる label noise を防ぐ）。TTL 判定・マーキングは evolve の決定論 phase として常時 emit（dry-run はマーキング書込をしない）、store_registry 宣言を retention=ttl(45日) に更新。**昇格誘導文言（#444）**: observability builder の未昇格 N 件表示に `/evolve-anything:evolve` の今日の修正確認 phase への誘導ヒントを追記。**TTL 失効の read 時導出（#89）**: `mark_expired` の `expired` フラグ書込は `run_evolve(dry_run=False)`（= `phases_capture`）経由のみだが、標準フロー（`--dry-run` 分析 → `--drain` 適用）はどちらもそこを通らない（drain 分岐は早期 return で phases_capture 非通過・grep 確認）。`read_unpromoted` が `expired` フラグだけ見て age を再計算しないため、フラグ未書込のまま 45日超の signal が `weak_unprocessed_by_pj` 経由の `material_count` から永久に落ちず queue material が膨張する seam（実機 dogfood 2026-06-25・figma-to-code 116 件が ~2026-07-25 から発現する潜在バグとして発見）。直しは **write 非依存の read 時 age 計算**: `weak_signals.ttl.is_effectively_expired(rec, now=None)`（純関数 — `expired is True` ∨（`detected_at` parse 可 ∧ `< now - TTL_DAYS`）/ parse 不能は安全側 False / `TTL_DAYS`・`_parse_iso`・`<` cutoff を `mark_expired` と単一ソース共有）を `read_unpromoted(exclude_expired=True)` の判定に挿す。これで `read_unpromoted` を使う全 reader が writer-death 非依存で正しくなる。`mark_expired`/`phases_capture` は dry-run report・将来 audit 永続化として温存（correctness は read 時計算が load-bearing）。設計原則: **派生系（log から導出可能な状態）は forward-only runtime write に correctness を委ねず read 時にデータから導出せよ**（writer が静かに死んでも壊れない）。`detected_at` は検出/取込時刻（`now_iso()`）でイベント時刻でない点に注意（過去ログ backfill では detected_at が now にリセットされ 45日 bug の即時 repro 不可・イベント時刻 TTL は別 issue） |
| `correction_semantic` | correction capture の二層化（#431）。背景: corrections.jsonl 累計 9 件中 本物の人間修正 1 件・残り 8 件が Stop hook の機械生成で、フェーズ昇格条件（corrections>=10, growth_engine）が永久未達 → 全 PJ が initial_nurturing 固定。固定語彙の拡充は実コーパスで否定済み（文中形候補は文字起こし/貼り戻し/疑問慣用句が大半で precision/recall とも実用にならない）。実際の修正は**ユーザー固有・意味論でしか拾えない**（正しい値の後置型「つむぎにしてほしい、四国めたんじゃなくて」/ ソフト指摘型「P6のデザインが違うんだけど」/ 観察型「〜気がする」）。**設計**: hot hook（語彙・ゼロ LLM・低レイテンシ・拡充しない）の上に**意味論レイヤー**を足す。package `scripts/lib/correction_semantic/`（store / prompt / batch / promote / provenance_weight）。**バッチ LLM 意味判定**（auto_memory の 2 相 / ADR-037 と同型・モデル Haiku・llm_broker 経由で Python は claude -p を呼ばない）: Phase A `emit_judgement_requests`（決定論・LLM 非呼び出し）が #430 utterances.db の dialogue 発話を 30 件/call のバッチプロンプトに変換（判定済み `correction_judged.jsonl` の物理キー突合で再判定除外、非対話 PJ は query デフォルト `source_kinds=('dialogue',)` で除外）→ Phase B 応答（assistant）→ Phase C `ingest_judgement_results`（決定論）が `prompt.parse_verdicts` で JSON 解釈（code fence 耐性・壊れた応答は空フォールバック）。「ユーザーが Claude の方向を正したターンか」を二値判定し修正なら idiom 抽出。応答欠損バッチは判定済みにせずスキップ（次 drain で再試行＝broker の skip 方針と同型）。**weak_signals 隔離記録**: 修正判定は corrections 本流に直接入れず #432 と共有レーンへ **channel="llm_judge"** で記録（reflect 確認後に昇格）。**個人辞書** `correction_idioms.jsonl`: 抽出 idiom を provenance（元発話の物理キー source_path:line_no・判定理由）付きで蓄積、idiom+物理キーの安定ハッシュで dedup。実コーパスで precision 検証後に hot hook の補助パターンへ昇格可能。**provenance 重み付け**（`provenance_weight`）: フェーズ昇格カウントを **human-source のみ**で駆動（`HUMAN_SOURCES={reflect_confirmed}`・`MACHINE_SOURCES={hook,backfill,migrate_learnings_queue}`・`correction_type=stop` は source 不問で機械・出所不明/欠落は保守的に機械扱い）。`audit/orchestrator._build_growth_report` の `corrections_count = len(corrections)` を `count_human_corrections(corrections)` に変更し、Growth Report を `Corrections: N (human) / M (total)` 表示に。機械ノイズで growth フェーズが動かないことをテストで保証（human=2/total=12 で Structured Nurturing に昇格しない / human=10 で昇格する）。**reflect 昇格フロー**: `evolve-reflect --show-weak-signals [--weak-channel llm_judge]` で未昇格レコード（channel/provenance.text/reason/signal_key）を表示 → 人間が本物の修正だけ確認 → `evolve-reflect --promote-weak <signal_key,...>` で corrections へ **source=reflect_confirmed**（human-source・reflect_status=applied）レコードを追記 + weak_signal を promoted=True に原子的 rewrite（二重昇格防止）。dry-run はどちらにも書かない。SKILL.md に Step 7.7 追記。**store_registry 宣言**（#434 ゲート）: `correction_idioms.jsonl`（reader=reflect）/ `correction_judged.jsonl`（disposition=drain・reader は同 package の emit 自己消費）を `writer_locus="batch"` で宣言。**evolve 配線 + slug 統一**: evolve オーケストレーターに correction_semantic の Phase A emit を同居（unjudged 件数・batches を surface・トークン見積りは `estimate_tokens`）。あわせて #440 の既知課題 `_ws_slug = Path(project_dir).name`（worktree 内実行で worktree 名になり utterances.db の pj_slug と食い違う）を、utterance_archive と同型の slug 導出（`pj_slug_from_cwd`＝`/.claude/worktrees/` で切って本体 repo basename）に統一する `_resolve_pj_slug` へ修正（weak_signals 配線と correction_semantic 配線の両方で使用）。**dry-run ゼロ書込**（pitfall_dryrun_stateful_store_write）を weak_signals / 個人辞書 / 判定進捗の 3 ストアとも最下層 write まで貫通させ E2E で書き込みゼロを assert。TDD 新規 47 テスト・決定論・LLM 非依存（テストは responses dict を直接渡し claude CLI を一切呼ばない）（`scripts/lib/correction_semantic/`） |
| `bootstrap_backlog` | 初回バックログ bootstrap モード（#443）。既存 weak_signals バックログ（channel ∈ content-rich = llm_judge / rephrase / permission_deny・未昇格・#99）を初回 evolve でまとめて確認する入口がなく死蔵していた問題に対処。`correction_semantic/bootstrap_backlog.build(pj_slug, dry_run=)` が marker（`bootstrap_done-<slug>.marker`）未設定なら当該 PJ の未昇格 backlog を内容キーワード（漢字/カタカナ 2 字以上）jaccard≥0.5 で group 化し `{is_bootstrap, pj_total, groups_total, groups}` を**常時 emit**。marker 立ち後は `is_bootstrap=False` で即返す（重い group 化をしない早期 return — 「TTL 失効に任せる」選択でも marker が立つ）。slug スコープ厳守（backlog 集計は cwd の PJ slug のみ＝DATA_DIR 全PJ共通 pitfall 対策）+ #442 の `expired` を防御的に除外。evolve は `result["correction_review"]["bootstrap"]` に相乗り emit。SKILL.md Step 6.1 が消費し `is_bootstrap=True` なら AskUserQuestion 3 択（まとめて確認 / 日次5件ずつ / TTL 失効に任せる）— 機械は「アクティブ PJ」を判定せず件数を表示するだけ（散文ステップで判定しない・#275 の教訓）。3 択いずれでも Skip でも evolve は完走。marker は store_registry 宣言済み（writer_locus=batch / retention=permanent / disposition=drain）。dry-run ゼロ書込を `mark_done` 最下層まで貫通。**marker 内容（#94）**: `mark_done` は marker に bootstrap 完了 ISO8601 時刻を書く（旧形式の空 marker は `bootstrap_done_at` の mtime fallback で後方互換）。これは `fleet.queue` が「marker 設置以前に detected した weak を material 計数から除外」する read 時導出の基準時刻になる（破棄/TTL 任せ判断済みの weak を queue が TTL まで二重計上していた seam の根治・[[learning_derive_state_from_logs_not_forward_write]]）。**テーマクラスタ畳み込み（#558）**: group 数が多い（実測 48件/45グループ）と Step 6.1 の per-group AskUserQuestion が質問マラソン化し explain-clearly（質問を畳む）と衝突するため、`cluster_groups` が group 数 `THEME_CLUSTER_THRESHOLD=12` 超のときだけ既存資産（`similarity.build_tfidf_matrix` + `reorganize` 階層クラスタリング、距離閾値 `_CLUSTER_DISTANCE_THRESHOLD=0.85`）を再利用してテーマ別バケットに決定論クラスタリングし、バケット単位の multiSelect 1 問に畳む。閾値以下は従来 per-group フロー（挙動不変）、sklearn/scipy 不在・文書僅少時は単一バケットに graceful degradation。短い日本語断片で圧縮が弱い件は follow-up #568。決定論・LLM 非依存（`scripts/lib/correction_semantic/bootstrap_backlog.py`） |
| `daily_review` | evolve の「今日の修正確認」phase（#446）。reflect Step 7.7（散文・手動起動のみ→昇格 0 件）からの移植で、毎日叩かれる evolve の決定論 phase に乗せて reflect 確認の摩擦を除去する。`correction_semantic/daily_review.build_review(pj_slug, ...)` が既読集合に無い未昇格 weak_signal（channel ∈ content-rich = llm_judge / rephrase / permission_deny・非expired・#99）を idiom 単位で group 化（個人辞書の物理キー突合 → 無ければキーワード jaccard≥0.5）・頻度降順・最大5件を `result["correction_review"]["daily"]` に**常時 emit**（新規 0 件でも eligible=False/groups=[] でキーを置く）。SKILL.md Step 6.2 が消費し AskUserQuestion で y/n 確認 — 「はい」→ `evolve-reflect --promote-weak`、**promote 成功後のみ** `record_reviewed(decision="promoted")` で既読追記（部分失敗 group は追記しない＝取りこぼし防止）、「いいえ」→ rejected 追記、Skip → 追記なし（次回再提示）。既読ストア `correction_review_seen.jsonl`（correction_judged と同方式の append-only 物理キー集合・PJ slug スコープ・read 側 set 化で重複追記冪等・detected_at cursor 案は同時刻境界バグで却下・store_registry 宣言済み）。dry-run は build（読み取りのみ）だけ走り既読集合に書かない。reflect Step 7.7 は手動全件レビュー用に残置。**content-rich チャネル拡張（#99）**: かつて channel="llm_judge" 固定で、決定論チャネル（#432）は `evolve-reflect --promote-weak` でしか昇格できず「evolve をフル実行しても決定論 weak が queue から減らない」非対称があった（移植未完の seam・`learning_weak_promotion_channel_asymmetry`）。対象チャネルを単一ソース `review_channels.REVIEW_CHANNELS`（content-rich = llm_judge / rephrase / permission_deny）へ拡張。content-poor（esc_interrupt / manual_edit_after_ai）は detector が周辺文脈（ユーザー発話・直前 AI 行動）を保存せず y/n 確認の判断材料が無いため除外し observability 集計に残す（実データで evolve-anything 未昇格 41 件中 36 件が content-poor）。決定論・LLM 非依存（`scripts/lib/correction_semantic/daily_review.py`） |
| `review_channels` | y/n 確認に出す weak チャネルの単一ソース（#99）。`REVIEW_CHANNELS`（content-rich = llm_judge / rephrase / permission_deny）と `signal_text(rec)`（channel 別 actionable 代表テキスト: llm_judge/rephrase=`user_only_text(text)` / permission_deny=tool_name+tool_input_summary 合成・denial_reason は "unknown" 大半ゆえ意味あるときだけ添える）を bootstrap_backlog / daily_review / promote が共有し、コピー慣習の partial fix（`pitfall_copied_parse_convention_partial_fix`）を避ける。`CONTENT_POOR_CHANNELS`（esc_interrupt / manual_edit_after_ai）は「除外は意図的（detector 文脈未保存）」を機械可読に残す。promote の `_correction_message` も text/reason 無しチャネルで signal_text へ fallback し message=channel 名の空 correction を防ぐ。**`grouping_keywords(rec)`（review F1・#99）**: review group 化に使うキーワードを representative 表示（signal_text）から分離する単一ソース。permission_deny の signal_text は固定 head「<tool> の実行を拒否」しか漢字を持たず `extract_keywords` が全件 `{実行,拒否}` に潰れて異なる拒否コマンドが 1 group に collapse していた（代表1件で一括 y/n＝判断粒度の喪失）ため、permission_deny だけ拒否コマンド（tool_name + tool_input_summary）の latin トークンで group 化（別コマンド→別 group / 同一→merge）、他チャネルは従来どおり signal_text の漢字/カタカナ keyword（挙動不変）。bootstrap_backlog.group_signals / daily_review._group_new の両 grouping ループが共有（partial-fix 回避）。**F1 follow（実 dogfood）**: grouping_keywords が tool_input_summary 全体を token 化し作業ディレクトリの path segment が grouping を支配 → 別コマンド（force push vs checkout/pull）が共通パスの jaccard で同一 group に collapse する over-merge を実データで発見（合成 fixture は緑＝synthetic fixture false confidence）。`_strip_path_words`（'/' を含む語を除外）で grouping を「コマンド種別」に純化（実 permission_deny 5 件 3→4 group・force push 2件は統合維持）。signal_text の 120字切り詰めも単純 slice→単語境界（最後の空白で切り「…」添付）化し representative の途中切れを解消。決定論・LLM 非依存（`scripts/lib/correction_semantic/review_channels.py`） |
| `idiom_autopromote` | idiom_dict 自動昇格（#447・ADR-047）。人間が一度 confirm した修正 idiom と同じ言い回しが再発したとき、毎回 AskUserQuestion で確認する摩擦を除去し weak_signal を機械昇格する。`correction_semantic/idiom_autopromote.autopromote(pj_slug, ...)` → `{promoted, capped, promoted_idioms, slug, dry_run}` を常時 emit。**照合単位は「pj_slug × idiom テキスト」**（`read_confirmed_idiom_texts`）— 出現ごとに変わる idiom_key ハッシュで照合すると新規再発（別発話→別物理キー）が永遠に不一致で構造的 no-op になる設計欠陥をレビューで検出し修正（回帰テスト `test_promotes_same_text_new_occurrence`）。昇格レコードは `source="idiom_dict"` / `promoted_by="idiom_dict"` で `HUMAN_SOURCES` に含む（根拠は human の confirm・revoke で巻き戻し可能）。confirmed が空なら即 promoted=0（昇格雪崩防止、実 PJ E2E: 全 11 PJ / 未確認 idiom 313 件で dry-run・非 dry-run とも promoted=0 / 3 ストア SHA256 不変）。**3つの安全弁**: ① userConfig `idiom_autopromote_daily_cap`（既定10件/日・超過は capped 繰り越し）② weak_signals observability に自動昇格の累計件数 + idiom 一覧を毎回 surface（黙って進まない・ADR-028）③ `evolve-reflect --revoke-idiom <idiom_key>` — confirmed=False + revoked_at（テキスト単位で同テキスト全 record）+ 由来 corrections を `invalidated=True` に原子的 rewrite（`promote.invalidate_idiom_corrections`）。`count_human_corrections` は invalidated を除外するためフェーズ進捗が正しく巻き戻る（weak_signals の promoted=True は維持＝再提示しない）。evolve に phase 配線（`result["idiom_autopromote"]` 常時 emit・dry-run ゼロ書込貫通）。**confirm 配線（#463）**: confirmed 化の正準経路は `evolve-reflect --promote-weak` — promote 成功後に `promote.resolve_idiom_keys_for_signals`（(pj_slug, source_path, line_no) の provenance 物理キー突合・promoted=True 後も解決可能）で承認シグナル対応の idiom を解決し `confirm_idioms(confirmed_by="reflect_promote_weak")` を呼ぶ。従来 `confirm_idioms` が本流から一度も呼ばれず雪崩防止不変条件（confirmed 0 件 → promoted 0）により本機構が**構造的に永久 0 件**だった配線漏れを修正。CLI に閉じる（ADR-045）— evolve SKILL.md Step 6.1 の `promote_signals` ライブラリ直接呼び（confirmed 化をバイパスする裏口）も `evolve-reflect --promote-weak` に一本化。閉ループ E2E（promote→confirmed→同テキスト再発→autopromote 実発火）で回帰封じ |
| `measurement_bug` | 複数 PJ 集計値の bit-exact 一致を測定バグ候補として surface（#445, #185）。learning_measurement_layer_diagnosis の「全 PJ 同値カウント = 測定バグ強シグナル」を #419-#423 では手動診断していたのを自動化。`audit/measurement_bug.py`（`detect_measurement_bug` 純関数 + `collect_cross_pj_metrics` の growth-state walk）+ observability builder `build_measurement_bug_section`（`audit/sections_measurement.py`）。**0 / 0.0 / None を除外した非自明値の PJ 間一致のみ検出**し ≥3 PJ が同一の非ゼロ値（env_score / issues_total）を共有したら候補とする — 0 同値は未測定・データ不足で正当に揃う（#423 既出）ため構造的に除外し FP を回避（precision 優先・ADR-043 整合）。データ源は growth-state-*.json walk（evolve-fleet status と同経路）、issues_total は #419 と同じ 5 フィールド合計の契約を共有。advisory surface のみ（スコア重み非反映）。決定論・LLM 非依存（`scripts/lib/audit/measurement_bug.py`） |
| `growth_report` | evolve レポート末尾の成長状態表示（#448）。「あと N 件で次フェーズ」「今日の昇格成果」を決定論で表示し、毎日 evolve する動機（進捗の見える化）を作る。閾値は growth_engine に切り出した 6 定数（`STRUCTURED_CORRECTIONS_TARGET` 等）が単一ソース（`detect_phase` / `compute_phase_progress` のリテラルを置換・挙動不変）。`scripts/lib/growth_report.py` の `build_growth_report(project_name, *, corrections, review_result, autopromote_result)` が `count_human_corrections` ベースで `corrections {human}/{target} — あと{remaining}件で構造化育成へ` 行を生成（閾値リテラル直書き禁止・read-only）。evolve/ が `result["growth_report"]` を常時 emit（audit phase 後・error でもキーを置く）、SKILL.md Step 9 が lines を列挙。決定論・LLM 非依存（`scripts/lib/growth_report.py`） |
| `outcome_promotion_readiness` | ADR-046 重み昇格レディネスの決定論判定（#461）。outcome 3軸の advisory→重み昇格の判断（期日 2026-06-24〜07-08）を人の勘でなく3条件の機械判定にする。**条件1 分散が十分**: 代表軸（correction 再発率）の per-PJ 値が全 PJ 同値でない（measurement_bug #445 の「全 PJ 同値＝測定バグ強シグナル」思想を流用、PJ<2 は insufficient_pj）/ **条件2 データ件数下限**: 分母 correction≥10・sessions≥30 を満たす PJ が ≥2（PJ 別実測テーブル付き）/ **条件3 方向の妥当性**: optimize_history の human_accepted=True を anchor に前後窓（既定14日・ADR-044 準拠で実 PJ dry-run 観察から決定）で first_try_success が期待方向へ動く相関。各条件 ✓/✗ + evidence（実測値・PJ 名）、3つ全 ✓ で「重み昇格を提案」行を surface。builder `build_promotion_readiness_section`（`sections_promotion_readiness.py`）を `_OBSERVABILITY_BUILDERS` に登録し markdown/構造化 両経路へ自動伝播（ADR-028）。read-only（DATA_DIR 非書込・dry-run byte 不変をテストで担保 #471）、軸計算は outcome_metrics（#423）の純ヘルパ再利用。実データ dry-run は3条件すべて ✗（時期尚早を正しく判定）。**session 系分母は #469 で実効化済み**: sessions.jsonl は #415 の ingest 後 rotate で live がほぼ空のため直読だと条件2/3 が構造的に永久 ✗ だった → `session_store.read_session_records(data_dir, *, since)`（DuckDB sessions.db **read_only 接続** + 未 ingest live jsonl の union read、ingest と同一の `(session_id, timestamp)` dedup・db 優先、HAS_DUCKDB fallback）を新設し、`outcome_metrics.read_sessions` 経由で outcome_metrics / outcome_promotion_readiness の両方が共有（二重実装なし）。実データで条件2 の sessions 分母 0→30（evolve-anything）を実証、残る ✗ は「分母を満たす PJ ≥2」のデータ件数条件＝設計どおりの時期尚早判定。read_only 接続はスキーマ作成も mkdir もしないため read-only 契約（sessions.db mtime 不変）も維持。**条件4 予測妥当性（順位相関・#42）**: 集計平均ベースの skill 順位が分布外（配備時）へ転移するかを測る（arXiv 2606.19704 Beyond Static Leaderboards）。新規 `predictive_validity.check_predictive_validity` が `skill_activations.jsonl` × sessions（union read）の per-skill 一発成功率を `ts` 中央値で in-sample（古い半分=過去）/ out-of-sample（新しい半分=未知）に分割し、両半分に共通出現する skill（≥`MIN_RANKED_SKILLS`=5・各半分で 1 skill あたり session ≥3）を first_try_success で順位付け → **純 Python Spearman 順位相関 rho**（`_ranks` タイ平均順位 / `_spearman` ゼロ分散は捏造せず 0.0・scipy 非依存）を算出。`rho ≥ PREDICTIVE_VALIDITY_RHO_FLOOR`（0.5）で pass。skill 名は namespace prefix を bare 化して join（#577 pitfall_join_key_namespace_mismatch 流儀）。**promote ゲートに AND 合流**（`insufficient_data` は pass=False で保守的に昇格ブロック＝予測妥当性を検証できない環境では昇格させない・「データ不足」を明示し捏造しない）。section は3分岐（pass / データ不足 / 低rho 誤昇格リスク）を full-expand / compressed-gap 両経路へ surface し、定数は `predictive_validity` module を単一ソースとして引く。file-size-budget 順守でロジック本体は別ファイル `predictive_validity.py`（245行）に分離し本体への追加は import+12行に留めた。実 DATA_DIR dry-run: n_skills=5 / rho=-0.0513<0.5（in/out で skill 順位が分布外へ転移）を正しく検出し昇格ブロック＝誤昇格抑制が機能。**分類マーカー（#76 Finding C）**: promote=False の全展開（未達条件 ✗ あり）はマーカー無しだと `report.py` の畳み込みで『✓ 評価済みクリーン（該当なし / drift なし）』へ紛れ「問題なし」と誤読されるため、当該「時期尚早」行に `ℹ` を付与し『観察中』へ分類されるようにした（全 ✓ で promote=True の場合は ✗ も ℹ も無く従来どおり）。**条件3 anchor 二重計上の是正（#77）**: `check_direction` は anchor（apply イベント）単位で `compared`/`expected_dirs` を加算するが before/after は PJ×窓集計値のため、同一 apply が optimize_history に近接 timestamp（実環境 0.8ms 差を観測）で二重記録されると同じデルタを多重計上して非独立証拠で過半が成立する潜在ラッチがあった（[[pitfall_aggregate_without_decomposition]]）。`_dedup_direction_evidence` で証拠を `(pj, axis, before, after)` で重複排除し distinct な証拠だけで過半判定する（pj をキーに含め別 PJ の同一デルタは独立保持・畳むのは同一 PJ×同一窓集計のみ。生 apply 記録数は `anchors` に保持）。実データで sys-bots の二重 anchor が `anchors=2 / compared=1` に畳まれることを実測。決定論・LLM 非依存（`scripts/lib/audit/outcome_promotion_readiness.py`, `scripts/lib/audit/predictive_validity.py`） |
| `cross_pj_priority` | confirmed idiom の PJ 横断優先提示（#462）。PJ A で confirm 済みの修正癖（例「いやいや」「わかりずらい」）は PJ B でも本物である可能性が高い前提で、daily_review / bootstrap_backlog の確認 group のうち**他 PJ の confirmed idiom と同テキスト一致**するものへ `cross_pj_confirmed: [slug,...]` ラベルを付け、先頭に安定 partition（matched-first・group 内相対順維持）で並べ替えて確認摩擦を削減。**提示のみ・自動承認しない**（ADR-047 の機械昇格は同一 PJ 内 confirmed に限定したまま＝雪崩防止不変条件を維持）。照合は `store.normalize_idiom_text`（strip のみ — 中央値10字の日本語断片に casefold/全半角正規化は過剰一致リスクで却下）を idiom_autopromote と**1関数共有**し意味一貫を担保。reader `read_cross_pj_confirmed_idiom_texts(pj_slug)` は自 slug 除外（自PJを cross-PJ と誤計上しない）+ confirmed=True + 非 revoked のみ。`prioritize(groups, pj_slug)` は read-only 純関数で全 group に `cross_pj_confirmed` を必ず付与（非一致は `[]`＝reader 側 missing-key 露出なし）、max_groups slicing より前に適用。evolve SKILL.md Step 6.1/6.2 が「他 PJ（{slug一覧}）で承認済み」を判断材料として提示。実データで figma-to-code の 2 group が先頭 surface を確認。決定論・LLM 非依存（`scripts/lib/correction_semantic/cross_pj_priority.py`） |
| `plugin_self` origin | プラグイン本体スキルの evolve 診断対象化（#185）。発端: evolve-anything を evolve-anything PJ 内で evolve すると本体スキル（repo 直下 `skills/`・23個）が「カスタムスキル 0件のためスキップ」で診断対象外になる構造的ギャップ（3ヶ月放置）。根本原因は `find_artifacts()` が `.claude/skills/` のみ走査していたこと。解消は issue の Option C + B: ① `skill_origin.classify_skill_origin` に `plugin_self` を新設 — `.claude-plugin/plugin.json` を持つリポジトリ直下 `skills/<name>/` を本体スキルとして分類（判定は `_is_plugin_self_skill`、`.claude/skills/` 配下のユーザー自作は親が `.claude` のため不一致）。② `find_artifacts()` は manifest 存在時のみ repo 直下 `skills/` を追加スキャン（#419 の収集除外を共有）— manifest 無しの通常 PJ は挙動不変（回帰ゼロを擬似 PJ で実証）。③ `skill_evolve_assessment` は plugin_self を custom 同等に評価（batch_guard 母集団・per-skill ループ両方）、インストール済み他プラグイン（origin=plugin）は除外維持し件数を `_meta=excluded_plugins` で surface（Option B、silence≠evaluated・ADR-028）。pitfall 剪定は origin フィルタ無しのため find_artifacts 由来で自動解決。④ **auto-apply 安全**: `is_protected_skill` を plugin_self も True に拡張 — SKILL.md を無人書き換えする唯一の経路（remediation `fix_skill_evolve`）は protection ゲートで proposable（人間承認必須）に降格し、診断対象化と直接書き換え防止を両立。実データ検証: find_artifacts で 21 件が plugin_self 分類・`is_protected_skill(skills/evolve/SKILL.md)`=True。決定論・LLM 非依存（`scripts/lib/skill_origin.py` / `scripts/lib/audit/artifacts.py`） |
| `testpaths_coverage` | pytest 収集漏れの決定論検出（#468）。発端: CLAUDE.md の canonical コマンド `python3 -m pytest hooks/ skills/ scripts/tests/ scripts/rl/tests/` が `scripts/lib/tests/`（1111 件＝correction_semantic / weak_signals / audit 系単体テストの大半）を**収集しておらず**、歴代マージゲート・#457 のフルスイート計測がこの分を含んでいなかった（「コマンドはあるが網羅の保証が無い」= learning_install_is_not_enforcement と同族）。根治は2層: ① `pytest.ini` に `testpaths = hooks skills scripts/tests scripts/rl/tests scripts/lib/tests bin/tests` を宣言し bare `python3 -m pytest` で全件収集（3608→4747 件）＝コマンドへのパス列挙依存を断つ。② 再発防止として「`test_*.py` を含む `tests/` ディレクトリが testpaths のどの path 配下にも入らない」漏れを静的突合で検出（orphan_store #422 の writer/reader 突合と同思想の testpath 版）。`parse_testpaths` / `find_test_dirs`（`.git`/`.venv`/`.claude` 等は除外）/ `detect_uncovered_test_dirs` の純関数構成、observability builder `build_testpaths_coverage_section`（`audit/sections_testpaths.py`）を `_OBSERVABILITY_BUILDERS` に登録（ADR-028。testpaths 宣言が無い PJ は非該当で沈黙・漏れなしは「✓ 該当なし」= silence≠evaluated）。検出器自身が実リポジトリで `bin/tests`（3 テスト）の未収録も発見し testpaths へ追加して uncovered=0。決定論・LLM 非依存（`scripts/lib/testpaths_coverage.py`） |
| `dogfood gate` | 通し評価ゲート（#496）。発端: 繋ぎ目調査（#484-#495）で「テスト緑・evolve 無エラー・でも成果物がバグだらけ」が体系的に発生 — 横断根因は「テスト緑・実環境赤」（合成 fixture / conftest path / HOME 隔離マスク）。`bin/evolve-dogfood-gate`（pytest 非依存 CLI、Layer3 がユーザーと同じ素の起動経路を要するため conftest の下駄を意図的に避ける）+ `scripts/lib/dogfood/` パッケージ。**Layer1**: (1a) dry-run SHA256 不変 — **隔離コピー方式**（PR #515）: 実 DATA_DIR を tmp に copytree し `CLAUDE_PLUGIN_DATA=<コピー>` で dry-run evolve を起動、コピー側のみ比較 → ライブセッション hook の ambient write 偽赤を構造的に排除 + dry-run バグでも実環境を汚さない。**文書化された三層除外**（bypass フラグでなく理由コメント付き frozenset）: ファイル名（`skill-evolve-cache.json` / `constitutional_cache.json` = cache warm 設計）/ dir prefix（`evolve_pending/` = #402/ADR-041 の運用ポインタ、#513 で誤ゲート回帰を復元）/ JSON キー（`evolve-state.json::skill_type_cache` = 実 state と同居する cache キーのみ除外し実 state の書込バグ検出は維持）。(1b) **非 dry-run store 差分（書かれるべき方向）を実装済み（#518）** — #484 解決を実環境に近い形で封じる回帰ゲート。隔離コピー方式（#515 流用）で DATA_DIR を tmp にコピー → `CLAUDE_PLUGIN_DATA=<コピー>` で `evolve --drain --result-json <result>` を起動（`--result-json` 指定で `drain_pending` が home 固定 MARKER_ROOT を読まず result JSON の `evolve_decisions.pending` を使う＝隔離完全）→ コピー側 store 差分 + drain サマリで `weak_signals_persisted` 存在 & `dry_run=False` & 決定論チャネル書込を assert。サマリ欠落 / `dry_run=True` / 例外は fail、drain 非ゼロ終了は error。drain サブプロセスは unit では mock（実 drain は `--layer 1` で別途確認）。+ 実PJ ingest E2E（旧 test_real_pj_e2e 直列35秒をゲートへ移設）。**Layer2**: dry-run result JSON の機械検査 — 必須キー / 非負 / 当PJ≤全PJ / observability contract（`_OBSERVABILITY_BUILDERS` 突合。evolve-only キーは `_EVOLVE_ONLY_OBSERVABILITY_KEYS` で明示 allowlist、#504）。**Layer3**: 全 SKILL.md の fenced block を抽出し安全分類して実行（python は import 検証・bash は --help/--dry-run のみ）— sys.path 不足（#487/#488/#495）を素の経路で捕捉。検出力実証: 実機1周で新規6件（sys.path 4件 + contract drift 2件）を即検出。exit 0/1/2。**#517 解消済み**: evolve/ module-level DATA_DIR が `CLAUDE_PLUGIN_DATA` 非対応だった盲点を env 優先解決（`_resolve_data_dir`→`rl_common.resolve_data_dir`、import 失敗時 env→home fallback）に統一し、`CLAUDE_PLUGIN_DATA` 隔離下でも evolve/ の読み書き（`evolve-state.json`/`usage.jsonl` 等）が漏れない。`MARKER_ROOT`（evolve_decisions.py の home 固定）は別契約で不変。**`--layer light`**: pre-push 向けの高速層構成（Layer1a 不変 + Layer2 + Layer3、実機約11秒）。フル `all`（Layer1b の `--drain` subprocess が支配的で約3.5分）から重い Layer1b drain と ingest E2E を除外。`scripts/git-hooks/pre-push.local`（gstack-redact の managed pre-push が chain する `pre-push.local` 拡張点へ `install.sh` でコピー、共有 hooks で worktree 横断）が**非ブロッキング**で起動（赤でも push 継続＝1人開発で `--no-verify` 迂回を招かないため警告のみ）。決定論・LLM 非依存（`bin/evolve-dogfood-gate` / `scripts/lib/dogfood/` / `scripts/git-hooks/`） |
| `evolve-release-sync` | リリース後のローカルプラグイン自動同期。発端: marketplace が evolve-anything 自身を **Directory source（= ローカルの作業ディレクトリ）** として登録しているため、リリース（bump）が worktree→PR→origin/main に取り込まれても、**ローカル main の working tree を pull しない限り marketplace が古いバージョンを配信し続ける**。結果として `claude plugin update` を叩くたびに「最新です」と言いつつ古い（低い）バージョンが入る慢性的な穴があった（cache↔repo の stale とは別問題で、repo 側 working tree 自体が遅れているのが真因）。`bin/evolve-release-sync`（bash）を `claude plugin tag --push` の直後に実行すると、`git --git-common-dir` で本体 repo を解決し（worktree から呼んでも本体を指す）→ 本体 main が `main` チェックアウト中であることを確認（誤同期防止に main 以外は exit 2）→ `git merge --ff-only origin/main`（分岐していれば exit 1 で手動確認を促す）→ `claude plugin marketplace update evolve-anything` → `claude plugin update evolve-anything@evolve-anything` を順に実行。`--dry-run` で実行予定コマンドのみ表示。`.claude/rules/commit-version.md` のリリース手順（tag → release-sync → docs-refresh）に組込。TDD（`bin/tests/test_release_sync.py`: dry-run のコマンド順序 / main 以外 abort / repo 外 abort）。決定論・LLM 非依存（`bin/evolve-release-sync`） |
| `pj_slug` | PJ slug 導出の単一ソース（#492）。発端: slug 導出が2系統（`resolve_slug`=git-common-dir 親 vs `pj_slug_from_cwd`=worktree マーカー文字列処理）に分裂し、同一ストアの read/write で別方式が混ざると worktree 環境で「書いたレコードを読めない」時限式 silent mismatch（#490 の 16 倍乖離も同根）。`scripts/lib/pj_slug.py` に `resolve_pj_slug`（authoritative: git-common-dir 親 basename → git 不可時は worktree マーカーあり path のみ fast フォールバック → 素の非 git dir は `_unattributed` 温存）と `pj_slug_fast`（文字列処理のみ・hooks hot path の subprocess 禁止に対応）を集約、既存2関数は thin wrapper 化（段階移行・後方互換 re-export）。原則: **同一ストアの read/write は同一関数**（weak_signals は書込・daily_review・section 表示すべて `_resolve_pj_slug`→`pj_slug_fast` 系で統一 #490）。`rl_common/persistence.py` の `PJ_SLUG_NORMALIZATION_DATE` が正規化境界日。**#29 SessionStart cache**: `pj_slug_fast` は `/.claude/worktrees/` マーカー配下しか親 repo へ畳めず、sibling-dir worktree（マーカー外）は write 時に幻 PJ slug を残していた（#593 の write 時残課題）。SessionStart（`hooks/restore_state.py` の `_persist_pj_slug_cache`）で hot-path でない `resolve_pj_slug`（authoritative）を1回解決し `DATA_DIR/pj_slug_cache.json`（`PJ_SLUG_CACHE_FILENAME`）に `{cwd: slug}` を書く（`_unattributed`・git 外素 dir は書かない・例外は failsafe で hook を落とさない）。`pj_slug_fast(path, data_dir=...)` は (1) マーカー fast-path →(2) data_dir 渡し時のみ cache 参照→(3) basename フォールバック、の順で subprocess を呼ばず sibling-dir worktree を本体 slug に解決（`data_dir` 未指定の既存呼び出し元は従来 basename 挙動＝後方互換）。バックフィル7ストア化（#602/migrate-pj-slug）は別 done。cache は ephemeral `.json`（SoR でない）ため store_registry/orphan 契約対象外。**#47 非git writer/reader 対称化**: 旧 `resolve_pj_slug` は worktree マーカー無しの非git dir を `_unattributed` 固定で返し、hot-path writer の `pj_slug_fast`（basename）と非対称だった（非git PJ で hook 書込テレメトリ=basename を reader=`_unattributed` で探して交差ゼロ＝全 section 沈黙 + 非git PJ 同士の `_unattributed` 混在誤帰属。zundamon-explainer は .git ありで未発火だが latent）。非git fallback を `pj_slug_fast`（basename）に委譲して writer/reader を同一規約へ統一、basename も取れない真の空 path のみ `_unattributed`（calibration 除外センチネル温存）。これで SessionStart bridge も非git PJ の cwd→basename を cache する（旧版は `_unattributed` で cache スキップ）。併せて `triage_ledger.resolve_slug` / `remediation/suppression_ledger.resolve_slug` の重複 git subprocess 実装を `resolve_pj_slug` に単一ソース化（別実装による non-git slug 食い違いを構造排除・未使用 import 除去）。セマンティクス変更: 非git PJ が basename で calibration に含まれる（非git は稀・basename は writer 既使用の識別子で除外より正確）。決定論・LLM 非依存（`scripts/lib/pj_slug.py` + `hooks/restore_state.py`、#29/#47） |
| weak_signals drain 永続化 | 決定論3チャネル永続化の apply 境界配線（#484）。発端: weak_signals 4チャネルのうち決定論3チャネル（manual_edit_after_ai / esc_interrupt / rephrase）が全PJ通算0件 — 根因は標準 evolve フロー＝`evolve --dry-run` 分析→assistant 対話適用のため、`run_batch(dry_run=dry_run)` の書込が最下層 dry-run ゲート（#491）で常にゼロになり、非 dry-run evolve は標準フローで走らない（#400 evolve_decisions と同型の「dry-run 検証の盲点」。llm_judge だけ 313 件あるのは SKILL.md Phase B/C の独立した非 dry-run 経路を持つため）。解消: `persist_weak_signals_drain`（`run_batch(dry_run=False)` の apply 境界専用入口）を `evolve --drain` CLI に配線、drain サマリの `weak_signals_persisted` で常時 surface。決定論検出は signal_key dedup で冪等＝drain 多重実行も安全。実環境実走で written=18・3チャネル初出現を確証。関連: pending marker の dry-run 書込は #402/ADR-041 の意図された設計で、#505 が誤ゲートした回帰を #513 が復元（SHA256 不変契約とは gate/E2E の文書化された除外リストで両立）。決定論・LLM 非依存（`scripts/lib/weak_signals/batch.py` / `evolve --drain`＝`skills/evolve/scripts/evolve/cli.py`・#531/ADR-048 で分割済み） |
| `idiom_filter` | 過汎用 idiom の FP guard（#527）。confirmed 化された過汎用な短文字 idiom が `idiom_autopromote`（#447/#463）の FP 製造機になる問題を決定論ゲートで根治。実測（`correction_idioms.jsonl` の confirmed 中の極短 idiom「いやいや」「じゃなくて」「気がする」「比率だけ」「いや、2/24の」）に合わせ 3 ゲートを 1 関数 `idiom_eligible(text)` に集約: (1) 最小長 floor（正規化後 8 文字未満を弾く）/ (2) 日常語 stopword（相槌・推量・否定のみで具体修正内容を持たない言い回しを弾く。具体名が残れば残す）/ (3) 文脈固有トークン（日付 `2/24` / 割合 `80%` / 序数 `3番目` など再発しても別文脈になる断片を弾く）。`eligible_reason`/`filter_idioms` も提供。`batch.ingest` が idiom 化時に弾き（個人辞書に入れず weak_signal は隔離記録のまま・弾いた件数を `idioms_filtered` で surface）、`idiom_autopromote` にも防御ゲートを置きガード導入前に confirmed 化済みの過汎用 idiom も自動昇格しない。**#527-4 提示配線**: bootstrap_backlog / daily_review が group に `confirmable_idiom`（「はい」確定で confirmed=standing auto-promote rule になる idiom テキスト・eligible 時のみ。過汎用は None）を decision 材料として常時 emit、evolve SKILL.md Step 6.1/6.2 の AskUserQuestion がこれを提示して idiom 単位の拒否を可能化（None なら今回限りの昇格と明示）。決定論・LLM 非依存（`scripts/lib/correction_semantic/idiom_filter.py`） |
| `representative` | correction group の representative 品質改善（#528-3 部分）。一行 representative に assistant の過去レポート引用が混じり判読不能だった問題に対処。`user_only_text(text)` が assistant 引用ブロック（`>` markdown quote / code fence / ℹ️・✓・✗ 等のステータス絵文字プレフィックス行）を strip し user 発話のみ抽出（全行が引用なら情報喪失回避で元 text fallback）。`prev_action_summary` が直前 AI 行動の 1 行要約を生成し evidence に添える（一行 representative の文脈補完）。batch が ingest 時に user-only text + prev_action を provenance に保存。**注意**: prev_action 追加は NEW weak_signal の `signal_key`（channel+provenance ハッシュ）を変えるが既存レコードは不変＝dedup 維持。決定論・LLM 非依存（`scripts/lib/correction_semantic/representative.py`） |
| `multiview_eval` | evolve 提案の多視点評価（#564, tech-eval SEAGym arXiv 2606.17546）。evolve 提案の評価が「単一の accept/reject」に潰れ「再利用可能な改善か / 過学習か / 退行リスクか / コスト増か」が見えなかった問題に対し、既存3部品（`chaos` 仮想アブレーション / `outcome_attribution` 一発成功率・rework率 / `usage.compute_negative_transfer` スキル追加前後の success delta）を skill 名で join し、各 evolve 対象スキルを4視点ラベル（reusable_improvement / overfit_suspect / regression_risk / cost_increase）に決定論分類する薄い集約レイヤ。`classify_multiview` は純関数で DATA_DIR 再読込なし（dry-run 安全）、degraded（テレメトリ不足）は沈黙でなく明示（silence≠evaluated #393-#396）。observability builder `sections_multiview` が audit/evolve レポートに advisory surface（chaos は shadow copy 全再計算で重いため builder では再実行せず outcome/negative-transfer のみ軽量集約、chaos 入力フックは docstring に明示）。chaos しきい値は `config.py` の `CHAOS_THRESHOLDS` から複製し契約テスト（`test_chaos_thresholds_match_fitness_config`）で drift 検出。replay スナップショット比較は将来拡張フックのみ（スコープ外）。**#577（実PJ dogfood fix）**: join 両辺のキー名前空間不一致（`target_skills`＝SKILL.md dir 名 bare `cleanup` vs `outcome_attribution`＝起動時のスキル名 `evolve-anything:cleanup`）で実データでは交差が常に空集合になり「該当視点なし」固定だった繋ぎ目を、`_bare_skill_name`（`<plugin>:` 剥がし・`Agent:*` 除外）+ `_index_outcomes`（exact-bare 優先）で正規化。実測 join 0→3 スキル。決定論・LLM 非依存（`scripts/lib/audit/multiview_eval.py`, `audit/sections_multiview.py`） |
| `relevance_gate` | FinAcumen 流の関連度ゲート付き経験提案＋無関係抑制（#565, tech-eval FinAcumen arXiv 2606.17642）。過去経験（weak_signal / idiom）の提案が Top-N 一律で無関係な記憶も提案根拠に混じる問題に対し、「現在の文脈キーワード集合」との意味的関連度（既存 `JACCARD_THRESHOLD`=0.5 流儀を再利用・引数で上書き可）で選別し、閾値超過を `kept`（関連度降順・提案根拠）に、閾値未満を `suppressed`（`suppressed_reason` を残し黙って消さない）に分離する純関数群（`candidate_text` / `score_relevance` / `gate_candidates` / `summarize_gate`）。各候補に `relevance_score`（0.0-1.0）を付与。文脈キーワードが抽出できないときは `gate_applied=False` で全件素通し（経験を勝手に隠さない安全側フォールバック）。`evolve-reflect --show-weak-signals --context "<文脈>"`（`--relevance-threshold` で閾値上書き）に配線し出力末尾の `relevance_gate` サマリで抑制の効きを確認可（`--context` 無しは全件提示の後方互換）。独自の閾値学習機構は作らない。**#578（実PJ dogfood fix）**: `RELEVANCE_THRESHOLD` が near-duplicate dedup 用 `JACCARD_THRESHOLD`(0.5) の流用で実コーパス（jaccard max~0.25）に到達せず kept=0 の全件 suppressed no-op に倒れていたため、relevance 専用の校正値 0.2 に decouple（metric は jaccard 据え置き）。実測 kept 0→3。決定論・LLM 非依存（`scripts/lib/correction_semantic/relevance_gate.py`） |
| remediation 参照リンク相対化 | separation の emit prompt がマシン固有絶対パスで参照リンクを指示する問題を根治（#524）。`fixers_llm.reference_link_for_prompt(ref_path_str)` が `.claude/` セグメント以降の PJ ルート相対パス（`.claude/references/<name>.md`）を導出し prompt に出力（`.claude/` を含まないパスは素通し＝安全側）。実際の書込先（絶対パス）は `meta["reference_path"]` に保持・ingest は従来どおり絶対パスへ書く（相対表示用 `meta["reference_link"]` も併記）。あわせて `references/remediation.md` に emit/ingest 6 関数の実 signature 表 + separation(4-arg)/split(2-arg) の呼び出し例を追記（3-arg `emit_compression_request` 例のみで 4-arg `emit_separation_request` 流用が TypeError になる #524-1 を解消）。決定論・LLM 非依存（`scripts/lib/remediation/fixers_llm.py`） |
| `report-feedback` | evolve/audit レポートのメタレビュー起票スキル。決定論 `evolve_introspect`（Step 11）は result dict の機械的矛盾（split↔archive 等）しか拾えず、「レポートを読んで初めて気づく」改善（数字の母数欠落・提案の質・誤検知・表示バグ・UX 摩擦）を起票する経路が無かった。本スキルは LLM がレポートの**中身（対象環境の改善）でなく出来栄え・挙動**をメタレビューし、evolve-anything 自身への改善候補を `evolve_introspect` の candidate スキーマで生成 → 同モジュールの `flatten_candidates`/`filter_duplicates`/`render_issue_body` を再利用して dedup・重複防止マーカーを共有 → 人間個別承認のうえ `todoroki-godai/evolve-anything` に起票する。2経路（**audit 経路**=`evolve-audit` の stdout レポート本文を入力・`self_analysis` 無し / **evolve 経路**=result JSON の `self_analysis` を決定論 seed として併用）＋会話経路（旧 `feedback` の後継）。起票先はパイプライン自身なのでどの PJ で動いても固定。他PJから呼ぶため SKILL のスクリプト参照は `${CLAUDE_PLUGIN_ROOT}` 経由（相対パスは対象PJ cwd で No such file になる pitfall 回避）。public repo 起票のため Step5 に「対象PJ固有語を一般化・数値は現象として記述」のプライバシーチェックを MUST 配置。**旧 `feedback` スキル（会話から GitHub Issue 化・全履歴7回のみ使用）を統合・削除**。LLM 部分は決定論検証不能だが「候補スキーマ↔dedup/render 配線」は契約テスト `scripts/lib/tests/test_report_feedback_contract.py`（5件・LLM 非依存）が固定（`skills/report-feedback/SKILL.md`） |
| `paired_trajectory` | paired trajectory auditing（観測版、#15, tech-eval）。発端: 「あるスキルが実際に挙動を良くしているか」を、能動再実行なしで既存テレメトリから対照観測する経路が無かった（`compute_component_transfer` は時系列前後デルタで別物）。同一タスク種別の軌跡を「そのスキルが使われた群 vs 使われなかった群」に分け、アウトカム（一発成功率等）の差を決定論で対照集計するシグナル化レイヤ。**観測版に限定**＝shadow 再実行ハーネスは作らない（別 issue）。observability builder `sections_paired` を `_OBSERVABILITY_BUILDERS` に登録し markdown/構造化 両経路へ advisory surface。outcome_attribution / multiview_eval / negative_transfer と相補（重複しない別軸）。degraded（対照群が組めない）は沈黙でなく明示（silence≠evaluated）。**#34（実PJ dogfood 訂正・close）**: paired が空なのは `task_type` フィールド欠落ではなく、task-type＝「skill 集合−対象スキル」導出が **1セッション2+スキルを要求**するため実運用でスパース（1スキルのみは context 空で全除外）。配線バグでなく**設計上の制約**＝高スキル密度セッション蓄積 or task-type 信号源リッチ化までは点灯しない（仕様化して受容）。決定論・LLM 非依存（`scripts/lib/audit/sections_paired.py` + `audit/usage.py`、#15） |
| recall `[[link]]` 1-hop | `bin/evolve-fleet recall` の芋づる想起（#11）。memory fact 本文中の `[[name]]` リンクを `Fact.links` として抽出し、キーワードヒットした fact の **1-hop 先**（同一 PJ 内）も結果へ追加する（dangling リンク無視・直接ヒットとの重複排除・スコア対象外で末尾 `↳ linked:` 表示・JSON は `linked` フィールド）。既存のキーワード検索挙動は不変で 1-hop 分は加算のみ。ADR-025（決定論キーワード検索・vector/embedding 非採用）と整合。決定論・LLM 非依存（`scripts/lib/fleet/recall.py`、#11） |
| recall validity-aware ranking | grounding metadata をランキングに消費する配線漏れの根治（#74）。背景: `valid_from` / `superseded_at` / `decay_days`（grounding validity）は `scripts/lib/memory_temporal.py` で書込・判定 API（`parse_memory_temporal` / `is_stale` / `is_superseded`）まで実装済みだが、`fleet/recall._score` は TF + name/desc boost のみで temporal validity を**ランキングに消費していなかった**。本変更で `parse_fact_file` が `parse_ok=True` のときのみ `_temporal_validity()`（memory_temporal の既存 API 再利用・再実装なし）を呼び `Fact.is_stale` / `Fact.is_superseded` を埋め、`_score` 末尾（INDEX_PENALTY の後・score>0 のときのみ）で penalty を適用する。**superseded を stale に優先し penalty を stack しない**（`_SUPERSEDED_PENALTY=0.25` / `_STALE_PENALTY=0.4`）。**ハード除外しない**＝stale/superseded でも keyword 一致なら結果に残し順位だけ下げる（RaMem 論文 (iii) のフォールバック保持＝古い記憶も最後の手段として参照可能にする趣旨）。frontmatter 無し（MEMORY.md index 等・`parse_ok=False`）は temporal を計算せず両方 False のまま（後方互換）。注意: 実 writer（`write_temporal_metadata`）は isoformat を quoted 文字列で書くため `is_stale`/`is_superseded` が解釈できる（unquoted ISO は YAML が datetime に自動変換し判定不能になる）。SessionStart 側（`hooks/instructions_loaded.py` の reinforce skip / STALE 警告）は #18 で既に stale/superseded を扱っており本変更の対象外（recall ランキングのみが net-new）。決定論・LLM 非依存（`scripts/lib/fleet/recall.py`、#74） |
| reinforce_memory 配線 | 忘却曲線強化の本番配線（#18）。`memory_temporal.reinforce_memory()`（importance/last_accessed 更新）が実装済みだが caller がテストのみ＝dead code だった問題を解消。(a) recall ヒット時＝`fleet/recall.reinforce_recall_hits(hits)` を CLI `_run_recall` から opt-in 発火（recall() 本体に入れると純粋性契約＝副作用は stderr 警告のみ、を壊し dry 分析でも書き込むため発火境界＝実コマンドに限定）、(b) SessionStart の MEMORY.md 注入時＝`hooks/instructions_loaded.py` の `_reinforce_loaded_memory()`（有効 memory のみ強化、stale/superseded はスキップ＝「よく参照される有効 memory ほど残る」意図に沿わせる）。決定論・LLM 非依存（`scripts/lib/memory_temporal.py` + `fleet/recall.py` + `hooks/instructions_loaded.py`、#18） |
| `memory_capability` | 記憶操作 capability section（read/use/write/maintain・#19, tech-eval）。発端: OPD-Evolver（arXiv 2606.17628）の「経験を保存することと進化術を learn することは別」という問題提起を受け、記憶を read/use/write/maintain の観点で評価し死蔵・未活用を可視化する経路が無かった。fitness の重み軸にはせず outcome_metrics と同じ advisory レーンに置く（既存4軸の重み再校正による退行リスクを避け、issue が懸念する既存軸との重複をそもそも発生させない）。**4観点→3軸統合の理由**: `reinforce_memory(filepath, reason)` の `reason` はファイルに書かれず（ログ用）、かつ reinforce は SessionStart で有効 memory 全件に発火する（`_reinforce_loaded_memory`）ため frontmatter から「recall 由来か注入由来か」を事後復元できず read を独立軸にできない（静的存在指標は near-constant）。→ write（temporal frontmatter あり率）/ maintain（非 stale かつ 非 superseded の健全率）/ use_read（last_reinforced_at あり率 + update_count 中央値、「per-memory の recall 回数でなく PJ 活性 proxy」の限界注記付き）の3軸。observability builder `sections_memory.build_memory_capability_section` を `_OBSERVABILITY_BUILDERS` に登録し markdown/構造化 両経路へ advisory surface（沈黙境界＝memory 実体0件は None、outcome と同契約）。設計は senpai 独立検証で「advisory 形態 GO・read/use 統合・slug 解決経路明記」を反映。**namespace バグ根治（実環境 dogfood 発見）**: memory dir は CC の `~/.claude/projects/<path-encoded>/memory`（cwd 絶対パスの `/`→`-`）に在り、`resolve_pj_slug` の repo-basename slug（例 `evolve-anything`）とは名前空間が別物。初版は `resolve_pj_slug` を使ったため別 dir を指し section が**常に沈黙**していた（synthetic fixture が同じ誤 slug で dir を作り実装と握り合って緑＝[[learning_synthetic_fixture_false_confidence]] の再演）。`pj_slug.resolve_cc_memory_dir` を**単一ソース**として新設し memory_capability と #18（reinforce/stale 配線）を両方それへ張替（[[pitfall_copied_parse_convention_partial_fix]] 準拠で read/read desync を防止）。実 PJ dogfood で memory 84 件・3軸算出を確認。決定論・LLM 非依存（`scripts/lib/memory_capability.py` + `scripts/lib/audit/sections_memory.py` + `pj_slug.resolve_cc_memory_dir`、#19） |
| `skill_vuln_scan` | 取り込みスキルの静的脆弱性スキャン section（SkillSpector 型・#13）。発端: 外部由来スキル（SKILL.md プロンプト + 同梱シェルスクリプト）を取り込む際、悪意あるパターン（リモート取得→shell 実行 / 秘密ファイルのネットワーク exfil / 破壊的コマンド / SKILL.md 埋め込み prompt injection / frontmatter の全ツール付与）を見落とす穴があり、それを **決定論・LLM 非依存** で静的検出する経路が無かった。`scan_skills(root)` が `root/skills/` 配下を行単位で走査し `Finding(rel_path,line,category,severity,pattern_id,snippet)` を列挙、`SkillVulnReport(applicable,scanned_files,findings)` を返す。**FP 較正（このリポジトリの偽陽性厳格主義に沿う）**: ① **combo 必須・bare 単体は非検出** — `curl https://...`（単独）は正当な取得なので素通し、`curl http://... | sh` のような shell へ流す combo のみ remote_exec(HIGH)。② **`gh api ... | base64 -d`（GitHub content デコード）は実在 FP** — `base64 -d` 単体は検出せず `base64 -d ... | sh` の combo のみ検出（実 `skills/release-notes-review/SKILL.md` の base64 行を回帰ロックで非検出固定）。③ destructive(MEDIUM) は `rm -rf /`・`~`・`$HOME`・`*` のみで `rm -rf ./build` の相対は非検出、chmod 777 / fork bomb。④ secret_exfil(HIGH) は「秘密ソース（~/.ssh/id_/.aws/credentials/.env/printenv 等）」と「ネット sink（curl/wget/nc/http）」が**同一行に共起**したときのみ（片方だけは非検出）。⑤ prompt_injection(MEDIUM) は ignore/disregard previous・do not tell the user・日本語「これまでの指示を無視」等。⑥ overbroad_tools(LOW) は frontmatter `tools: *`。**対象拡張子は `.md`/`.sh`/`.bash` のみ**（`.py` は FP 抑制のため本 PR 対象外＝follow-up）。除外ディレクトリは testpaths_coverage 同等（`.git`/`__pycache__`/`tests` 等）。配線: observability builder `audit/sections_skill_vuln.build_skill_vuln_section` を `_OBSERVABILITY_BUILDERS` に登録（markdown/構造化 両経路へ自動伝播）。skills/ が無い PJ は None（沈黙）、findings 空は「✓ 評価したが該当なし」、findings あり は ⚠（HIGH を上に並べ rel_path:line evidence）で `classify_section`=critical に落ちる。**follow-up（本 PR 対象外）**: `skills/import/` への PreToolUse 取り込みゲート配線と `.py` スキャン。**実 corpus dogfood**: 実 main checkout の skills/ 42 ファイルを走査し findings 0（base64 FP 非検出を確認）。決定論・LLM 非依存・読み取りのみ（`scripts/lib/skill_vuln_scan.py` + `scripts/lib/audit/sections_skill_vuln.py`、#13） |
| `fanout_cost` | fan-out 費用対効果 section（#14, tech-eval, arXiv 2606.13003 "The Illusion of Multi-Agent Advantage"）。発端: 「複数 agent を fan-out すると本当に得か」を自 PJ テレメトリで検証する経路が無く、subagent-guard は件数ゲートのみで「効くか」を見ていなかった。fitness 重みにはせず outcome_metrics と同 advisory レーン。**cost 先行・advantage データゲートの2部構成**: ① cost（非スパース・常時算出）= fan-out session 率（同一 parent session_id が subagent ≥2 体 spawn / subagent ≥1 体 spawn）+ 平均 subagent 数 + agent_type 内訳。token 単位 session join は subagents↔token_usage に綺麗な対応が無いため未対応で subagent 体数を proxy とし注記（捏造しない）。② advantage（floor ゲート付き）= fan-out 群 vs single 群の一発成功率 delta を各群 ≥5 session（`MIN_GROUP_SESSIONS_FLOOR`、outcome_metrics の edit_sessions≥5 に倣う）で算出、未満は「データ不足（サンプル不足）」明示（#15/#10 が踏んだ構造的スパース性への対処）。データ源: subagents.jsonl（agent_type 空除外 #36・`fleet/collectors` と同契約）+ sessions（`outcome_metrics.read_sessions` の union read・#469 を流用し error_count==0 を一発成功）。PJ スコープは `outcome_metrics._normalize_pj` を共有（worktree 安全・#489）。**設計判断**: 効果計測を当初 issue の narrow target（rl-scorer 3軸 vs 単一）に限定せず汎用 subagent fan-out（impl-worker/Explore 等）に拡張（narrow は実運用スパース）。実データ（rl-anything slug・subagents 7,419行）でロジック実証済み: fan-out率 0.69 / 平均 14.84 体 / advantage −0.20（論文の「fan-out 優位は見かけ倒し」と整合する負 delta が小標本で観測）。**現環境注意**: canonical DATA_DIR（evolve-anything）は migrate-data #364 未実行で空、実データは rl-anything dir 残留 + 旧 slug 属性のため current PJ では沈黙（outcome_metrics 等と共通の pre-existing seam・fanout_cost のバグではない）。決定論・LLM 非依存（`scripts/lib/fanout_cost.py` + `scripts/lib/audit/sections_fanout.py`、#14） |
| `memory_contagion` | 評価源バイアスの記憶伝播を audit advisory で検出する section（#73）。発端: 評価源（evaluator source）に偏りがあると、その経験が記憶 / correction に蓄積され将来の提案・採点に伝染する（Memory Contagion・authority bias 増幅）。当 PJ の評価源を **human / machine に決定論で分解**し、機械評価源の蓄積が人間確認源を大きく上回っていないかを surface する（完全に決定論・LLM 非依存・スコア非関与）。**評価源分解の根拠は既存実装を再利用**（再実装しない）: corrections は `correction_semantic.provenance_weight.is_human_correction`（HUMAN_SOURCES={"reflect_confirmed","idiom_dict"} のみ human、それ以外と Stop hook 系は機械）、idioms は `correction_semantic.store.read_idioms` 各 record の `confirmed`（人間が #446 review で承認）を human・未確認（LLM judge 段階）を machine とみなす。`human_total = human_corrections + confirmed_idioms` / `machine_total = machine_corrections + unconfirmed_idioms`。**保守 verdict 4分類**（cry wolf を避ける側に倒す・閾値は `MACHINE_FLOOR=10` / `RATIO=3.0` で実コーパス dry-run 校正前提）: 両ゼロ→applicable=False（沈黙）/ human=0 & machine≥floor→`no_human_baseline`（比較基準なし＝ℹ 扱い・⚠ にしない）/ human>0 & machine≥floor & machine≥RATIO×human→`contagion_risk`（⚠ authority bias の兆候）/ それ以外→`healthy`（小標本・バランス健全）。**PJ slug スコープ厳守**（全 PJ 共通 DATA_DIR pitfall）: corrections.jsonl / correction_idioms.jsonl はいずれも全 PJ 共通ストアなので、capture_rate と同じスコープ方式（corrections は `capture_rate._normalize_pj` + `_project_match`、idioms は `pj_slug_fast(project_dir)` × `store_read_union.pj_slug_match`）で当 PJ 分のみ数える。引数なし cwd 導出は worktree で slug 食い違いを起こすため禁止し、builder は `project_dir` を受けてそこから slug を導出する。builder `build_memory_contagion_section` を `_OBSERVABILITY_BUILDERS` に登録し markdown / 構造化 両経路へ自動伝播（ADR-028）。決定論・LLM 非依存（`scripts/lib/audit/memory_contagion.py` + `scripts/lib/audit/sections_contagion.py`、#73） |
| `fleet_queue` | 学習素材ベースの evolve 待ち列挙 `evolve-fleet queue`（#79 Phase 1a・daily-evolve Epic #78 の入口）。発端: 全 PJ から「今 evolve でレビューすべき PJ」を **決定論・ゼロ LLM** で列挙する経路が無かった。毎朝の定期実行（Phase 1b #80）から叩かれ、ユーザーが対話で処理する PJ を選ぶ。**待ち定義（学習素材ベース）**: `material_count = weak_unprocessed`（未昇格・未expired の weak_signals 件数）`+ new_corrections`（前回 evolve 以降の新規 corrections 件数）が `--threshold`（既定 **5**・env `EVOLVE_QUEUE_THRESHOLD` 上書き可・userConfig 拡張余地）以上の PJ。補助シグナル `activity_since`（subagents 等）は**フィルタに使わず列挙理由に併記**。**コア純関数** `select_evolve_queue(pj_materials, threshold)`（store I/O なし・閾値 `>=` 比較 + material_count 降順 / pj_slug 昇順ソート + reason 付与・テスト容易）。**store reader**: `weak_unprocessed_by_pj`（既存 `correction_semantic.promote.read_unpromoted(exclude_expired=True)` を再利用し pj_slug でスコープ＝weak_signals.jsonl は単一 DATA_DIR ファイル・pj_slug は record 属性）/ `new_corrections_by_pj`（corrections を `last_evolve_at` 以降で filter・state 不在=None は全件＝初回全件待ち）。`build_queue_result` が Phase 1b #80 が読む **exact スキーマ**（`generated_at`/`threshold`/`tracked_total`/`queue[{pj_slug, material_count, weak_unprocessed, new_corrections, last_evolve_at, activity_since, reason}]`）を組み立てる。**per-PJ last_evolve state**: 既存 `evolve-state.json` はグローバルで PJ 別に「前回 evolve 以降」を測れないため、新ストア **`evolve-queue-state.jsonl`** を新設（`queue_state.py`・append-only `{pj_slug, last_evolve_at, ts}` + read 側 last-append-wins fold＝reward_ema/subagent_traces と同方式で write barrier に素直に乗せる）。store_registry に **active**（`writer_locus="batch"`・retention=permanent）登録 + active store keyset snapshot 追従、書込は **store_write barrier（ADR-049）経由**で `evolve --drain` の apply 境界（非 dry-run・正準 DATA_DIR）でのみ永続化（reward_ema #64 / weak_signals #484 と同型・dry-run ゼロ書込 #308/#513）。reader（fleet queue）は read-only 純度（ファイル不在→空・**1バイトも書かない**・非作成）。**名前空間統一**: corrections の `project_path` は実コーパスでフルパス（`/Users/.../amamo`・古い hook）/ bare slug（`amamo`・#593 後）が混在するため `_correction_slug` が `project_name_from_dir`（pj_slug_fast → worktree 切詰 → basename fallback の単一ソース）で weak_signals の `pj_slug` と揃えてから突合する（新方式を発明しない・[[pitfall_copied_parse_convention_partial_fix]] 回避）。**閾値既定 5 の根拠**（ADR-044「閾値前に実データへ dry 適用」教訓）: 実 DATA_DIR dry-run（weak 373件 / corr 90件）で active PJ の material_count 最小が 5（temp dir 除く・figma 116 / rl-anything 65 / amamo 64 / atlas 24 / sys-bots 24 …）、trickle PJ（kazevolve=2）との自然 gap が 5 にあるため。**既存 `evaluate_corrections`（グローバル correction trigger）との関係＝補完（層が異なる）**: あちらは単一 PJ で correction 閾値超過時にその場で evolve を即時提案する（reactive・PJ ローカル・correction のみ）。queue は全 PJ 横断で「朝にまとめて選ぶ」proactive な入口（pull 型・weak+corr の合算）。両者は競合せず役割が直交。**実 PJ E2E**: `bin/evolve-fleet queue --json` が tracked 10 PJ・waiting 6 PJ を読み取りのみで列挙・state ファイル非作成・corrections slug 正規化（rl-anything corr=30 等）を実測。CLI `_run_queue`（text テーブル `PROJECT/MATERIAL/WEAK/CORR/LAST_EVOLVE/REASON` + `（N waiting / M tracked）` / `--json`）。TDD 25件・決定論・LLM 非依存（`scripts/lib/fleet/queue.py` + `scripts/lib/fleet/queue_state.py`、#79）。**dogfood 追補（#85/#86・2026-06-25）**: ⑴ **#85** — `activity_since.sessions` が `_gather_queue_result` でリテラル 0 にハードコードされ subagents は実値なのに sessions だけ 0 という内部不整合（session を PJ 別集計する関数が不在なのが根因）。`collectors.aggregate_sessions_by_project`（`aggregate_subagents_by_project` と対称）を新設し `session_store.read_session_records_union`（cross-dir union・read_only・#469 流儀）で読んだ session を project 別に **distinct session_id**（sessions.jsonl は 1 session 複数行ゆえ行数でなくユニーク数）で集計、project は `outcome_metrics._normalize_pj`（fanout_cost と共有の単一ソース）で slug 化・空/解決不能は `(unknown)`・30日窓・読み取り専用。⑵ **#86** — queue の母集団は `tracked_projects` 限定だが material 母集団（weak_signals/corrections の全 pj_slug）の方が広く、material を持つが tracked 外の PJ（実機: amamo material 64 / ai-daily-report 17 / daily-ai-github-trending 16 / aws-cost-guardian 9）が待ちにも skipped_dead にも出ず**完全沈黙**して真の evolve 候補を取りこぼしていた（O2）。`collect_untracked_materials`（純関数）を新設し、material slug のうち ① tracked に無い ② CC discovery（`fleet_config.filter_valid_projects(discover_cc_projects())`）で**実 dir に解決できる**（phantom/temp slug 除外ゲート＝`tmpXXXX` 等を弾く）③ `(unknown)` でない slug について `weak_unprocessed_by_pj + new_corrections_by_pj`（untracked は last_evolve state 無し＝全件）を集計し `material_count >= threshold` を降順で返す。`build_queue_result` に末尾 kwarg `material_slugs` / `untracked_dir_map`（既定 None で後方互換＝両方与えられたときのみ算出）を追加し result に `untracked_with_material` キーを追加。`cli._gather_queue_result` の `_collect_material_slugs`（weak=`read_unpromoted` の `pj_slug` / corr=`_correction_slug` で per-PJ reader と同一正規化）+ 実 dir gate map を構築して渡す。formatters `_append_untracked` が footer 下に上位5件を `（未追跡だが学習素材あり: amamo (material 64), … — tracked 追加を検討: evolve-fleet discover）` で surface（silent truncation 禁止）。**auto-track はしない**（advisory のみ・人間が `evolve-fleet discover` で承認＝pull 型思想と整合）。O1: footer の `M tracked` を `M tracked (config)` にし denominator が config 由来である旨を自己説明化（`tracked_total` は意味を変えず `len(pj_slugs)`）。#80 daily consumer（`queue_notice`）は `.get()` 防御読みのためキー追加は非破壊。TDD 追加・決定論・LLM 非依存（`fleet/collectors.py` + `fleet/cli.py` + `fleet/formatters.py` 追加）。**dogfood 追補（#87/#88・2026-06-25・compact 後の queue 実機 dogfood で発見）**: ⑶ **#87** — `evolve-fleet queue` を実機で叩き 12 material slug 全ての行き先を実データ突合した結果、**evolve-anything（plugin 自身）が自分の queue から完全に消える** seam バグを発見。連鎖: tracked config が旧 dead path `~/matsukaze-utils/rl-anything`（rename 済→`evolve-anything` LIVE）を指す → `build_queue_result` が dir gate を material 集計より先に評価し dead は `skipped_dead` 行きで material（weak35+corr30=65）が一度も数えられない → `skipped_dead` は material 数を出さず「廃止 PJ」と誤解させる → canonical fold `rl-anything→evolve-anything` が tracked_canon 一致で `collect_untracked_materials` からも除外。**Fix 3点**: ① **rename redirect** — dead 判定で `_canonical_slug(slug)` が `untracked_dir_map`（discovery slug→live dir map）の live dir に解決できるなら live path に redirect して material を集計し waiting 候補に乗せる（canonical slug + live path で集計＝`_aliases_for` が旧 slug レコードを畳む）。② **skipped_dead 透明化** — 真の dead entry にも `weak_unprocessed`/`new_corrections`/`material_count` を添えて可視化。③ **activity_since の alias fold** — `cli._gather_queue_result` の activity_map を新設 `queue.fold_activity_counts`（slug の equivalence 集合 = 自身 + canonical + 双方 alias にわたって subagents/sessions 合算）経由にし、collectors が canonical slug でキー付けする実値（実 155 sessions が tracked slug `rl-anything` で 0 に落ち・`matsukaze-takashi` home 名に 903 誤バケツ）を weak/corr と同じ namespace に揃える。⑷ **#88** — `collect_untracked_materials` の phantom gate（`not Path(path).is_dir()`）が閾値以上 material を持つ実 dir 未解決の untracked slug（実機: temp slug `tmpdcm8avo8` material=5）を完全無音 drop していた（`skipped_dead` は透明化するのに phantom だけ不可視という非対称）。新設 `collect_phantom_materials`（`collect_untracked_materials` の補集合）が拾い result に `skipped_phantom`（slug + material_count）を追加、formatters `_append_skipped_phantom` が footer に `（skipped N phantom: <slug> (material M), … — 実 dir 未解決ゆえ除外）` を上位5件 surface（temp slug が「意図的に除外」と分かる形・waiting 非昇格）。**起票時の即時 config 修正併用**: このPCは `fleet-config.json` tracked の dead path を実パス `evolve-anything` へ修正済み（即座に本体 65 material が waiting に出現・skipped_dead 消滅を実機確認）。#87 redirect はそれとは別に「将来 rename された任意 PJ」の安全網としてコード側で根治。snapshot は追加のみ（`collect_phantom_materials`/`fold_activity_counts`）。**worker 運用**: impl-worker が2回連続 watchdog ストール（1回目=path 解決中 / 2回目=CHANGELOG 分割中・どちらも commit ゼロ）→ はまり即断で頭が worktree を引き取り formatter footer 配線 + TDD 3件 + commit 完遂。TDD・read-only 集計の修正・決定論・LLM 非依存（`fleet/queue.py` + `fleet/cli.py` + `fleet/formatters.py`）。**dogfood 追補（#79・2026-06-25・/queue 実機検証）**: `new_corrections_by_pj` の timestamp 比較を**辞書順**から **datetime 比較**（`_ts_strictly_after`／`_parse_iso` で `Z`↔`+00:00` 終端混在を吸収・naive は UTC 補完・両者パース不能のみ辞書順フォールバックで旧挙動温存）に修正。実コーパスの corrections は `Z` 終端（8件）/ `+00:00` 終端（82件）が混在し `persist_last_evolve` は `.isoformat()`＝`+00:00` を書くため `"...Z" > "...+00:00"` が同一 instant でも True になり、初回 `evolve --drain` 後に drain と同時刻帯の `Z` 終端 corr を誤計上しうる潜在シーム（現状 last_evolve=None で全件計上のため休眠・[[pitfall_iso8601_lexical_compare_tz_suffix]]）。docstring の「辞書順比較で十分」の事実誤りも訂正。TDD 2件（同一 instant 除外 / 実時刻後は計上維持）。**dogfood 追補（#91/#92・2026-06-25・/queue 実機監査）**: ⑴ **#91** — corrections の `project_path` が空/None のレコード（実機 9 件・backfill=8 / hook=1）は `_correction_slug` が空文字に落ち、どの PJ の `material_count` にも数えられず `untracked_with_material` にも `skipped_phantom` にも出ず queue から構造的に完全不可視（#86/#88 の「無音で落とさない」原則と非対称な最後の穴）。`count_unattributed_corrections`（決定論・source 別集計 `{total, by_source}`・ファイル不在は 0）を新設し `build_queue_result` result に `unattributed_corrections` キー追加、formatters `_append_unattributed` が footer に `（PJ 未帰属 corrections: 9 件 — backfill=8, hook=1 — project_path 欠落で queue 不可視）` を surface（total 0 / キー欠落は無音・後方互換・snapshot は未 re-export ゆえ不変）。今後 hook 不具合で `project_path` 欠落が増えても気づける（実測で既に hook 由来 1 件）。⑵ **#92** — 初回（`evolve-queue-state.jsonl` 不在）で全 PJ が `LAST_EVOLVE=never` なのに REASON が `weak + new corr` 表記で「一度も evolve してないのに前回以降の新規 corr」が矛盾に見えた（`new_corrections_by_pj` の `last_evolve_at=None`＝全件カウントは設計通り・値は正しい）。`select_evolve_queue` の reason を `last_evolve_at is None` のとき `weak=N + corr=M（初回・全件）>= T` に出し分け（drain 済は従来 `new corr`）。ロジック不変・表示のみ。実コーパスで全 9 待ち PJ の表記切替を実測。TDD 計10件・read-only・決定論・LLM 非依存。**dogfood 追補（A/①・2026-06-26・/queue 再監査 → tacchi 採点）**: ⑴ **A（cold-start cliff）** — 純 cold-start（`evolve-queue-state.jsonl` 不在で全待ち PJ が `never`）では `new_corrections_by_pj(last_evolve_at=None)` が増分でなく**全履歴 backlog の全件**を計上するため `material_count` は velocity（最近の勢い）でなく累積量を表し、ランキングも累積順になる。これを知らず上位 PJ を選ぶと stale な backlog を掴む（weak_signals の bootstrap_backlog #443 が weak 側で手当て済みの問題が queue corr 側は未手当てだった）。`_append_coldstart_notice`（formatters）が**全待ち PJ が `last_evolve_at is None` のとき限定**で footer に `（初回表示: corr は全履歴を計上＝累積量順で velocity ではありません。evolve 適用後は増分のみになり数値が下がります）` を surface（一部でも drained なら混在ノイズゆえ非表示・純 cold-start は初回 drain で自己消滅する一過性状態）。⑵ **①（REASON 業務語化）** — `select_evolve_queue` の reason 内部 plumbing 語 `（全件・未 drain）`（emit→drain 2 相 #402 の用語）を業務語 `（初回・全件）` に置換（毎朝 queue を叩くだけの利用者に内部用語を要求しない）。⑶ **見送り判定（tacchi 採点）**: **B（below-threshold 件数 surface）**=`tracked_total − len(queue)` で導出可能・非 actionable・「意図的フィルタ」は campaign の silent truncation（バグ的欠落）と別カテゴリゆえ見送り（near-miss が欲しければ別 issue）。**C（calibration ゲートの daily 監視）**=検出器 `evolve_introspect._detect_calibration_regression` はデータ生産者（`/evolve`）と同居ゆえ cron 化は未変化データの再スキャン＝論理矛盾、`/evolve` 実行時に拾えば十分ゆえ見送り。**②（未帰属 hook=1）**=実測で全9件が 2026-05-19 以前・以降ゼロの歴史的残渣と判明し live writer リーク否定＝観察のみ。TDD 4件（REASON 業務語 + cold-start notice の all-never / mixed silent / empty silent）・read-only・決定論・LLM 非依存。**dogfood 追補（#94・2026-06-26・/queue 実機 dogfood → 頭直接 fix）**: bootstrap で破棄/TTL 任せと判断済みの weak を queue が material から落とさず TTL（45日）まで二重計上していた seam を是正。figma-to-code が evolve セッションの bootstrap phase で 116 件を「全件破棄」し `bootstrap_done-figma-to-code.marker` を設置したのに queue は `MATERIAL=116` で待ちトップに居座った（min-sys/figma-to-code#82）。原因: `weak_unprocessed_by_pj` が `read_unpromoted(exclude_expired=True)` のみで `bootstrap_done` marker を見ず、bootstrap の「破棄＝weak_signals.jsonl 不変・marker のみ立てる」設計（TTL 自然失効に委ねる）と非連動だった。**read 時導出で是正**（`is_effectively_expired` #89 と同哲学）: `mark_done`（bootstrap_backlog）が marker に bootstrap 完了 ISO8601 時刻を書き（旧形式の空 marker は `bootstrap_done_at` の mtime fallback で後方互換）、`_exclude_bootstrap_consumed` が marker 設置時刻**より前**に `detected_at` を持つ weak を除外（tz 比較罠 [[pitfall_iso8601_lexical_compare_tz_suffix]] は `_parse_iso` で datetime 化して回避・parse 不能は安全側で残す）。marker 設置**後**の新規 weak は正当な待ちとして残し線引きが正しい。`bootstrap_consumed_by_pj` + result `bootstrap_consumed` + formatters `_append_bootstrap_consumed` が除外件数を footer に `（bootstrap 消化済みを待ちから除外: <slug> N件…）` で透明化（silent truncation 禁止）。store_registry の marker note を「ISO 時刻 1行」に更新し reader に fleet.queue を追記。TDD 13件・read-only・決定論・LLM 非依存。 **dogfood 追補（②・#96 PR 同梱・2026-06-26）**: queue footer に WEAK 列の意味を明示 — 「生検出数 vs 表示 WEAK」の乖離（実機 amamo 生64→WEAK16 は promoted 昇格済み40 + bootstrap 消化8 で正当だが手掘りなしに追えなかった）を `_append_weak_semantics`（formatters）が待ち PJ 時に `（WEAK は未処理のみ＝promoted 昇格済み・TTL 失効・bootstrap 消化済みを除いた実残数。検出された weak の生総数とは一致しません）` で surface（集計でなく定数注記＝footer ノイズと算出コストを抑制・表示のみ・ロジック/件数不変）。TDD 2件（待ち時 surface / 待ち 0 で非表示）。 **dogfood 追補（#113・2026-07-02・seam 監査）**: material の weak を content-rich channel（`correction_semantic/review_channels.REVIEW_CHANNELS` 単一ソース・複製なし）に限定。content-poor channel（esc_interrupt / manual_edit_after_ai＝y/n 確認から除外され promote しても signal_text 空で昇格不能）が実データで material の 70%（76件中53件）を占める死荷重だった。除外分は `weak_content_poor_by_pj` + result 新キー `weak_content_poor`（`--json` 後方互換追加）+ footer `（content-poor channel を material から除外: …）` で透明化（#94 と同方針・silent truncation 禁止）。read/scope/bootstrap 除外は `_scoped_kept_signals` 共有 helper に集約（partial-fix 回避）。`_append_weak_semantics` の WEAK 列説明も「content-rich 未処理のみ」に更新。TDD 11件。 |
| `daily` | 毎朝の evolve queue 自動実行 + SessionStart 通知（#80 Phase 1b）。発端: Phase 1a の `evolve-fleet queue`（学習素材ベースの evolve 待ち判定・ゼロ LLM）を**毎朝 1 回**自動実行し、結果（evolve 待ち PJ 一覧）を対話セッション開始時にユーザーが気づける形で**通知**したい。設計の核は **無人で回せるのは決定論パイプライン（ingest→queue）まで＝適用（evolve 自体）は対話セッションで人間が承認する分業**（自律進化を勝手に適用しない安全側設計）。**3 部品**: ⑴ **定期実行（macOS launchd）** — `bin/evolve-daily-install`（`--time HH:MM` 既定 09:00 / `--uninstall`・冪等）が launchd plist を生成し `~/Library/LaunchAgents/com.evolve-anything.daily.plist` に書き `launchctl load`。plist の `StartCalendarInterval` で毎朝発火、`EnvironmentVariables` に `CLAUDE_PLUGIN_DATA` を埋め、stdout/stderr を `$CLAUDE_PLUGIN_DATA/logs/evolve-daily.log` に集約（エラー時のスタックを残す）。`RunAtLoad=false`（install 時に即走らせない）。再 install は既存 plist を一旦 `launchctl unload` してから書き直し `load`（設定変更反映・二重登録回避）。⑵ **runner** — launchd が呼ぶ `bin/evolve-daily-run` が `fleet ingest`（全 PJ 発話取込・ゼロ LLM）→ `fleet queue --json`（待ち判定）を順に実行し、queue の JSON を `$CLAUDE_PLUGIN_DATA/evolve-queue.json` に保存（DATA_DIR は `rl_common.resolve_data_dir` で解決＝hook/tool 文脈の DATA_DIR 分裂を踏まない）。ingest が非ゼロでも queue は古い取込で判定できるので継続（warn ログのみ）、queue 失敗時は returncode を伝播。⑶ **通知** — SessionStart hook `restore_state._deliver_evolve_queue_notice` が `evolve-queue.json` を読み（**read-only・DuckDB 接続なし・transcript 走査なし**＝pitfall_hot_hook_eager_import 準拠）、待ち PJ があれば「evolve 待ち: figma-to-code, sys-bots（N 件）」を **systemMessage**（ADR-038 = user 向けチャネル）で 1 行 JSON 出力、`generated_at` が `stale_days`(既定2) より古ければ「queue が N 日前」advisory を付す。空 queue / ファイル無し → **無音**。env ガードは utterance staleness と同型（`data_dir_migration.is_cc_install_layout` が真の install レイアウト env のときだけ実環境 DATA_DIR を probe・テスト isolation の tmp env では沈黙して JSON stdout を汚さない）。`evolve-queue.json` は `fleet queue --json` 出力をそのまま保存した **read 専用の派生物（SoR でない）**ため **store_registry には登録しない**（store_write barrier 非対象・writer は launchd runner の単純 write）。**Phase 1a (#79) には非依存**: queue の実コードは持たず、plist は `fleet ingest`→`fleet queue --json` をシェルコマンド文字列として書くだけ、hook は queue の exact スキーマ fixture で検証する。新規 `scripts/lib/daily/`（`plist`=plist/runner コマンド/パス生成 + `LAUNCHD_LABEL`/`DEFAULT_HOUR`/`DEFAULT_MINUTE` 定数 SoT / `queue_notice`=`read_queue`+`build_queue_notice`(stale 判定・malformed generated_at は advisory なしで graceful)+`queue_notice_output` / `install`=`install`/`uninstall`・`_launchctl` に subprocess 集約で mock 可能）。TDD 27件（plist 埋め込み / 既定時刻 / plist パス / runner コマンド順 ingest<queue / read_queue 正常・欠落・破損 / 待ち一覧・空無音・None 無音・stale advisory・fresh 無 advisory・malformed graceful / systemMessage dict / install plist 書込+load・冪等 unload→load・uninstall unload+削除・plist 無し no-op・uninstall 冪等 / hook 配信有・空無音・ファイル無し無音・install 外無音・env 無し無音・副作用ゼロ・handle_session_start 配線）。決定論・LLM 非依存（`scripts/lib/daily/` + `bin/evolve-daily-install` + `bin/evolve-daily-run` + `hooks/restore_state.py`、#80） |
| `artifacts_hygiene` | artifact 衛生の決定論検出器4件（#124 #125 #126 #129・2026-07-03 PC 環境手動監査の検出器ギャップ起票分）。発端: 手動監査で見つかった「audit の死角」のうち決定論・ゼロ LLM で検出器化できる指摘を issue 化し実装（実例: グローバル CLAUDE.md が 0 バイトのまま放置 / skill-creator の Description Optimization が作る `<skill>-workspace/` 残骸 dir / `SKILL.md.backup` 残置 / `name: gstack` の 2 dir 重複）。**4検出器**: ⑴ **#124 `detect_global_claude_md`** — `~/.claude/CLAUDE.md` の未存在 / strip 後空を検出、中身があれば沈黙（healthy は None）。⑵ **#125 `detect_missing_skill_md`** — skill root（`.claude/skills` + plugin レイアウトは `skills/`・`.claude-plugin/plugin.json` ゲートで find_artifacts と整合）直下で SKILL.md を配下に 1 件も持たない dir を列挙、`EXCLUDED_SKILL_DIRS`/dot-dir 除外。⑶ **#126 `detect_backup_files`** — skills 配下の `*.md.bak`/`*.backup`/`*.orig` を列挙。⑷ **#129 `detect_duplicate_skill_names`** — `find_artifacts` 収集後に frontmatter `name` でグルーピングし**実体（非 symlink）2 件以上**のグループのみ発火、symlink wrapper（gstack の `_gstack-command` 等の正規別名参照）は重複計上から除外し `symlink_paths` に区別表示（FP 回避と可視化の両立）。全検出関数は走査 base を**引数で受け**、実 `~/.claude` はデフォルト値解決時のみ読む（テストは tmp_path fixture・実環境非依存）。section builder は `build_advisory_section`（#115 共通枠）経由で `_OBSERVABILITY_BUILDERS` に 4 key（global_claude_md / missing_skill_md / backup_files / duplicate_skill_names）登録＝markdown/構造化 両経路へ surface。builder 追加に伴う evolve result keyset snapshot は追従済み（worker targeted テストでは検出されず頭の full suite が捕捉＝#64/#38 と同型の教訓）。TDD 21件・決定論・LLM 非依存（`scripts/lib/audit/sections_artifacts.py`） |
| `memory_hygiene` | memory dir 衛生の決定論検出器3件（#127 #128 #131・同監査起票分）。**3検出器**: ⑴ **#127 索引孤児（`memory_index_orphan.py`）** — MEMORY.md 本文の `[text](file.md)` リンク集合（相対パスは basename 正規化・アンカー付き対象外）と `memory/*.md` 実ファイル集合を突合し、`unindexed_files`（索引に無い実ファイル＝想起されない）/ `indexed_missing`（実体の無い stale リンク）を検出。`memory_capability._memory_files` を再利用、MEMORY.md 不在は検査対象外。実例: update_count 56/61 の高頻度強化 pitfall 2 件が索引から不可視だった。⑵ **#128 frontmatter スキーマ検証（`memory_schema_check.py`）** — `name`（kebab-case 正規表現）/ `description` / `metadata.type`（user/feedback/project/reference の4類型）の欠落・逸脱・不正値を検出。MEMORY.md（索引・frontmatter 無し仕様）は対象外。既知の較正余地: temporal frontmatter だけ持つ既存 memory も「違反」に拾うため over-fire なら対象調整。⑶ **#131 完全重複残骸（`memory_dup_residue.py`・fleet 横断）** — `~/.claude/projects/*/memory/` の dir ペア間で `*.md` 内容 SHA256 集合を突合し、**完全包含（subset）される dir のみ**残骸候補として surface（部分重複は Phase 1 非報告＝FP 抑制）。equal 集合の双方向重複は旧 slug 側 residue 優先→dir 名の決定論タイブレークで片側固定。`PJ_SLUG_ALIASES`（read 層別名 SoT）に載る旧 slug は「rename 由来の可能性大」ラベル。**削除は tar 退避 + rm の手順提案のみ・auto-apply しない**。走査は各 memory dir 直下 `*.md` のみ非再帰・MEMORY.md（PJ ごとに divergence する索引）除外・transcripts（*.jsonl・実環境 1.9GB）非接触。`default_projects_dir` は call-time 関数（module-level ~/.claude Path 定数を `test_observability_isolation_guard` が検出するため）。実例: 旧 rl-anything memory 79 files が現 PJ の完全重複として残置（#45/#47 の read 層 alias fold は読み落としを救うが残骸自体は不可視だった）。3件とも clean 時非表示（None）で `sections_memory.py` から `_OBSERVABILITY_BUILDERS` に 3 key 登録。memory dir 解決は `resolve_cc_memory_dir` 単一ソース（CC パスエンコード・repo-basename slug と混同しない #19）。TDD 28件・決定論・LLM 非依存（`scripts/lib/memory_index_orphan.py` + `memory_schema_check.py` + `memory_dup_residue.py` + `audit/sections_memory.py`） |
