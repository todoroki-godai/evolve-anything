# コンポーネント詳細 — daily-evolve パイプライン・icebox 棚卸し

CLAUDE.md のコンポーネント表の詳細版（SoT）の一部。全体索引は [components.md](components.md) を参照。
新コンポーネント追加・既存変更時は、該当ドメインのこのファイルに詳細を書き、
CLAUDE.md のサマリ表には 1 行（名前 + 一言 + 参照）だけ追記する。

**分割経緯**: 2026-08-02、[spec/components-fleet.md](components-fleet.md) が単一ファイル閾値
（>100KB）を超えたため、daily-evolve パイプライン（fleet_queue/queue_verify/fleet_detect/daily）と
icebox 棚卸し（icebox_notice/icebox_reconcile）を意味的にまとまる単位として本ファイルへ分離した
（内容は無変更）。

| コンポーネント | 説明 |
|----------------|------|
| `fleet_queue` | 学習素材ベースの evolve 待ち列挙 `evolve-fleet queue`（#79 Phase 1a・daily-evolve Epic #78 の入口）。発端: 全 PJ から「今 evolve でレビューすべき PJ」を **決定論・ゼロ LLM** で列挙する経路が無かった。毎朝の定期実行（Phase 1b #80）から叩かれ、ユーザーが対話で処理する PJ を選ぶ。**待ち定義（学習素材ベース）**: `material_count = weak_unprocessed`（未昇格・未expired の weak_signals 件数）`+ new_corrections`（前回 evolve 以降の新規 corrections 件数）が `--threshold`（既定 **5**・env `EVOLVE_QUEUE_THRESHOLD` 上書き可・userConfig 拡張余地）以上の PJ。補助シグナル `activity_since`（subagents 等）は**フィルタに使わず列挙理由に併記**。**コア純関数** `select_evolve_queue(pj_materials, threshold)`（store I/O なし・閾値 `>=` 比較 + material_count 降順 / pj_slug 昇順ソート + reason 付与・テスト容易）。**store reader**: `weak_unprocessed_by_pj`（既存 `correction_semantic.promote.read_unpromoted(exclude_expired=True)` を再利用し pj_slug でスコープ＝weak_signals.jsonl は単一 DATA_DIR ファイル・pj_slug は record 属性）/ `new_corrections_by_pj`（corrections を `last_evolve_at` 以降で filter・state 不在=None は全件＝初回全件待ち）。`build_queue_result` が Phase 1b #80 が読む **exact スキーマ**（`generated_at`/`threshold`/`tracked_total`/`queue[{pj_slug, material_count, weak_unprocessed, new_corrections, last_evolve_at, activity_since, reason}]`）を組み立てる。**per-PJ last_evolve state**: 既存 `evolve-state.json` はグローバルで PJ 別に「前回 evolve 以降」を測れないため、新ストア **`evolve-queue-state.jsonl`** を新設（`queue_state.py`・append-only `{pj_slug, last_evolve_at, ts}` + read 側 last-append-wins fold＝reward_ema/subagent_traces と同方式で write barrier に素直に乗せる）。store_registry に **active**（`writer_locus="batch"`・retention=permanent）登録 + active store keyset snapshot 追従、書込は **store_write barrier（ADR-049）経由**で `evolve --drain` の apply 境界（非 dry-run・正準 DATA_DIR）でのみ永続化（reward_ema #64 / weak_signals #484 と同型・dry-run ゼロ書込 #308/#513）。reader（fleet queue）は read-only 純度（ファイル不在→空・**1バイトも書かない**・非作成）。**名前空間統一**: corrections の `project_path` は実コーパスでフルパス（`/Users/.../amamo`・古い hook）/ bare slug（`amamo`・#593 後）が混在するため `_correction_slug` が `project_name_from_dir`（pj_slug_fast → worktree 切詰 → basename fallback の単一ソース）で weak_signals の `pj_slug` と揃えてから突合する（新方式を発明しない・[[pitfall_copied_parse_convention_partial_fix]] 回避）。**閾値既定 5 の根拠**（ADR-044「閾値前に実データへ dry 適用」教訓）: 実 DATA_DIR dry-run（weak 373件 / corr 90件）で active PJ の material_count 最小が 5（temp dir 除く・figma 116 / rl-anything 65 / amamo 64 / atlas 24 / sys-bots 24 …）、trickle PJ（kazevolve=2）との自然 gap が 5 にあるため。**既存 `evaluate_corrections`（グローバル correction trigger）との関係＝補完（層が異なる）**: あちらは単一 PJ で correction 閾値超過時にその場で evolve を即時提案する（reactive・PJ ローカル・correction のみ）。queue は全 PJ 横断で「朝にまとめて選ぶ」proactive な入口（pull 型・weak+corr の合算）。両者は競合せず役割が直交。**実 PJ E2E**: `bin/evolve-fleet queue --json` が tracked 10 PJ・waiting 6 PJ を読み取りのみで列挙・state ファイル非作成・corrections slug 正規化（rl-anything corr=30 等）を実測。CLI `_run_queue`（text テーブル `PROJECT/MATERIAL/WEAK/CORR/LAST_EVOLVE/REASON` + `（N waiting / M tracked）` / `--json`）。TDD 25件・決定論・LLM 非依存（`scripts/lib/fleet/queue.py` + `scripts/lib/fleet/queue_state.py`、#79）。**dogfood 追補（#85/#86・2026-06-25）**: ⑴ **#85** — `activity_since.sessions` が `_gather_queue_result` でリテラル 0 にハードコードされ subagents は実値なのに sessions だけ 0 という内部不整合（session を PJ 別集計する関数が不在なのが根因）。`collectors.aggregate_sessions_by_project`（`aggregate_subagents_by_project` と対称）を新設し `session_store.read_session_records_union`（cross-dir union・read_only・#469 流儀）で読んだ session を project 別に **distinct session_id**（sessions.jsonl は 1 session 複数行ゆえ行数でなくユニーク数）で集計、project は `outcome_metrics._normalize_pj`（fanout_cost と共有の単一ソース）で slug 化・空/解決不能は `(unknown)`・30日窓・読み取り専用。⑵ **#86** — queue の母集団は `tracked_projects` 限定だが material 母集団（weak_signals/corrections の全 pj_slug）の方が広く、material を持つが tracked 外の PJ（実機: amamo material 64 / ai-daily-report 17 / daily-ai-github-trending 16 / aws-cost-guardian 9）が待ちにも skipped_dead にも出ず**完全沈黙**して真の evolve 候補を取りこぼしていた（O2）。`collect_untracked_materials`（純関数）を新設し、material slug のうち ① tracked に無い ② CC discovery（`fleet_config.filter_valid_projects(discover_cc_projects())`）で**実 dir に解決できる**（phantom/temp slug 除外ゲート＝`tmpXXXX` 等を弾く）③ `(unknown)` でない slug について `weak_unprocessed_by_pj + new_corrections_by_pj`（untracked は last_evolve state 無し＝全件）を集計し `material_count >= threshold` を降順で返す。`build_queue_result` に末尾 kwarg `material_slugs` / `untracked_dir_map`（既定 None で後方互換＝両方与えられたときのみ算出）を追加し result に `untracked_with_material` キーを追加。`cli._gather_queue_result` の `_collect_material_slugs`（weak=`read_unpromoted` の `pj_slug` / corr=`_correction_slug` で per-PJ reader と同一正規化）+ 実 dir gate map を構築して渡す。formatters `_append_untracked` が footer 下に上位5件を `（未追跡だが学習素材あり: amamo (material 64), … — tracked 追加を検討: evolve-fleet discover）` で surface（silent truncation 禁止）。**auto-track はしない**（advisory のみ・人間が `evolve-fleet discover` で承認＝pull 型思想と整合）。O1: footer の `M tracked` を `M tracked (config)` にし denominator が config 由来である旨を自己説明化（`tracked_total` は意味を変えず `len(pj_slugs)`）。#80 daily consumer（`queue_notice`）は `.get()` 防御読みのためキー追加は非破壊。TDD 追加・決定論・LLM 非依存（`fleet/collectors.py` + `fleet/cli.py` + `fleet/formatters.py` 追加）。**dogfood 追補（#87/#88・2026-06-25・compact 後の queue 実機 dogfood で発見）**: ⑶ **#87** — `evolve-fleet queue` を実機で叩き 12 material slug 全ての行き先を実データ突合した結果、**evolve-anything（plugin 自身）が自分の queue から完全に消える** seam バグを発見。連鎖: tracked config が旧 dead path `~/matsukaze-utils/rl-anything`（rename 済→`evolve-anything` LIVE）を指す → `build_queue_result` が dir gate を material 集計より先に評価し dead は `skipped_dead` 行きで material（weak35+corr30=65）が一度も数えられない → `skipped_dead` は material 数を出さず「廃止 PJ」と誤解させる → canonical fold `rl-anything→evolve-anything` が tracked_canon 一致で `collect_untracked_materials` からも除外。**Fix 3点**: ① **rename redirect** — dead 判定で `_canonical_slug(slug)` が `untracked_dir_map`（discovery slug→live dir map）の live dir に解決できるなら live path に redirect して material を集計し waiting 候補に乗せる（canonical slug + live path で集計＝`_aliases_for` が旧 slug レコードを畳む）。② **skipped_dead 透明化** — 真の dead entry にも `weak_unprocessed`/`new_corrections`/`material_count` を添えて可視化。③ **activity_since の alias fold** — `cli._gather_queue_result` の activity_map を新設 `queue.fold_activity_counts`（slug の equivalence 集合 = 自身 + canonical + 双方 alias にわたって subagents/sessions 合算）経由にし、collectors が canonical slug でキー付けする実値（実 155 sessions が tracked slug `rl-anything` で 0 に落ち・`matsukaze-takashi` home 名に 903 誤バケツ）を weak/corr と同じ namespace に揃える。⑷ **#88** — `collect_untracked_materials` の phantom gate（`not Path(path).is_dir()`）が閾値以上 material を持つ実 dir 未解決の untracked slug（実機: temp slug `tmpdcm8avo8` material=5）を完全無音 drop していた（`skipped_dead` は透明化するのに phantom だけ不可視という非対称）。新設 `collect_phantom_materials`（`collect_untracked_materials` の補集合）が拾い result に `skipped_phantom`（slug + material_count）を追加、formatters `_append_skipped_phantom` が footer に `（skipped N phantom: <slug> (material M), … — 実 dir 未解決ゆえ除外）` を上位5件 surface（temp slug が「意図的に除外」と分かる形・waiting 非昇格）。**起票時の即時 config 修正併用**: このPCは `fleet-config.json` tracked の dead path を実パス `evolve-anything` へ修正済み（即座に本体 65 material が waiting に出現・skipped_dead 消滅を実機確認）。#87 redirect はそれとは別に「将来 rename された任意 PJ」の安全網としてコード側で根治。snapshot は追加のみ（`collect_phantom_materials`/`fold_activity_counts`）。**worker 運用**: impl-worker が2回連続 watchdog ストール（1回目=path 解決中 / 2回目=CHANGELOG 分割中・どちらも commit ゼロ）→ はまり即断で頭が worktree を引き取り formatter footer 配線 + TDD 3件 + commit 完遂。TDD・read-only 集計の修正・決定論・LLM 非依存（`fleet/queue.py` + `fleet/cli.py` + `fleet/formatters.py`）。**dogfood 追補（#79・2026-06-25・/queue 実機検証）**: `new_corrections_by_pj` の timestamp 比較を**辞書順**から **datetime 比較**（`_ts_strictly_after`／`_parse_iso` で `Z`↔`+00:00` 終端混在を吸収・naive は UTC 補完・両者パース不能のみ辞書順フォールバックで旧挙動温存）に修正。実コーパスの corrections は `Z` 終端（8件）/ `+00:00` 終端（82件）が混在し `persist_last_evolve` は `.isoformat()`＝`+00:00` を書くため `"...Z" > "...+00:00"` が同一 instant でも True になり、初回 `evolve --drain` 後に drain と同時刻帯の `Z` 終端 corr を誤計上しうる潜在シーム（現状 last_evolve=None で全件計上のため休眠・[[pitfall_iso8601_lexical_compare_tz_suffix]]）。docstring の「辞書順比較で十分」の事実誤りも訂正。TDD 2件（同一 instant 除外 / 実時刻後は計上維持）。**dogfood 追補（#91/#92・2026-06-25・/queue 実機監査）**: ⑴ **#91** — corrections の `project_path` が空/None のレコード（実機 9 件・backfill=8 / hook=1）は `_correction_slug` が空文字に落ち、どの PJ の `material_count` にも数えられず `untracked_with_material` にも `skipped_phantom` にも出ず queue から構造的に完全不可視（#86/#88 の「無音で落とさない」原則と非対称な最後の穴）。`count_unattributed_corrections`（決定論・source 別集計 `{total, by_source}`・ファイル不在は 0）を新設し `build_queue_result` result に `unattributed_corrections` キー追加、formatters `_append_unattributed` が footer に `（PJ 未帰属 corrections: 9 件 — backfill=8, hook=1 — project_path 欠落で queue 不可視）` を surface（total 0 / キー欠落は無音・後方互換・snapshot は未 re-export ゆえ不変）。今後 hook 不具合で `project_path` 欠落が増えても気づける（実測で既に hook 由来 1 件）。⑵ **#92** — 初回（`evolve-queue-state.jsonl` 不在）で全 PJ が `LAST_EVOLVE=never` なのに REASON が `weak + new corr` 表記で「一度も evolve してないのに前回以降の新規 corr」が矛盾に見えた（`new_corrections_by_pj` の `last_evolve_at=None`＝全件カウントは設計通り・値は正しい）。`select_evolve_queue` の reason を `last_evolve_at is None` のとき `weak=N + corr=M（初回・全件）>= T` に出し分け（drain 済は従来 `new corr`）。ロジック不変・表示のみ。実コーパスで全 9 待ち PJ の表記切替を実測。TDD 計10件・read-only・決定論・LLM 非依存。**dogfood 追補（A/①・2026-06-26・/queue 再監査 → tacchi 採点）**: ⑴ **A（cold-start cliff）** — 純 cold-start（`evolve-queue-state.jsonl` 不在で全待ち PJ が `never`）では `new_corrections_by_pj(last_evolve_at=None)` が増分でなく**全履歴 backlog の全件**を計上するため `material_count` は velocity（最近の勢い）でなく累積量を表し、ランキングも累積順になる。これを知らず上位 PJ を選ぶと stale な backlog を掴む（weak_signals の bootstrap_backlog #443 が weak 側で手当て済みの問題が queue corr 側は未手当てだった）。`_append_coldstart_notice`（formatters）が**全待ち PJ が `last_evolve_at is None` のとき限定**で footer に `（初回表示: corr は全履歴を計上＝累積量順で velocity ではありません。evolve 適用後は増分のみになり数値が下がります）` を surface（一部でも drained なら混在ノイズゆえ非表示・純 cold-start は初回 drain で自己消滅する一過性状態）。⑵ **①（REASON 業務語化）** — `select_evolve_queue` の reason 内部 plumbing 語 `（全件・未 drain）`（emit→drain 2 相 #402 の用語）を業務語 `（初回・全件）` に置換（毎朝 queue を叩くだけの利用者に内部用語を要求しない）。⑶ **見送り判定（tacchi 採点）**: **B（below-threshold 件数 surface）**=`tracked_total − len(queue)` で導出可能・非 actionable・「意図的フィルタ」は campaign の silent truncation（バグ的欠落）と別カテゴリゆえ見送り（near-miss が欲しければ別 issue）。**C（calibration ゲートの daily 監視）**=検出器 `evolve_introspect._detect_calibration_regression` はデータ生産者（`/evolve`）と同居ゆえ cron 化は未変化データの再スキャン＝論理矛盾、`/evolve` 実行時に拾えば十分ゆえ見送り。**②（未帰属 hook=1）**=実測で全9件が 2026-05-19 以前・以降ゼロの歴史的残渣と判明し live writer リーク否定＝観察のみ。TDD 4件（REASON 業務語 + cold-start notice の all-never / mixed silent / empty silent）・read-only・決定論・LLM 非依存。**dogfood 追補（#94・2026-06-26・/queue 実機 dogfood → 頭直接 fix）**: bootstrap で破棄/TTL 任せと判断済みの weak を queue が material から落とさず TTL（45日）まで二重計上していた seam を是正。figma-to-code が evolve セッションの bootstrap phase で 116 件を「全件破棄」し `bootstrap_done-figma-to-code.marker` を設置したのに queue は `MATERIAL=116` で待ちトップに居座った（min-sys/figma-to-code#82）。原因: `weak_unprocessed_by_pj` が `read_unpromoted(exclude_expired=True)` のみで `bootstrap_done` marker を見ず、bootstrap の「破棄＝weak_signals.jsonl 不変・marker のみ立てる」設計（TTL 自然失効に委ねる）と非連動だった。**read 時導出で是正**（`is_effectively_expired` #89 と同哲学）: `mark_done`（bootstrap_backlog）が marker に bootstrap 完了 ISO8601 時刻を書き（旧形式の空 marker は `bootstrap_done_at` の mtime fallback で後方互換）、`_exclude_bootstrap_consumed` が marker 設置時刻**より前**に `detected_at` を持つ weak を除外（tz 比較罠 [[pitfall_iso8601_lexical_compare_tz_suffix]] は `_parse_iso` で datetime 化して回避・parse 不能は安全側で残す）。marker 設置**後**の新規 weak は正当な待ちとして残し線引きが正しい。`bootstrap_consumed_by_pj` + result `bootstrap_consumed` + formatters `_append_bootstrap_consumed` が除外件数を footer に `（bootstrap 消化済みを待ちから除外: <slug> N件…）` で透明化（silent truncation 禁止）。store_registry の marker note を「ISO 時刻 1行」に更新し reader に fleet.queue を追記。TDD 13件・read-only・決定論・LLM 非依存。 **dogfood 追補（②・#96 PR 同梱・2026-06-26）**: queue footer に WEAK 列の意味を明示 — 「生検出数 vs 表示 WEAK」の乖離（実機 amamo 生64→WEAK16 は promoted 昇格済み40 + bootstrap 消化8 で正当だが手掘りなしに追えなかった）を `_append_weak_semantics`（formatters）が待ち PJ 時に `（WEAK は未処理のみ＝promoted 昇格済み・TTL 失効・bootstrap 消化済みを除いた実残数。検出された weak の生総数とは一致しません）` で surface（集計でなく定数注記＝footer ノイズと算出コストを抑制・表示のみ・ロジック/件数不変）。TDD 2件（待ち時 surface / 待ち 0 で非表示）。 **dogfood 追補（#113・2026-07-02・seam 監査）**: material の weak を content-rich channel（`correction_semantic/review_channels.REVIEW_CHANNELS` 単一ソース・複製なし）に限定。content-poor channel（esc_interrupt / manual_edit_after_ai＝y/n 確認から除外され promote しても signal_text 空で昇格不能）が実データで material の 70%（76件中53件）を占める死荷重だった。除外分は `weak_content_poor_by_pj` + result 新キー `weak_content_poor`（`--json` 後方互換追加）+ footer `（content-poor channel を material から除外: …）` で透明化（#94 と同方針・silent truncation 禁止）。read/scope/bootstrap 除外は `_scoped_kept_signals` 共有 helper に集約（partial-fix 回避）。`_append_weak_semantics` の WEAK 列説明も「content-rich 未処理のみ」に更新。TDD 11件。 |
| `queue_verify` | queue の verify 待ち read-time 導出 + queue 全体状態ラベル（Epic #267）。発端: queue の REASON は material 件数のみで、直前 run で accept した提案がまだ効果検証されていない（verify 待ち）ことも、queue が空である理由が「本当に素材が無い」(EMPTY) のか「素材はあるのに処理できない」(SETUP_REQUIRED) のかも表示上区別できなかった。**新しいストアは作らない**（#267 freeze 方針）。verify 待ちは既存2レーンの accept 記録（`advisory_decision_log.jsonl` の `decision=="accept"` / `optimize_history` の `human_accepted is True`）から **read 時に純粋導出**する `scripts/lib/fleet/queue_verify.py` を新設。**「直近 run」の判定**: 両レーンの accept 記録のうち `recorded_at`/`timestamp` が最も新しいものが属する `run_id`（run_id を持たない旧 schema レコードは対象外＝黙って混ぜない）。**exposure の定義**（C2 是正）: 当初「前回 evolve 以降の sessions 数」として既存の rolling 30日窓 `activity_since["sessions"]` を転用したが、これは accept 前の古いセッションまで即座に `verifiable` を成立させ、そのセッションが30日窓から抜けると `awaiting_exposure` へ逆戻りするラッチ崩壊があった。**exposure = 「直近 run の記録時刻**以降**に開始した」distinct session 数**に是正し、`collectors.aggregate_sessions_by_project` に `since: datetime\|None` を純加算（指定時は `window_days` より優先・未指定は後方互換）、`queue_verify._exposure_sessions_since_latest_accept` / `latest_accept_event` が最新 accept の記録時刻を起点に個別クエリする（`activity_since["sessions"]` は rolling 30日の補助シグナル表示用として別途残り、exposure とは混ぜない）。exposure が 0 なら「適用したがまだ実タスクが走っていない」＝`awaiting_exposure`、1以上なら `verifiable`。**TTL 失効（I1）**: `VERIFY_PENDING_TTL_DAYS=14`。直近 run の記録時刻から 14日を**超えたら**（境界=ちょうど14日も失効側）read 時に `status="none"` として扱う（新ストア不要・forward write 不要・`weak_signals.is_effectively_expired` ＝#89 と同じ read 時 age 導出の流儀）。`compute_verify_pending` に `now: datetime\|None`（既定 `datetime.now(timezone.utc)`）を注入可能にしテスト容易化。**queue 閾値未満でも昇格（C1）**: `select_evolve_queue`（純関数・store I/O なし）は従来 `material_count < threshold` の PJ を queue から落としていたが、これは evolve 直後（material が0にリセットされた直後＝verify 待ちが最も可視化されるべき瞬間）にこそ queue から消える逆説を生んでいた。`verify_pending["status"] != "none"` の PJ は material 閾値未満でも queue に含めるよう変更し、reason の語順を反転して verify 待ちを主節にする（`format_verify_pending_promoted_reason` — 通常は material が主節・verify は末尾追記だが、閾値未満昇格 item は「なぜ出ているか」が先頭で分かるようにする）。ソート順は material_count 降順のまま変えない（verify 昇格 item は定義上 material_count が低いので自然に下位へ並ぶ・緊急度の逆転を意味しないため特別扱いしない）。**TZ 罠の是正（C3）**: `fitness_evolution.record_evolve_diff_decision` の `timestamp` が `datetime.now().isoformat()`（naive local）で書かれており、`queue_verify._parse_iso` が naive を UTC 決め打ちすると JST で 9 時間ずれ最新 run 判定・TTL 判定を誤らせていた。writer 側は aware UTC（`datetime.now(timezone.utc)`）に統一し、`_parse_iso` 側は naive を**ローカル時刻**（`dt.astimezone()`＝実行機のシステムローカル tz で解釈）として扱うよう変更（他の未修正 writer——`run_loop.py`/`genetic-prompt-optimizer/scripts/optimize.py`——が今後も naive local で書き続けるため、read 側の頑健性として必要）。**rename alias 非対称の是正（I2）**: advisory 側は `pj_slug_match` で rename alias 対応済みだが `optimize_history_store.load_history(slug)` は完全一致のみで、2レーンの rename 耐性が非対称だった（rename 済 PJ の skill lane accept が queue の verify_pending に反映されない）。`_load_optimize_history_with_aliases` が `fleet.queue._equivalence_slugs`（既存 SoT・関数スコープ遅延 import で循環回避）で alias 分の履歴を entry の `id` で dedup しつつ union read する（id 無し entry は安全に dedup できないため全件保持）。**queue_status（SETUP_REQUIRED 誤発火の是正・C4）**: `compute_queue_status` は当初 `skipped_dead` の有無だけで SETUP_REQUIRED を判定していたが、`build_queue_result` の dead PJ 分岐は material がゼロでも `skipped_dead` に append するため、**material がゼロの dead PJ だけで SETUP_REQUIRED が誤発火**していた。`material_count > 0` のものだけを blocked としてカウントするよう是正。**未帰属 corrections の永久ラッチ是正（C5）**: `count_unattributed_corrections` は project_path 欠落で帰属先 PJ が無いレコードを時刻フィルタなしで全件数えていたため、1件の古いレコードが SETUP_REQUIRED を永久ラッチさせていた。`since`（既定 None=全件・後方互換）を純加算し `build_queue_result` から直近30日窓を渡すよう是正。**advisory ストア重複走査の解消（I3）**: `build_queue_result` が PJ ごとに `read_advisory_decisions(slug)` を呼び直すと O(PJ数×ログ全体) になっていた。`attach_verify_pending`（queue_verify 側に配置・queue.py の 800行分割必須バジェットを圧迫しないため）が `read_advisory_decisions(None)` で全 PJ 分を1回だけ読み、canonical slug（rename fold 後）で group by してから per-PJ に配る（`canonicalize` は呼び側 `fleet.queue._canonical_slug` を関数注入・モジュールレベルの逆 import は循環になるため避ける）。TDD 60件超・決定論・LLM 非依存（`scripts/lib/fleet/queue_verify.py` + `scripts/lib/fleet/queue.py`（`select_evolve_queue`/`count_unattributed_corrections`）+ `scripts/lib/fleet/collectors.py`（`aggregate_sessions_by_project`）+ `skills/evolve-fitness/scripts/fitness_evolution.py`、#267） |
| `fleet_detect` | 全 PJ 横断の決定論 weak_signals 検出 `evolve-fleet detect`（#304）。**発端**: 決定論 weak_signals の永続化は `evolve --drain` の apply 境界にしか配線されておらず（#484/#513）、daily runner は `ingest`→`tokens`→`queue` の3ステップで検出を含まない。結果「evolve を回さない → 素材が生まれない → queue が `EMPTY` → 『待ちなし』表示 → evolve を回さない」の鶏卵ループになり、実測で生ログ（sessions/usage/subagents）は毎日更新される一方 corrections/weak_signals は最後の drain 実行日で 8 日間停止していた。一度も evolve していない PJ は素材が作られる機会自体が無く queue から永久に不可視。**設計**: 検出を evolve から切り離し`detect_all_projects` が全 PJ 分を回す（決定論・ゼロ LLM・`signal_key` dedup で冪等）。**母集団**は fleet config の tracked ではなく `~/.claude/projects` の実 transcript dir（tracked に載っていない PJ こそ素材が要る）。**slug 名前空間**は read 側（queue/reflect）と揃えるため `pj_id_to_path`（#304 で `pj_id_to_slug` と貪欲探索コアを共有化・隠し dir の先頭 `.` 復元を追加）→ `resolve_pj_slug` を通し、worktree transcript を本体 repo slug に寄せる（撤去済み worktree は `--claude-worktrees-` 区切りの前で解決し直す。幻 slug を queue に出さない = [[pitfall_worktree_slug_show_toplevel]] と同型）。**1 slug に複数 dir**（本体+worktree）があるため**全 dir 分を集めてから 1 回だけ `append_signals`** する（dir ごとに append すると dry-run は未書込ストアと突合して同一シグナルを dir 数ぶん重複計上し、実書込と件数が食い違う＝観測が嘘をつく。実測 803 vs 531）。errors.jsonl は 1 回だけパースして全 PJ で共有（7MB 級を PJ 数ぶん読まない）。**配線**: `bin/evolve-daily-run` の step 1c（`ingest` の後・`queue` の前、fail-open で continue）。LLM 判定を要するチャネルは従来どおり対話 evolve に残す（無人で承認待ちハングしない設計は維持）。**併せて修正**: `detect_permission_deny` が全 PJ 共有の errors.jsonl を PJ フィルタ無しでスタンプしており、PJ ごとに呼ぶと同じ deny が複製され全 PJ bit-exact 同値になっていた（measurement_bug の指紋・61 PJ × 5 件の phantom を生む）。判定を #206 の単一ソース述語 `record_project_match` に委譲。**CLI**: `--backfill`（上限 60→2000）/ `--pj`/`--dry-run`/`--json`。 | `scripts/lib/fleet/detect.py` + `fleet/cli.py` + `bin/evolve-daily-run` |
| `daily` | 毎朝の evolve queue 自動実行 + SessionStart 通知（#80 Phase 1b）。発端: Phase 1a の `evolve-fleet queue`（学習素材ベースの evolve 待ち判定・ゼロ LLM）を**毎朝 1 回**自動実行し、結果（evolve 待ち PJ 一覧）を対話セッション開始時にユーザーが気づける形で**通知**したい。設計の核は **無人で回せるのは決定論パイプライン（ingest→queue）まで＝適用（evolve 自体）は対話セッションで人間が承認する分業**（自律進化を勝手に適用しない安全側設計）。**3 部品**: ⑴ **定期実行（macOS launchd）** — `bin/evolve-daily-install`（`--time HH:MM` 既定 09:00 / `--uninstall`・冪等）が launchd plist を生成し `~/Library/LaunchAgents/com.evolve-anything.daily.plist` に書き `launchctl load`。plist の `StartCalendarInterval` で毎朝発火、`EnvironmentVariables` に `CLAUDE_PLUGIN_DATA` を埋め、stdout/stderr を `$CLAUDE_PLUGIN_DATA/logs/evolve-daily.log` に集約（エラー時のスタックを残す）。`RunAtLoad=false`（install 時に即走らせない）。再 install は既存 plist を一旦 `launchctl unload` してから書き直し `load`（設定変更反映・二重登録回避）。⑵ **runner** — launchd が呼ぶ `bin/evolve-daily-run` が `fleet ingest`（全 PJ 発話取込・ゼロ LLM）→ `fleet tokens --backfill`（増分 token 消費取込・ゼロ LLM・session_progress カーソル + INSERT OR IGNORE で冪等・既定 90 日ウィンドウ・**#157**）→ `fleet queue --json`（待ち判定）を順に実行し、queue の JSON を `$CLAUDE_PLUGIN_DATA/evolve-queue.json` に保存（DATA_DIR は `rl_common.resolve_data_dir` で解決＝hook/tool 文脈の DATA_DIR 分裂を踏まない）。ingest / token ingest が非ゼロでも queue は古い取込で判定できるので継続（warn ログのみ）、queue 失敗時は returncode を伝播（ただし第4ステップの icebox 棚卸し集計〔`icebox_notice` 行参照・#194〕は巻き添えにせず実行してから非ゼロを返す）。⑶ **通知** — SessionStart hook `restore_state._deliver_evolve_queue_notice` が `evolve-queue.json` を読み（**read-only・DuckDB 接続なし・transcript 走査なし**＝pitfall_hot_hook_eager_import 準拠）、待ち PJ があれば「evolve 待ち: figma-to-code, sys-bots（N 件）」を **systemMessage**（ADR-038 = user 向けチャネル）で 1 行 JSON 出力、`generated_at` が `stale_days`(既定2) より古ければ「queue が N 日前」advisory を付す。空 queue / ファイル無し → **無音**。env ガードは utterance staleness と同型（`data_dir_migration.is_cc_install_layout` が真の install レイアウト env のときだけ実環境 DATA_DIR を probe・テスト isolation の tmp env では沈黙して JSON stdout を汚さない）。`evolve-queue.json` は `fleet queue --json` 出力をそのまま保存した **read 専用の派生物**（SoR は fleet queue の元データで本ファイルではない）。**#399 codex round1 是正**: writer/reader が実在するため store_registry に derived_cache として宣言済み（writer は launchd runner の単純 write・store_write barrier は非経由のまま）。**Phase 1a (#79) には非依存**: queue の実コードは持たず、plist は `fleet ingest`→`fleet queue --json` をシェルコマンド文字列として書くだけ、hook は queue の exact スキーマ fixture で検証する。新規 `scripts/lib/daily/`（`plist`=plist/runner コマンド/パス生成 + `LAUNCHD_LABEL`/`DEFAULT_HOUR`/`DEFAULT_MINUTE` 定数 SoT / `queue_notice`=`read_queue`+`build_queue_notice`(stale 判定・malformed generated_at は advisory なしで graceful)+`queue_notice_output` / `install`=`install`/`uninstall`・`_launchctl` に subprocess 集約で mock 可能）。TDD 27件（plist 埋め込み / 既定時刻 / plist パス / runner コマンド順 ingest<queue / read_queue 正常・欠落・破損 / 待ち一覧・空無音・None 無音・stale advisory・fresh 無 advisory・malformed graceful / systemMessage dict / install plist 書込+load・冪等 unload→load・uninstall unload+削除・plist 無し no-op・uninstall 冪等 / hook 配信有・空無音・ファイル無し無音・install 外無音・env 無し無音・副作用ゼロ・handle_session_start 配線）。決定論・LLM 非依存（`scripts/lib/daily/` + `bin/evolve-daily-install` + `bin/evolve-daily-run` + `hooks/restore_state.py`、#80）。**#409 追補（2026-08-11・改善案の SessionStart 提示）**: 「y/n 提示にはコマンドを叩かねば辿り着けない」を解消するため、runner が queue 判定後に `daily/proposal_digest.py` の `build_proposal_digest(queue, data_dir=...)`（新しい group 化ロジックは発明せず既存 `correction_semantic.daily_review.build_review(dry_run=True)` の group をそのまま提示用に slim 化・read-only）を呼び、結果を `evolve-queue.json` の新キー `proposals`（`{generated_at, per_pj: {slug: [group,...]}, global: [group,...]}`）へ埋め込む（既存 `evolve-queue.json` の derived_cache 登録に相乗り・新ストア不要）。**global レーンの暫定基準**（issue 本文の「`~/.claude` 配下 artifact を global 扱い」案は weak_signal group に適用不能なため差し替え）: 同一 idiom テキストが 2 PJ 以上の weak_signal に出現する group を `correction_semantic.cross_pj_priority` と同じ `normalize_idiom_text` で照合し global へマージ（全 PJ 分の signal_keys を1件に統合・per_pj からは除外）。SessionStart hook `restore_state._deliver_session_proposals` が `per_pj[pj_slug] + global` を結合し、`correction_review_seen.jsonl`（digest 生成と同じ `data_dir` を明示指定した hermetic path — `daily_review.read_reviewed_keys()` の既定 union read は import 時固定の `rl_common.DATA_DIR` を経由し call-time env と食い違いうるため明示 path で回避）で既読 signal_key を含む group を除外、`MAX_SESSION_PROPOSALS=2` 件まで `hookSpecificOutput.additionalContext`（ADR-038 = Claude 向けチャネル・SessionStart は Stop/SubagentStop と異なり ADR-038 の対象外だが sessionTitle と同型のキー構造を踏襲）で「ユーザーの依頼が無いときだけ AskUserQuestion で y/n 提示せよ」という行動指示 + 各 group の代表テキスト + 回答コマンド（`bin/evolve-reflect --promote-weak <keys>` / `--reject-weak <keys> --pj <slug>`）を提示する。`reflect.py` に `--reject-weak`（`daily_review.record_reviewed(decision="rejected")` を呼ぶだけ）と `--pj`（未指定は `pj_slug.resolve_pj_slug` で解決）を新設し、既存 `--promote-weak` にも `record_reviewed(decision="promoted")` 呼び出しを追加（従来は `promoted=True` フラグのみで既読ストアへ書いておらず、同日中に再生成されない digest スナップショット越しの再提示を防げなかった）。TDD 33件（`test_proposal_digest.py` 16 / `test_restore_state_session_proposals.py` 9 / `test_reflect.py` 5 / `test_daily_run_proposals.py` 3）。決定論・read-only・LLM 非依存（`scripts/lib/daily/proposal_digest.py` + `bin/evolve-daily-run` + `hooks/restore_state.py` + `skills/reflect/scripts/reflect.py`、#409） |
| `icebox_notice` | daily runner の **icebox 棚卸し気づきトリガー**（#194）。発端: tech-eval の icebox 起票（closed + label `icebox`・再開条件付き凍結）は「再開条件を誰も監視しない」ため実質 write-only になる問題（`feedback_tech_eval_issue_gate` の運用穴）。**3 部品**: ⑴ **集計（runner 第4ステップ）** — `bin/evolve-daily-run` が ingest→tokens→queue の後に `gh issue list --repo todoroki-godai/evolve-anything --label icebox --state closed --json closedAt --limit 100`（**read-only・fieldアローリスト=closedAt のみで issue 本文は一切流入しない**＝injection 経路なし・account switch 不要・timeout 30s=`ICEBOX_GH_TIMEOUT_SECONDS`）を実行し、`$CLAUDE_PLUGIN_DATA/icebox-status.json` に `{count, oldest_days, generated_at}` を保存。**fail-open 4種**（gh バイナリ不在 OSError / TimeoutExpired / 非ゼロ exit / JSON 形状異常）はいずれも既存ファイル非破壊（書込スキップで前回生成分を残す）で daily-run を継続。queue 失敗時も icebox ステップは巻き添えにせず実行（rc は queue の returncode を伝播）。count>0 かつ closedAt パース成功 0 件（gh の JSON 形式変更等）は「恒久沈黙モード」として stderr warning `parse 0/N` で可視化。⑵ **判定** — `scripts/lib/daily/icebox_notice.py` の `build_icebox_notice(status, threshold_days=N)` が `oldest_days >= N` で発火（count/oldest_days は bool を弾く型ガード付き・欠落/非数値/非dict は沈黙）。N は呼び出し元（`restore_state._deliver_icebox_notice`）が userConfig `icebox_review_threshold_days`（既定30・`audit_interval_days` と同周期）から渡す。関数シグネチャ既定値 `DEFAULT_THRESHOLD_DAYS=90` は threshold_days 省略時のライブラリ内フォールバックとしてのみ残置。⑶ **通知** — SessionStart hook `restore_state._deliver_icebox_notice` が read → systemMessage（ADR-038）で **1行集約**（「icebox N件・最古M日。棚卸し: `gh issue list --label icebox --state closed`」形式・個別 issue 列挙は絶対にしない）。閾値未満/ファイル無し/壊れ JSON は無音。`icebox-status.json` は `evolve-queue.json` と同じ **read 専用派生物**（SoR は gh issue の closedAt）。**#399 codex round1 是正**: store_registry に derived_cache として宣言済み。TDD 39件（icebox_notice 17 / daily-run ステップ 11 / restore_state 配線 9、既存 daily-run テスト2件を4ステップ順序検証に更新）。PR #195 レビューで queue 巻き添え解消・gh timeout・parse 全滅可視化の3点を反映。決定論・LLM 非依存（`scripts/lib/daily/icebox_notice.py` + `bin/evolve-daily-run` + `hooks/restore_state.py`、#194） |
| `icebox_reconcile` | icebox（凍結 issue）棚卸しの3レーン決定論分類（#352, #353）。**発端**: #194/#351 の通知は件数と最古経過日数を告げるだけで、個々の issue が「再開条件を満たしたか」を判定しなかった。2026-08-01 の一発監査（凍結54件全件の再開条件を audit 出力と突合）で「条件文が自由文だから判定できない」のでなく「条件は観測できる前提で書かれているのに観測器（audit observability section 等）が動いていない」ことが真の障害と判明し、判定を3レーンに分ける設計に転換。**レーン定義**（優先順位 met > observer_missing > archive_candidate > None、互いに排他）: レーン1「成立」= issue 本文 `## 再開条件` 配下の fenced YAML `reopen-when:` ブロック（`source`/`metric`/`op`/`threshold`）を実ストア（weak_signals/subagent_traces/token_usage の最小 evaluator セット）と決定論突合し閾値を満たせば成立。レーン2「観測器不在」= ブロックが無い、または `source`/`metric` が EVALUATORS に未実装（年齢に関わらず適用・取りこぼし無しの受け皿）。レーン3「失効」= 判定可能だが `ARCHIVE_AGE_DAYS`=180日超・未成立・closedAt 以降に本文が実質編集されていない（archive 候補として提示のみ、自動 close はしない）。**二重配線**: 主経路は daily runner （`bin/evolve-daily-run` 第4ステップが `gh issue list --json number,body,closedAt,updatedAt` を read-only で取得し `icebox_reconcile.build_verdicts` が `icebox-verdicts.json` に保存、SessionStart hook がレーン1「成立」分のみ名指し通知）。保険経路は `audit/sections_icebox_reconcile.py` が `icebox-verdicts.json` を読むだけ（**gh を一切呼ばない**・決定論・ゼロネットワーク I/O）でレーン2/3を advisory surface（「daily runner が壊れていても pull で全景が出る」#194/#351 の教訓を踏襲）。**既読ストア** `icebox_verdict_seen.jsonl` は (issue番号, lane+closed_at ハッシュ) を軸に一度提示した verdict を再提示しない（`correction_review_seen.jsonl` と同型の物理キー集合パターン）。**PR #353 レビュー修正（一発監査で判明した「reopen-when パーサが必須キーの有無しか検証せず型・値域・長さを一切見ていない」根本原因の全件是正、B1-B9+P1-P10）の主要類型**: 型検証（B1: source/metric/op の非 str で daily runner が毎朝クラッシュする経路を `extract_reopen_when` の型検証 + `classify_issue` の metric_fn 呼び出し try/except 化で遮断、B2: `math.isfinite()` で NaN/inf threshold の偽成立を防止）、値エコー注入対策（B4: issue 本文は untrusted 入力のため `source`/`metric` をトークン正規表現で検証し reason/systemMessage に生値を一切 echo しない、`build_met_notice` に総長 cap を追加）、既読 fingerprint 安定性（B5: fingerprint から value/reason を除外し lane/closed_at のみを軸にする— token_usage 等の単調増加 metric で評価値が動くたび fingerprint が変わり同じ成立を永久に再通知する事故を根治。再凍結＝closed_at 変化は 再提示対象に戻す）、gh --limit truncation（B8: `gh --limit` を100→1000に拡大し到達時は `truncated` フラグ + stderr/audit 警告で レーン3の最古集合が構造的に消える問題を可視化）、file_lock 排他（P1: SessionStart の read-decide-print-write を `file_lock` で 1トランザクション化し同時セッションでの二重通知を防止）。他: B7 は `icebox-verdicts`/`status.json` を `atomic_write_text` 経由に変更（部分書込防止）、B9 は weak_signals evaluator を `rl_common.iter_read_data_dirs` ベースの union read に合わせ DATA_DIR 分裂時の過少カウントを解消。異常系テスト多数追加（非 str ブロック・NaN/inf・injection 文字列・複数ブロック・評価器例外・union read・pj_slug 上書き無視・truncated・file_lock 直列化等）。決定論・LLM 非依存・読み取りのみ（`scripts/lib/icebox_reconcile.py` + `scripts/lib/icebox_verdict_seen.py` + `scripts/lib/audit/sections_icebox_reconcile.py` + `bin/evolve-daily-run` + `hooks/restore_state.py`、#352, #353） |
