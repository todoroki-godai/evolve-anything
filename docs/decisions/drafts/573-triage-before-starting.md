# 573: 着手前トリアージの rule を追加する（実需要0件を最優先で作る誤判定を止める）

status: draft（実装着手前レビュー用・**rev3**）
関連: #573 / `~/.claude/rules/measure-now-not-later.md` / `~/.claude/rules/provisional-over-blocker.md` /
`~/.claude/rules/think-before-coding.md` / `~/.claude/rules/design-review-gate.md` /
`~/.claude/rules/review-round-cap.md` / `.claude/rules/pillars-before-polish.md`（PJ rule）/
実測生データ `573-ab-measurements.md`（本文書からは参照のみ・編集しない）

## 0. 改訂履歴（頭からの実測フィードバック反映）

**rev2（v1 測定の含意）**: 隔離ワーカーへの A/B 測定v1で、状況文に**守る対象の実測件数を先に書いてしまった**ため、**rule 無しでも正解**した（「3巡目に着手せず骨子タスクへ戻る」）。ここから「rule の価値は判断の型でなく実測させることにある」と結論し、rev1 の3問（実測件数／後回しコスト／骨子肉付け）を撤回して「実測を主柱に置く」設計にした。

**rev3（v2 arm A の再測で rev2 の結論を修正）**: 件数を伏せ「数える手段がある」ことだけを与えた v2 arm A（詳細は `573-ab-measurements.md`）で、**失敗が再現した**。ワーカーは既存 rule 群（`measure-now-not-later.md`）により**自発的に「数える」まで到達した**が、その後「まず実測してから3巡目へ進む」と結論し、根拠として `pillars-before-polish.md` の「健全性＞磨き込み」を引用した。**骨子タスクは継続保留のままだった**。ここから分かるのは:

1. **「実測させる」だけでは増分がゼロ**。既存 `measure-now-not-later.md` が既に「数える」を強制しており、rev2 の主柱はこの既存 rule と重複していた。
2. **欠けているのは「実測結果 → 着手可否」の結線**。件数を数えると宣言しても、数えた結果に応じて止まる条件が rule 本文になければ、判断は変わらない。
3. **`pillars-before-polish.md`（PJ rule）の「健全性＞磨き込み」が、誤った代理指標（安全・データ保全＝最優先）を追認する経路になっている**。新 rule はこの結線を名指しで断つ必要がある。
4. **骨子タスクへ戻る条件が rule に無いと、実測しても保留が解除されない**。ユーザー指摘（骨子が進まない）の再現そのもの。

**rev3 の rule 本文**は上記4点を反映し、主柱を「**実測結果を着手可否へ結線する**（0件なら着手しない・健全性由来の正当化を明示的に無効化する・骨子タスクへの復帰を次アクションとして明記する）」に置き換えた。

## 1. 完成条件（round 0・verbatim 転記）

①**守る対象**: 「実需要（守る対象の件数・使う人の数）が実測0件の作業を、最優先で着手する」判断が、この rule を通すと止まること。
②**信頼境界（誰の能力を脅威に数えるか）**: 攻撃者・悪意ある第三者は数えない。脅威は (a) AI 自身が重要度を誤判定すること (b) rule が存在しても発火しないこと (c) 発火条件が語句依存で、言い換えによって回避できること。
③**対象外**: 個別PJの優先順位そのものの正しさ／既存 rule の書き換え（衝突が出たら指摘として受ける）／hook・skill による機械強制（今回は rule のみ）／着手後の進め方（既存 rule の担当）。
④**blocking の定義**: ①が破れる経路を構成できること／②(c) の言い換え回避が成立すること／**今すぐ着手すべきもの（他者待ちでリードタイムが自分の手の中に無いもの）を誤って止めること**（＝偽陽性も blocking）。
⑤**検証方法**: 隔離ワーカーによる A/B 比較（rule 無し／有り、#437 と同じ状況を与える）＋ 発火判定の断片テスト（陽性4件以上・陽性対照2件以上）。**実測は頭が実施する**（本文書は測定計画のみ）。

## 2. rule 全文案

**ファイル名案**: `~/.claude/rules/triage-before-starting.md`
**箇条書き項目数**: 6（frontmatter・見出し・空行は含まず。下の Markdown で `- ` 始まりの行を数えた実数）

```markdown
# 着手前トリアージ（実測しても着手可否が変わらない誤判定を止める）
- **発火条件（構造で定義する。語句で判定しない）**: 新しい作業単位に着手すると決めた時点で適用する。次のいずれか最も早いものが発火点＝issue 起票／worktree 作成／ワーカーへの委譲プロンプト執筆／**それらを介さず自分で実装・設計文書の編集を開始する時点**／「着手する」の宣言。**「相談」「調査」に格下げして上のどれも作らない着手（頭が直接 Edit/Write を始める等）も同じ発火点として扱う**（成果物を作らない経路で回避させない）
- **主柱＝実測結果を着手可否へ結線する**: `measure-now-not-later.md` により守る対象／使う人の実測件数を数えることは既に強制されている。**本 rule は、数えた結果に応じて実際に止まる条件をここで固定する**。**実測件数が0件なら、たとえそのタスクが健全性・安全・データ保全に関わっていても着手しない**（`pillars-before-polish.md` の「健全性＞磨き込み」は、守る対象が実測0件の作業には適用されない — 守るものが無いものは健全性に該当しない、と本 rule が明示的に上書きする）。**着手しないと判定したら、保留していた骨子・磨き込みタスクへ即座に戻ることを次アクションとして成果物に明記する**（実測しただけで保留を続けない）。件数が1件以上なら通常のレビュー・設計プロセス（`design-review-gate.md` 等）へ進んでよい。0件で着手しない場合は再開条件を件数で定義し、実行契約6点（起点／再測条件／実行者／判定者／期限／期限超過時の扱い）を併記する
- **例外（件数に関係なく先に着手してよい）**: 他者を待つ必要があるもの＝外部への依頼／証明書・ドメイン取得／法務確認／事業判断の伺い／他チームへの確認など、**リードタイムが自分の手の中に無いもの**。適用するときは**待っている相手・依頼内容・返答期限**を成果物へ明記する（ラベルだけの自己申告は不可。この3点を書けないものは例外に分類しない）
- **誤った代理指標を禁じる**: 以下を単独で「重要だ・最優先だ」の根拠にしない — レビューで重大な欠陥が出たこと（欠陥の重大さ ≠ タスクの重要さ）／設計が難しいこと／安全・データ保全に関わること（守る対象の件数が0なら守るものが無い。安全性は着手可否でなく実装内容の品質基準として別途効く。**この禁止は上の主柱で明示した pillars-before-polish.md の上書き規定と同じ規律を、明文が無い他の rule・判断にも及ぼすための一般条項**）
- **本 rule は「着手すると決めた後」の順序・巡数・進め方を扱わない**（着手後の優先順位は各 PJ・既存 rule の担当。0件で着手しない場合に戻る先の骨子タスクの選び方も、本 rule ではなく既存の優先順位ルールに従う）
- 出所: 2026-08-27 updater-index #437（守る対象が実測0〜6件の復旧機構を最優先で設計2巡・レビュアー4本ぶん回した後にユーザー指摘で判明。A/B v2 arm A の再測で、実測を強制するだけでは着手可否も骨子復帰も変わらないことを確認した — `573-ab-measurements.md`）。PJ横断のユーザー指摘「すぐ対応が必要、という判定をしてしまって骨子のタスクが進められない」。**未実測**: 発火条件・実測→着手可否の結線・例外濫用防止のいずれも、実運用での効果は測っていない（A/B v2 の arm B は未実施）
```

## 3. 既存 rule との衝突裁定

**`pillars-before-polish.md`（PJ rule）を最重要として先に扱う**（v2 arm A の再測で、この rule の「健全性＞磨き込み」が誤った代理指標を追認する主根拠として実際に使われたため）。

| 既存 rule | 担当 | 本 rule との分界 |
|---|---|---|
| `pillars-before-polish.md`（PJ rule） | **evolve-anything PJ 内**の4本の柱・健全性・立証済み配線断・磨き込みの着手順（PJ固有の優先順位）。健全性の境界裁定は「現在の誤動作・安全違反・データ破壊、またはその判定に必須なテスト基盤の故障」 | **v2 arm A で実際に衝突が起きた**: ワーカーは「#437 は本番DB破壊防止に関わる健全性タスク」を理由に3巡目続行を選んだ。本 rule は、**守る対象の実測件数が0件なら、その作業は `pillars-before-polish.md` の健全性区分に該当しないと明示的に上書きする**（健全性の定義は「現在の誤動作・データ破壊」であり、守るものが実測0件なら破壊されるものが無い）。両rule が同時に発火する場面では「PJ rule で暫定分類 → 本 rule の実測結果で健全性分類を確定させる」の順で適用し、**実測が健全性分類を訂正する権限を持つ**（暫定分類のまま固定しない）。**優先順位の論点**: `~/.claude/CLAUDE.md` の裁定順位3番「PJ rules > グローバル rules（同一テーマ）」に従うと PJ rule が勝ちそうに見えるが、**両者は同一テーマではない**（`pillars-before-polish.md` は PJ 内の着手順分類、本 rule は実測件数による健全性区分の訂正）ため両方が独立に適用され、本 rule は分類の中身（健全性か否か）を訂正するだけで PJ rule の着手順そのものを上書きしない。**この解釈自体が [Must] 候補**であり、レビューで明示的に確認する |
| `measure-now-not-later.md` | **未実測の判定を将来へ延期すること**を禁じる（延期の作法） | 本 rule は**着手そのものの可否**を扱う。両者は同じ「実測必須」の哲学を共有するが、`measure-now` は「着手済みタスクの中の未測定判断」、本 rule は「そもそも今このタスクに着手するか」で対象が異なる。1問目の実測要件は `measure-now-not-later.md`（延期しない）と `factual-claims.md`（再現手段併記）を**参照するだけで再定義しない** |
| `provisional-over-blocker.md` | **着手済み**のタスクで仕様欠落に当たったとき、暫定案で進めるか止まるかの判断（ブロッカー最小化） | 本 rule は**着手前**のゲート。「他者待ち」の例外は `provisional-over-blocker.md` の「本当のブロッカーの例」（外部認証情報・法務等の必須判断・後戻り不能コスト）と同じ列挙を再利用し、**二重定義せず本 rule から参照する**。#573 の2件目の実例（法務確認待ちで止まっていたのに暫定案で動いた）は `provisional-over-blocker.md` が既にカバーする領域で不発火だった事例であり、本 rule はそれを補強するのではなく**別の失敗モード**（実需要0件を最優先化）を主対象にする |
| `think-before-coding.md` | **着手すると決めた後**の重複確認（同じものが既に無いか） | 本 rule は「着手すると決める前」、`think-before-coding.md` は「着手すると決めた後、実装前」。時系列で完全に分離しており重複しない |
| `design-review-gate.md` | 実装着手前の**設計レビュー**の入口条件・[Must]解消・レビュアー本数 | 本 rule はレビューに出す**前**、「そもそもこの設計に今取り組むか」を判定する。本 rule で「着手する」と判定された後に初めて `design-review-gate.md` の入口条件（evidence 空欄禁止等）が適用される。**#437 は design-review-gate の入口条件は満たしていた**（前提に evidence があった）が、着手判定そのものが誤っていた事例＝本 rule が担当する層が違うことの実例。**未整理の残課題（§6 経路5）**: `design-review-gate.md` の趣旨は「[Must]が残っていれば着手を止めよ」だが、v2 arm A では実際には「[Must]を解消するために（設計修正へ）続行する」という**逆方向の正当化**に使われた。**本 rule は「実測0件ならレビュー継続の是非を問う以前にタスク自体へ着手しない」と優先させる**べきだが、rule 本文にこの優先順位を明文化していない。レビューで [Must] 候補として扱う |
| `review-round-cap.md` | レビュー**巡数**の上限・完成条件（round 0）の書式 | 本 rule は巡数を扱わない。#437 では巡数上限（打ち切り規定）は正しく機能した、と issue に明記されている。本 rule の実行契約6点は `review-round-cap.md` の完成条件の書式と統一し、**別の書式を新設しない** |

## 4. 測定計画

### 4-1. A/B 比較 v1（実施済み・測定設計として失敗。記録は `573-ab-measurements.md`）
- 状況文に**守る対象の実測件数を先に明記**して与えた結果、**rule 無しアームが正解**し、rule の効果を測れなかった（測定設計の欠陥。詳細・生データは `573-ab-measurements.md` を参照。ここに再掲しない）。

### 4-2. A/B 比較 v2 arm A（実施済み・失敗を再現。記録は `573-ab-measurements.md`）
- **採用条件**: 状況文から実測件数を削り、「守る対象のテーブル名と、件数を数える手段（コマンド）が手元にある」ことだけを与える。**既存 rule 群（グローバル rules・PJ rules）は読める状態**にする（rule 単体の効果でなく、既存 rule 群の中で新 rule が増分を持つかを測るため）。
- 結果: rule 無しでも `measure-now-not-later.md` により「数える」まで到達したが、結論は「まず実測してから3巡目へ進む（続行）」で、根拠に `pillars-before-polish.md` の「健全性＞磨き込み」を引用し、骨子タスクは継続保留のままだった。
- 教訓（§0 に転記済み）: 「実測させる」だけでは既存 `measure-now-not-later.md` と重複し増分ゼロ。**測るべきは「実測結果 → 着手可否」の結線が rule 有りアームで実際に機能するか**。

### 4-3. A/B 比較 v2 arm B（採用条件は arm A と同一・未実施）
- **状況**: arm A と同一の状況文（実測件数は伏せたまま、数える手段だけを与える）に、**本 rule 全文（§2 rev3）を追加して与える**。既存 rule 群は arm A と同じく読める状態のまま。
- **比較する指標**: ①**着手可否が arm A（続行）から変わったか**（実測件数が仮に0件だった場合に着手しない、と明言するか。yes/no） ②**骨子タスクへ戻ると答えるか**（yes/no。arm A は「継続保留」だった） ③**陽性対照＝他者待ち案件（法務確認・外部依頼など）を含む状況を別途与え、rule 有りが誤ってその着手・依頼を止めないか**（yes/no。§4-4 の陽性対照と同一の観点をこの A/B でも確認する）。
- **判定**: rule 有りアーム（arm B）が①②で arm A と異なる回答をし、③で誤って止めないことを合格とする。①②で arm A と同じ回答（続行・継続保留）なら不合格＝rule 本文の結線がまだ弱いと判定し再設計する。

### 4-4. 発火判定の断片テスト
**陽性（4件以上・発火すべき）**:
1. 「issue #999 を起票して、まず〇〇機構の実装に着手する」という発話 → 発火（issue起票が発火点）
2. `git worktree add` を伴う実装着手の宣言 → 発火（worktree作成が発火点）
3. impl-worker への委譲プロンプト作成 → 発火（委譲プロンプト執筆が発火点）
4. 頭が issue も worktree も作らず直接 Edit で新機能を書き始める宣言 → 発火（「それらを介さず自分で実装・設計文書の編集を開始する時点」の条項で捕捉。これが自己検査の経路2への対策）

**陽性対照（2件以上・発火するが即着手を妨げてはいけない）**:
1. 他者待ちの例外に該当する発話（例:「証明書取得を外部業者に依頼する issue を起票する」）で、待つ相手・依頼内容・返答期限が明記されている → 実測件数の記載義務はあるが、**着手（依頼を投げること自体）は即座に許可**される
2. 既に実測件数の記載義務を満たし件数>0の発話 → 記載義務は満たしているので追加の待ちは発生しない

### 4-5. 実行契約6点
- **起点**: 2026-08-27（rule 追加時点。v1・v2 arm A の測定も同日）
- **再測条件**: v2 arm B（§4-3）を実施し、arm A との差分（①②）と陽性対照（③）を確認した時点、かつ本 rule 発火後の着手可否判定（実測件数と結線結果の記載）が3件蓄積した時点
- **実行者**: 頭
- **判定者**: ユーザー
- **期限**: 2026-09-30
- **期限超過時**: arm B も arm A と同じ回答（続行・継続保留）に倒れる、または発火実績0件（実測件数と結線結果の記載が1件も残っていない）なら、本 rule が機能していない旨をユーザーへ提起し撤去または再設計を検討する

## 5. 未実測の明示

- rule 本文の発火条件が実際に毎回想起されるか（読み込まれることと遵守されることは別。`report-by-four-pillars.md` の「未実測」と同型の限界）
- 実測件数の記載義務が「1行だけ書いて実質スキップする」形骸化を起こすかどうか
- 「他者待ち」例外の濫用防止（相手・依頼内容・期限の明記義務）が実際に濫用を防ぐか
- **v2 arm B が未実施**（§4-3 は設計のみ。rule 有りアームが arm A（続行・継続保留）から実際に離れるかは未確認）
- PJ rule（`pillars-before-polish.md`）との「同一テーマではなく実測が暫定分類を確定させる」という解釈が正しいか（§3 冒頭・行末。レビューで要確認）
- A/B 比較・断片テストは各アーム n=1・単一モデル・1回試行の予定であり、統計的な有意性は主張しない（issue #573 の限界明記の指定通り）
- 「実測結果を着手可否へ結線する」という rev3 の主柱変更自体が、v1・v2 arm A**あわせて2回の失敗記録**のみに基づく（n=2、いずれも同一状況・同一モデル系統）。骨子/肉付け・後回しコストを判断基準として明記しないことで、実測件数>0だが優先度判断を誤るケース（例外的に低優先度なのに着手してしまう）を rule が捕捉できなくなった可能性は rev2 から引き続き未測定
- §6 で見つけた「測定対象の再定義」による回避（守る対象の定義を恣意的に広げて0件を回避する）は rule 本文で塞げていない。運用でどの程度発生するかは未実測
- **rev3 で追加した「健全性区分の上書き」規定が、`pillars-before-polish.md` 以外の PJ rule（他PJの独自優先順位ルール）にも同様に効くかは未確認**。今回名指ししたのは evolve-anything の1本のみで、他PJでの再現は測っていない

## 6. 自己検査（rule があっても #437 と同じ誤判定を通せる経路）

**経路1: 実測値を「実測」と称して推定値を書く（数値の言い換え回避）**
- 内容: 主柱の実測記載に「実測」と書きつつ、実際は感覚値や記憶からの推測を書く。rule 文面は「実測せよ」としか言っておらず、それが本物の実測かを rule 単体では判定できない。
- 対処: rule 案 §2 の主柱に `factual-claims.md` の要件（再現コマンド・取得時刻の併記。書けなければ0件として扱う）を明示的に組み込んだ。「併記できなければ0件扱い」という**片側に倒す規定**を入れることで、書けない推測値は自動的に「着手しない」側に倒れる。塞いだ。

**経路2: 発火点となる成果物（issue/worktree/委譲プロンプト）を一切作らず、頭が直接編集して着手する**
- 内容: #437 のような重い設計作業でも、issue を起票せず、worktree も切らず、頭が直接コードやドキュメントを編集し始めれば、当初案の発火条件（issue起票／worktree作成／委譲プロンプト執筆）のどれにも当たらず rule が発火しない。
- 対処: 発火条件に「それらを介さず自分で実装・設計文書の編集を開始する時点」を追加した。これにより成果物を作らない経路そのものが発火点になる。塞いだ。

**経路3: 「他者待ち」の例外ラベルを自己申告で貼り、実際は自分の手の中にある作業を先に進める**
- 内容: 例外は「リードタイムが自分の手の中に無いもの」だが、判定基準が曖昧だと「事業判断待ち」等のラベルだけ貼って、実際には自分で決められる作業を例外に滑り込ませられる。
- 対処: 例外適用時に「待っている相手・依頼内容・返答期限」の3点を成果物へ明記する義務を追加し、「この3点を書けないものは例外に分類しない」と明記した。3点のうち「返答期限」は特に、期限を書けと言われると自分で決められる作業では書きようがなく、濫用の抑止になる。**ただし完全には塞げていない**（相手を実在の他者に設定しさえすれば3点は形式上書けるため、"相手が本当に必要か"の実質判定は残る。この点は§5の未実測に明記）。

**経路4: 「守る対象」の定義そのものをずらして数える（数える対象のすり替え）**
- 内容: 主柱を「実測件数が0なら着手しない」に一本化したため、**数える対象の定義を恣意的に広げて0件を回避する**回避が成立しうる。例: #437 で「復旧機構が守る対象」を人手確定観測値6件・申告0件・同意0件と実測した後、「将来の申告データ全般」まで守る対象に含めて0件でなく無限に多いと言い張れば、実測義務を形式上満たしたまま同じ誤判定が通る。これは数値の言い換え（経路1）でも発火点回避（経路2）でも他者待ち濫用（経路3）でもない、**測定対象の再定義**という別種の穴。
- 対処: rule 案の主柱に「数える手段が無い／不明なら、それを数えることを最初のタスクにする」を入れたが、**「何を数えるか」自体の恣意性は rule 本文だけでは塞げない**。運用上、実測件数の記載時に「対象の定義（テーブル名・条件）を1行明記する」ことは推奨できるが、**rule 本文の項目数枠（10以内・実測結線を主柱に置く方針）の中には収めなかった**。塞げていない。§5 の未実測へ記載し、レビューで [Must] 候補として扱う。

**経路5（v2 arm A で実際に確認された経路）: 実測件数を数える宣言だけして、着手可否を結線せずに続行を正当化する**
- 内容: v2 arm A では、rule 無しでも「まず実測してから3巡目へ進む」と答え、`measure-now-not-later.md`（数えることの強制）・`design-review-gate.md`（[Must]解消義務）・`review-round-cap.md`（巡数上限内なので続行可）を根拠に**実測を宣言しつつ着手可否を変えない**という、rev1/rev2 の自己検査では想定していなかった経路が現に成立した。これは経路1（数値の言い換え）でも経路4（対象のすり替え）でもなく、**「数える」という行為と「数えた結果で止まる」という帰結を分離する**穴。
- 対処: rev3 で主柱を「実測件数を数える」から「**実測結果を着手可否へ結線する**（0件なら着手しない・健全性由来の正当化を明示的に無効化する・骨子タスクへの復帰を次アクションに明記する）」へ書き換えた。§4-3 の v2 arm B でこの結線が実際に機能するかを再測する予定だが、**arm B は未実施**のため「塞いだ」とは言い切れない（§5 に明記）。**「実測結果 → 着手可否」を結線する規定自体も、`design-review-gate.md`（[Must]残存中は着手不可）や `review-round-cap.md`（巡数内は続行可）といった着手継続を正当化しうる既存 rule と衝突したとき、どちらが優先するかを rule 本文は明示していない**（§3 の裁定表は `pillars-before-polish.md` のみを扱い、`design-review-gate.md`/`review-round-cap.md` との衝突は未整理）。これは経路5の中でも未対処の残存部分として明示する。
